# GPU wedge / host-degradation event log — 2× MI50 (gfx906), official amdgpu DKMS

**Status (Kevin, 2026-09-03): tp=2 instability on this system is ACCEPTED as
hardware-related — it is a dev system. No RMA escalation, no wedge-chasing;
keep logging events + canary checks, and treat repeated wedges as an expected
operational cost (stop/retry per house recipe), not a new investigation.**

Protocol: **every** half-wedge, full wedge, and host-degradation
observation gets a row here (timestamped UTC), with a pointer to the
details in `degradation_details.md`. Sources: `journalctl -k` /
`/var/log/kern.log` (`GPU reset begin/end`, `PSP resume failed`,
`qcm fence wait loop timeout`), server logs, bench harness logs.

Legend: **HW** = half-wedge (GPU reset Source:4, recovered, "VRAM is
lost"), **FW** = full wedge (reset → `PSP resume failed` / ret −62,
GPU dead until reboot/power-cycle), **DEG** = host-state degradation
(serving stays up but sync-cadence-heavy paths collapse; see
`degradation_details.md`), **BOOT** = host boot boundary.

| UTC timestamp | GPU | type | context / trigger | evidence, outcome |
| --- | --- | --- | --- | --- |
| 2026-08-21 03:17 | 0 | FW | (pre-gfx906-session server work) | PSP resume failed, ret −62; reboot followed |
| 2026-08-21 11:26 | 0 | FW | TP=2 serving session server churn | PSP resume failed, ret −62; reboot 11:52 |
| 2026-08-21 14:21–17:35 (8×) | mostly 1 | HW | TP=2 server boot/teardown churn (S4-S6 era) | Source:4 resets, recovered each time |
| 2026-08-21 21:47–23:52 (5×) | 0/1 | HW | TP=2 serving, server restarts | recovered; plain decode unaffected |
| 2026-08-22 00:01–02:42 (9×) | 0/1 | HW | N4 plain-greedy TP=2 A/B night (fa_ab) | recovered; plain decode 40.86 t/s — unaffected |
| 2026-08-22 06:22 | 0 | HW | F7 arm weight-load SIGABRT (m32768_p0, exit 134) | recovered |
| 2026-08-22 06:46–11:39 | — | **DEG** | mtp2 TP=2 serving 3× slow (120.5 vs 40.1 ms/step post-reboot) in a boot with ≥14 prior HW resets, ~14 h uptime, ~10× 20-GB weight-load cycles | 4 separate server boots all degraded (24.90/24.93 steady); plain TP=2/TP=1 fine; trace: worker blocked in `hipEventSynchronize` ~57 ms/step; cleared ONLY by reboot |
| 2026-08-22 08:33, 10:00, 10:02 | 0 | HW | in-process TP=1 runs SIGABRT (rc=134, weight load) | recovered |
| 2026-08-22 10:26, 10:41×2, 10:43×2, 10:45 | 0/1 | HW | mtp1 TP=2 arm: 3× `hipErrorLaunchFailure` at weight load (wedge burst) | recovered per-reset; arm abandoned |
| 2026-08-22 11:03, 11:13 | 1/0 | HW | trace + async-scheduling runs, boot failures | recovered |
| — | | **BOOT** | 2026-08-22 12:39:47 reboot | DEG gone: mtp1/mtp3/mtp2 serving all healthy (33 / 45.4 / 40.1 ms/step) |
| 2026-08-22 12:53, 12:55 | 1, 0 | HW | mtpscale n3 boot attempts 1-2 failed | recovered; n3 booted 3rd try, served FAST (88 t/s) — 2 resets did NOT degrade |
| 2026-08-22 13:44 (×2), 13:46 | 0, 1 | HW | during/after si8 serve window (qcm fence timeout, failed queue evict) | si8 bench still completed at 74.8 t/s |
| 2026-08-22 13:55–14:04 (6×) | 1 | HW | si32 + mtp2-rebaseline boot failures (half-wedge cascade) | each "recovered" but next boot still failed |
| 2026-08-22 14:06 | 0 | **FW** | mtp2_rb m131072_p1 boot attempt | reset → PSP resume failed ret −62; GPU0 dead (rocm-smi N/A, 31 % zombie VRAM) at 14:09; **needs reboot** |
| — | | **BOOT** | 2026-08-22 ~14:50 reboot (GPU0 recovered, temps/clocks normal) | canary 38.6 t/s = healthy; **2 prior resets were insufficient to degrade; ≥14 were** (onset bracket) |
| 2026-08-22 15:24, 15:40, 15:41 | 1 | HW | mtp2 re-baseline boot-attempt failures | recovered; all 4 arms then benched HEALTHY (49.4/37.7/74.7/73.5 t/s) — **3 resets did not degrade** (onset data point) |
| 2026-08-22 20:05–20:12 | 1 (GPU1 44% transient, no owner) | SW | first TP=2 35B-MoE run (C2-V t2n1_off): rank-1 worker SIGSEGV-class crash in `RocmPlatform.get_device_name` (amdsmi `NOT_INIT`, masked by `with_amdsmi_context` finally); rank-0 hung on shm broadcast till SIGTERM. **No kernel reset** in window (journal clean); amdsmi broken on every run this boot (protected fallback at rocm.py:913; `get_device_name` the one unprotected caller). TP=2 arms re-ran clean with a sitecustomize shim | no GPU damage; software crash |
| 2026-08-23 05:18:29 | 0 | HW | 27B mtp2 canary (W2 session) SIGABRT rc=134; `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(1) succeeded` — first HW reset this boot. Pre-wedge: amdsmi broken since boot (08-22 20:05Z); 35B-MoE mtp2 smoke ~39.5 t/s decode (vs ~81 baseline) + baseline greedy FP non-reproducible 04:5x–05:1xZ (**later explained: the 35B MoE baseline is non-deterministic at temp=0 in this build — post-reset re-runs drift identically; not a host artifact**) | recovered; canary 38.9 t/s = healthy-for-this-boot |
| 2026-08-23 05:47:46 | 0 | HW | W2 w2_mtp2_e arm (35B-MoE mtp2 eager boot) — `hipErrorLaunchFailure` at SetDevice; same comp_1 fence timeout → `GPU reset(2) succeeded`. 4 clean engine cycles (base_g/base_g2/mtp2_g/base_e) in the 05:18–05:47 window | recovered at the time; canary re-run next |
| 2026-08-23 06:08:37 | 0 | **FW** | post-reset#2 canary: `MAPPING_ERROR 0x1` → `PSP resume failed` → `GPU reset end with ret = -62` — **GPU0 dead, needs host reboot** (3rd comp_1 fence timeout this boot in 50 min: 05:18 / 05:47 / 06:08 — burst pattern per boot/recovery procedure) | **REBOOT REQUIRED**; W2 mtp2 arms (acceptance re-run) paused until after reboot |
| — | | **BOOT** | 2026-08-23 06:33 boot (Kevin) | clean: GPU temps 33/32 °C, 0 amdgpu errors since boot, canary 38.6 t/s (healthy-for-this-model); W4 A/B on the fresh boot: 35B N=8 off/on 166.9/191.0 t/s (+14.5 %, off = C2-V record 167.4 → host clean) |
| 2026-08-23 ~08:51, ~08:52 | 0 | HW | W4 27B (Qwen3.8) N=8 arms: off arm OOM'd at util 0.93 (356 MB inductor buf, free: 0) and aborted; next (on) arm `hipErrorLaunchFailure` rc=134 at boot | closed: clean re-run at util 0.90 passed (98.2/104.2 t/s, no recurrence, 2 engines booted fine after) — OOM-teardown collateral, NOT a wedge (details file § 2026-08-23) |
| 2026-08-23 ~11:36–11:44 | 0, 1 | SW/HW? | Qwen3.8-27B TP=2 256k serve (3rd boot attempt, util 0.85): worker stuck in init on shm-broadcast wait (~11:36), 39 % VRAM pinned per GPU, GPU use 0 %, SIGTERM not honored (stuck in C extension) | SIGKILL'd (GPU idle — low P2P-mid-op risk); 2 prior TP=2 boots this session were clean, so this is a one-off init deadlock, pattern = C2-V t2n1_off shm hang; watch next boot for GPU1 wedge (known SIGKILL-TP=2 risk) |
| 2026-08-23 ~12:48 | 1? | HW? | TP=2 no-prefix-caching boot (Qwen3.8-27B 256k OOM investigation): `hipErrorLaunchFailure` at init (~12:48:46), EngineCore failed to start. Prior teardown was a clean SIGTERM (VRAM 0 % verified), two earlier boots since the 11:47 SIGKILL were clean | VRAM 0 % both GPUs, rocm-smi responsive, no zombie procs; retrying once (house recipe); if it fails again → BACO/reboot. Possible drift toward host degradation — canary probe if any further anomaly |
| 2026-08-23 17:59–18:06 (3×) | 0 | HW | OOM-hunt session canary attempts 1-3 (27B mtp2 canary, weight load): each SIGABRT rc=134 `hipErrorLaunchFailure` at shard 2/5→3, each triggering `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(2/3/4) succeeded` (18:00:02, 18:01:35, 18:06:15; reset #1 was 12:48 GPU1). VRAM clean between attempts; AMD_SERIALIZE_KERNEL=3 on attempt 3 did not change the failure point | 4 resets this boot (since 06:33) — burst pattern; STOPPED retrying per boot/recovery procedure (full-wedge risk). Canary never completed: host-health verdict deferred. Not a DEG signature per se (failures are at load, not sync-cadence), but treat all subsequent boots as suspect until a canary passes |
| 2026-08-23 18:23 | 1 | HW | OOM-hunt 9B TP=2 instrumented boot (Qwen3.5-9B, gather-gen probe): rank-1 worker `hipErrorLaunchFailure` at SetDevice → `GPU reset(2) succeeded` on 0000:0e:00.0 (GPU1, 18:23:00). VRAM 0 % after; killed cleanly (SIGTERM, no vllm procs left) | 5+ resets this boot — confirmed wedge-burst state; **host reboot requested** (Kevin offered) before any further GPU work. All OOM-hunt GPU experiments deferred to the fresh boot |
| 2026-08-23 18:35:55 | 0 | HW | OOM-hunt canary (27B, weight load): comp_1 fence timeout → reset; **`ERREVENT_ATHUB_INTERRUPT` uncorrectable hardware error latched** (on-die host fabric hub) — first on-die RAS latch of the day | recovered |
| 2026-08-23 18:39:40 | 1 | HW | OOM-hunt GPU1 canary (canary_gpu1.sh): comp_1 fence timeout → reset | recovered; GPU1 quiet after this |
| 2026-08-23 18:50:19 | 0, 1 | HW | **pcie_bif correctable RAS latch on BOTH cards within 6 ms** (18:50:19.637 GPU0 / .643 GPU1) — simultaneous dual-card timing; shared host-side cause or delayed post-reset flush (ambiguous) | counters re-latched; no wedge |
| — | | **BOOT** | 2026-08-23 19:20:55 boot (full power cycle, 6 min off per OOM-hunt doc) | first canary on the fresh boot FAILED at 19:23:30 — power cycle did NOT clear the fault |
| 2026-08-23 19:23:30, 19:35–19:46 (3×) | 0 | HW | OOM-hunt on fresh boot: canary_postpc, trace/nosdma canaries, safeopen/mater repros — all `hipErrorLaunchFailure` mid weight-load (DEVLOG-boot-failure.md §1) | each recovered via BACO reset |
| 2026-08-23 20:14:35 + 20:15:16, 20:15:48 (2×) | 0 | HW | boot-failure 2nd session (pi): pcie_bif correctable latch 41 s before a live-caught comp_1 wedge; double-wedge 32 s apart; same boot had PASSING windows (repro ×4 + full canary 38.9 t/s at 20:03–20:08; repro ×5+ after 20:16) — **intermittent die/fabric flap, GPU0 primary (16/19 resets today); GTT-exhaustion theory REFUTED (20 MiB peak of 12,260 MiB during the full 19.57 GiB load)** | recovered per reset; host usable in good windows; card-swap test recommended — DEVLOG-boot-failure.md §7 |
| 2026-08-23 20:21:33 | 0 | HW | isolated comp_1 fence timeout during the "good window" (30 s after the repro loop ended; no wedge before/after within minutes — runs 20:23:50, 20:40, 20:41 all passed) | recovered; good windows contain isolated wedges — flap, not strict good/bad phases |
| 2026-08-23 21:46:53 | 1 | HW | fa-gather-lifecycle arm A (256k needle harness, TP=2 27B, first run post-fix): worker init `hipErrorLaunchFailure` at SetDevice → comp_1 fence → BACO reset on 0000:0e:00.0 (GPU1), "device wedged, but recovered through reset". GPUs clean after (0 % VRAM); process exited cleanly. Note: GPU1 (the previously-quiet card) wedged; arm A retried | recovered; isolated wedge inside an otherwise-good window (canary 38.6 t/s at 20:41, no resets 20:21–21:46) — same flap pattern |
| 2026-08-23 22:02:01, 22:25:12 | 1 | HW | fa-gather-lifecycle arm A retries 2-3: worker-init SetDevice wedges on GPU1 each time (attempt 3 then ran 22:07–22:16 clean — full boot, graph capture — before my harness assert bug killed it) | flap cadence ~20 min on GPU1 during this session |
| 2026-08-23 22:42:12 | 0, 1 | HW | **both cards BACO-reset in the same millisecond** (0b + 0e) during arm B init — common-cause event, same signature as the 18:50:19 dual-card pcie_bif latch | host drifting (8+ resets this boot since 21:46); arm A OOM-repro SUCCEEDED on attempt 5 (22:29–22:38, the wedge-free window) |
| 2026-08-23 22:58:30 | 1 | HW | arm B init (launched 22:57:38 right after a 5.5-min clean probe window) wedged at SetDevice 52 s in | **STOPPED retrying — 12 resets this boot; good windows now shorter than the ~6-min boot; further attempts add resets toward the degraded state. Reboot (root) is the remedy; arm B + decode A/B pending on the next clean boot (harness + auto-launcher ready at /local/tmp/fa_fix/) |
| — | | **BOOT** | 2026-08-24 05:20:44 boot D (Kevin, post stop-retrying). Zero resets through a ~60-min heavy window: arm A2 OOM-repro (05:28–05:37, byte-exact 2nd occurrence), arm B 250k prefill PASS (05:44–06:19, 148 tok/s incl. prefill), MoE decode A/B (06:29+) | reboot restored the long good window — consistent with the degraded-state model; watch for the first reset to gauge boot-D flap onset |
| 2026-08-24 07:46:46 | 0 | HW | **first reset since boot D** — Qwen3.8-27B 256k TP=2 + MTP *serving* server launch (07:45:44, first GPU use since the ~06:40 A/B window): worker-init `hipErrorLaunchFailure` at SetDevice → comp_1 fence → `GPU reset(1) succeeded` on 0000:0b:00.0 (GPU0), "device wedged, but recovered through reset". Both cards probe-clean after | isolated wedge (not yet a burst); boot-D flap onset at ~2.5 h; launch retried |

| — | | **BOOT** | 2026-08-24 11:37 boot E (Kevin, post boot-D degradation) | 0 resets through ~75 min heavy use: rc2-image 27B TP=2 MTP serve + full bench session (serving canary 56.2/56.7 t/s; 27B ctx curves; 35B suites + gate + MTP + N=8 re-stamps) |
| 2026-08-24 13:00:53 | 0 | HW | **first reset since boot E** — isolated comp_1 fence timeout during the boot's 2nd server launch (greedy 27B TP=2 docker, weight load shard 2/5) → `GPU reset(1) succeeded`, worker SIGSEGV (rocr HW-exception abort, exit 139). Cards clean after (rocm-smi OK, VRAM 0/0, 31 °C) | isolated wedge in a good window (cf. 2026-08-23 20:21:33, 21:46:53); probe + one retry — 2nd wedge = burst, stop + reboot; **retry clean** (13:05, full curve done) |
| 2026-08-24 14:47–14:56 | 0,1 | HW/CPU | boot E: two threads **per TP worker** (4 cores total) burned 100% user CPU and went **frozen** — rip constant on trivial instructions (mov/cmp) in `libhsa-runtime64` `Runtime::IPCClientImport` (GPU0↔GPU1 P2P IPC path) and glibc `__poll`; vLLM Python side clean (py-spy: MainThread parked in `SpinCondition.wait`). Worker_TP0 then died (14:56:10, no OOM/MCE/kernel trace) | not a vLLM busy-wait — CPU microarch stuck-state on the P2P-IPC threads; reproducible within boot E (13:25 docker + 14:37 venv instances, from ~early start); cleared by 15:01 reboot (boot F) — watch new server for recurrence |

| — | | **BOOT** | 2026-08-25 (Kevin, power cycle post boot-F CPU-spin investigation) | fresh boot; testing a locally-patched `libhsa-runtime64.so.1.21.0` (TheRock ROCR-Runtime rebuild) with two source fixes: `IPCRecvHandle` unbounded EOF-spin fix (see `tp_stuck_threads_analyze_claude.md` §0) and `InterruptSignal::EventPool::alloc()` permanent-latch fix (§8) for the `AsyncEventsLoop`/`WaitMultiple` CPU-spin root cause, deployed via `LD_LIBRARY_PATH` |
| 2026-08-25 07:45:35 | 1 | HW | Qwen3.8-27B TP=2 MTP serve, patched-lib retest (1st launch this boot): both workers `hipErrorLaunchFailure` at `SetDevice`→`copy_()` while loading the **drafter (MTP) model**, immediately after TP0's main-model shard load completed cleanly (50 s, 5/5 shards) — `qcm fence wait loop timeout expired` → `GPU reset begin! Source: 4` → `BACO reset` → `GPU reset(1) succeeded` on 0000:0e:00.0 (GPU1), "device wedged, but recovered through reset". GPU0 (0b:00.0) showed no reset events — its crash was RCCL-collateral once GPU1's peer vanished. VRAM 0% both cards after, no vllm procs left | isolated wedge (not yet a burst); retried once per house recipe |
| 2026-08-25 07:48:15 | 1 | HW | Qwen3.8-27B TP=2 MTP serve, patched-lib retest (2nd launch, immediate retry): **identical** signature — same phase (drafter-model load, 0/5 shards), same ring (`comp_1.0.0`), same card (GPU1, 0e:00.0), `GPU reset(2) succeeded`. ~2.5 min after the 1st reset | **2nd wedge = burst per house recipe — STOPPED retrying, did not attempt a 3rd launch.** Both cards probe-clean after (28°C, 0% util); no vllm procs left. Patched-lib CPU-spin fix (`EventPool::alloc()`) not yet validated — server never reached steady-state serving either attempt, so the original CPU-spin symptom couldn't be re-checked. Not obviously caused by the library patch (failure is GPU-fence/HW, not HSA-runtime-logic; same `comp_1.0.0`/GPU1 signature recurs across many boots/library states in this log, e.g. 2026-08-23 18:23, 2026-08-24 13:00:53) but can't be fully ruled out without a clean run on the stock (unpatched) library for comparison |

| — | | **BOOT** | 2026-08-25 ~07:53 reboot (Kevin, per house recipe after the above burst) | fresh boot; `EventPool::alloc()`-patched lib retested successfully — server reached steady-state serving this time (Application startup complete), confirming the patch loads and runs correctly, but **CPU-spin symptom unchanged** (~236% CPU across 2 hot threads/worker, same as pre-patch) |
| 2026-08-25 08:21:31 | 1 | HW | Qwen3.8-27B TP=2 MTP serve (patched lib + `ROC_ACTIVE_WAIT_TIMEOUT=1000` config test): `hipErrorLaunchFailure` at weight load → comp_1 fence timeout → `GPU reset(1) succeeded` on GPU1 (0e:00.0), first reset this boot | isolated wedge; retried once per house recipe — retry succeeded, server reached steady state, confirming `ROC_ACTIVE_WAIT_TIMEOUT` does not change the CPU-spin signature (live trace: still 2 hot threads/worker in `AsyncEventsLoop`/`WaitRelaxed`, syscall trace showed zero `AMDKFD_IOC_WAIT_EVENTS` calls from either hot thread over a 4s window — confirmed pure userspace spin) |
| 2026-08-25 09:10:50 | 1 | HW | Qwen3.8-27B TP=2 MTP serve (testing a `.pth`-injected `hipSetDeviceFlags(hipDeviceScheduleBlockingSync)` shim, to test whether HIP's default active-wait scheduling — traced to `clr/hipamd/src/hip_device_runtime.cpp:823-843`, `hipDeviceScheduleAuto` resolves to `SetActiveWait(true)` whenever GPU count < CPU thread count — is the actual CPU-spin root cause): same signature, comp_1 fence timeout → `GPU reset(2) succeeded` on GPU1. Note: the `.pth` hook itself failed this run (library-path bug, fixed after: absolute path to `/opt/rocm/core-7.14/lib/libamdhip64.so` instead of bare `libamdhip64.so` name, which isn't resolvable via `LD_LIBRARY_PATH` at the point `.pth` files execute during interpreter startup) — so this wedge is NOT a data point on the blocking-sync theory, just another recurrence of the pre-existing hardware issue | **2nd wedge this boot = burst per house recipe — STOPPED retrying.** Both cards clean after; no vllm procs left. `hipDeviceScheduleBlockingSync` theory (see `tp_stuck_threads_analyze_claude.md` §8) still untested live — `.pth` hook fixed and verified working standalone (`hipSetDevice`/`hipSetDeviceFlags` both return 0 for both devices with the corrected absolute path), ready for next boot |

| — | | **BOOT** | 2026-08-25 ~09:48 reboot (Kevin, per house recipe) | fresh boot; `.pth` hook (`VLLM_HIP_BLOCKING_SYNC_TEST=1`, corrected absolute-path version) retested |
| 2026-08-25 ~09:52 | — | **FIX CONFIRMED** | Qwen3.8-27B TP=2 MTP serve with `hipSetDeviceFlags(hipDeviceScheduleBlockingSync)` applied per-device via the `.pth` hook in both TP workers (confirmed: `hipSetDevice`/`hipSetDeviceFlags` both `ret=0` for devices 0+1 in pids 2791/2792) — server reached steady state cleanly, **no GPU wedge this run** | **Root cause of the "200%+ CPU per TP worker" symptom confirmed as HIP's default active-wait scheduling (`hipDeviceScheduleAuto`), not a ROCR bug.** Hottest thread/worker: ~330 ticks/3s (~110%) → 7 ticks/5s (~1.4%); `ps` per-worker total ~236% → ~88%. Functional check: chat-completion request returned correct output, 230ms round-trip, no regression. `rocm-smi`/`journalctl -k` clean. Full writeup: `tp_stuck_threads_analyze_claude.md`. Currently a test-only `.pth` hook, not yet integrated into vLLM proper |
| 2026-08-25 10:23–11:44 | 1 | HW | Qwen3.8-27B TP=2 MTP serve while integrating the blocking-sync fix into `vllm/platforms/rocm.py` proper (`set_device()` + `VLLM_ROCM_USE_BLOCKING_SYNC` env, replacing the `.pth` test hook): 1st attempt hit `hipErrorLaunchFailure` mid weight-load → comp_1 fence timeout → `GPU reset(1) succeeded` on GPU1 (0e:00.0), isolated (1st this boot) — retried per house recipe. Retry (2nd attempt) hit a **different, non-GPU issue**: worker stuck in init on shm-broadcast wait (~11:23, then again on a 3rd attempt ~11:40), 34% VRAM pinned per GPU, GPU use 0%, no kernel reset either time (journal clean) — matches the precedented "one-off init deadlock" pattern (2026-08-23 ~11:36–11:44 entry above). 1st occurrence: SIGTERM sent, workers unresponsive >15s, escalated to SIGKILL (workspace `/local/git/AGENTS.md` TP=2-teardown guidance re-read mid-session: prefer SIGTERM+wait, SIGKILL risks a P2P-mid-op GPU1 wedge) — no wedge resulted. 2nd occurrence: SIGTERM sent, worker processes exited cleanly and VRAM released to 0%/0% within ~10s; the orphaned API-server parent process (GPU-idle by that point) still hung ~10s past that and was SIGKILL'd only after VRAM was already confirmed clear — no wedge resulted either time | no lasting host damage from either recovery; 3 launch attempts total this session (1 GPU wedge, 2 shm-broadcast hangs), each recovered per house recipe; retrying the 4th attempt next |
| 2026-08-25 12:41:24 | 1 | HW | Qwen3.8-27B TP=2 MTP serve, debugging why `hipDeviceScheduleBlockingSync` (confirmed applied + read-back correctly as `0x4` via `hipGetDeviceFlags` in both workers at `init_device()`) isn't eliminating the live CPU-spin symptom (strace still shows zero KFD wait syscalls on the hot threads despite the flag being set) — `hipErrorLaunchFailure` mid weight-load → comp_1 fence timeout → `GPU reset(2) succeeded` on GPU1 (0e:00.0) | **2nd wedge this boot = burst per house recipe — STOPPED retrying.** Unrelated to the blocking-sync debugging (crash is GPU-fence/HW during weight load, same recurring signature as always). Both cards clean after; no vllm procs left. Blocking-sync mechanism still unresolved: flag is confirmed set and stable at `init_device()`-call time, but the hot threads' actual live behavior (via strace) is unaffected — root cause of the disconnect not yet found; see `tp_stuck_threads_analyze_claude.md` for the investigation trail |
| 2026-08-25 ~13:20 | — | **FIX CONFIRMED (final)** | Qwen3.8-27B TP=2 MTP serve, fresh boot, `vllm/platforms/rocm.py`/`gpu_worker.py` **reverted to clean upstream** (the in-process integration cannot work — see below); only `docs/gfx906/gfx906-blocking-sync.pth` copied into the venv's site-packages plus `VLLM_GFX906_HIP_LIB_PATH=/opt/rocm/core-7.14/lib/libamdhip64.so.7` in the launch env | **Root cause fully resolved.** HIP bakes `ActiveWait()` into each queue's signal pool AT QUEUE-CREATION TIME (`VirtualGPU::HwQueueTracker::Create`, `clr/rocclr/device/rocm/rocvirtual.cpp:536-566`) — setting `hipDeviceScheduleBlockingSync` after the queue already exists (i.e. from any in-process vLLM call, however early) does nothing for that queue's already-baked-in signals; confirmed live (flag read back correctly as `0x4` via `hipGetDeviceFlags`, hot threads' `strace` still zero KFD syscalls). Only a `.pth` file — which runs before torch/vLLM import anything, so no HIP queue exists yet — is early enough. Live result: hottest thread/worker 6-7 ticks/5s (~1.2-1.4%), matching the very first successful test exactly. Chat completion correct. No wedge. Full mechanism + fix write-up: `tp_stuck_threads_analyze_claude.md`; install step: `docs/gfx906/running.md` §0 |
| — | | **BOOT** | 2026-08-25 19:58:22 boot G (Kevin) | fresh boot after the 08-25 blocking-sync fix session; TP=1 local-venv work: Ornith CT-asym-zp onboarding (5 clean eager runs — 2 smoke + 3 PPL, gfx906+triton arms — before the first wedge below), then the Qwen3.8-27B ngram/spec-decode A/B battery |
| 2026-08-25 22:19:02 | 0 | HW | Ornith 35B-MoE TRITON-arm serving launch (1st graph-mode launch this boot; 5 clean eager runs preceded) wedged at weight-load shard 3/5 (`hipErrorLaunchFailure` in `copy_`→SetDevice) → qcm fence timeout → BACO → `GPU reset(1) succeeded` on 0000:0b:00.0, "VRAM is lost"; rocm-smi clean after (36 °C, 0%/0%) | isolated wedge (1st this boot); retry (house recipe) clean — Ornith A/B completed that session (gfx906 65.0 t/s vs triton 3.5 t/s, DEVLOG-ornith-wna16.md) |
| 2026-08-26 00:20:25 | 0 | HW | Qwen3.8-27B ngram-battery run 1 (nospec_r1) SIGABRT at weight-load shard 1/5 (`hipErrorLaunchFailure` in `copy_`→SetDevice); ~25 s later kernel: `qcm fence wait loop timeout expired` → BACO → `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(2) succeeded` on 0000:0b:00.0, "device wedged, but recovered through reset" — reset reads as cleanup of the aborted process's mid-op queue state; the concurrent ngram_r1 run (launched 00:20:00) completed load + 89 s torch.compile + capture unaffected | 2nd HW event this boot but ~2 h apart and no live process hit — not a burst; battery continuing, a 3rd event (or two close together) stops the session + reboot (root) |
| 2026-08-26 ~00:58 | 1 | HW | GDN mixed-batch probe (9B, GPU1, first GPU1 use this boot) SIGABRT at weight-load shard 1/3 (`hipErrorLaunchFailure`, c10::AcceleratorError in SetDevice) — same weight-load-hang signature as 00:20:25/GPU0 and the recurring "degraded boot" pattern (boot G is ~6 h old, 2 prior BACO resets on GPU0). GPU1 VRAM back to ~11 MB; follow-up torch matmul on GPU1 clean | isolated wedge (different card, ~37 min after the GPU0 event); one retry per house recipe — probe rerun |
| 2026-08-26 ~00:58 | 0 | HW | Qwen3.8-27B nggpu_r4 re-run (ngram_gpu battery fix, GPU0) SIGABRT at weight-load shard 3/5 — `hipErrorLaunchFailure` — **same minute as the GPU1 probe wedge** (both processes loading 20 GB-class weights concurrently). Both cards probe-clean after (rocm-smi 0%/0%, 32/30 °C; GPU1 torch matmul OK) | **2 wedges in the same minute = burst per house recipe — STOPPED all GPU work; reboot (root) required before further inference. Pending on next clean boot: nggpu_r4 27B number (battery otherwise complete), W1 before/after mixed probes, nospec_r1 27B baseline (24.9 already observed as an artifact run, matches the 25.25 record) |
| — | | **BOOT** | 2026-08-26 05:48 boot H (Kevin, per house recipe after the 00:58 burst) | mtp2 27B canary **38.9 t/s** at 05:5x (healthy band; prior passing canaries 38.6–38.9) — host clear; W1 mixed probes + L3 nggpu_r4 re-run resumed |
| 2026-08-26 ~06:03 | 0 | HW | 9B GDN mixed-probe (W1 after-arm) SIGABRT at weight-load shard 2/3 (`hipErrorLaunchFailure` in `copy_`→SetDevice) — the recurring weight-load-hang signature (5th occurrence across boots; canary passed ~10 min earlier on this boot). GPU0 VRAM 0 %, matmul clean after | isolated wedge on boot H (1st this boot); one retry per house recipe — probe rerun |
| 2026-08-26 ~06:52 | 0 | HW | 27B W1-before-arm serving A/B SIGABRT at weight-load shard 5/5 (same `hipErrorLaunchFailure` in `copy_`→SetDevice). ~49 min after the 06:03 event with a ~45-min clean window between (10 successful loads incl. the 27B W1 after-arm) — not a 30-min burst; reads as the chronic intermittent weight-load hang | 2nd event boot H, not a burst; one retry per house recipe — before-arm rerun (a repeat failure stops the session + reboot) |
| 2026-08-26 20:05:23 | 1 | HW | TP=2 Qwen3.8-27B promotion smoke retry (4096 batched tokens): TP1 failed during weight load at `SetDevice`/`copy_()` → `qcm fence wait loop timeout` → BACO reset | recovered: GPU1 reset succeeded, VRAM 0%/both cards clean; no full-wedge signature. Details: `degradation_details.md` 2026-08-26 20:05 |
| — | | **BOOT** | 2026-08-26 20:56:19 (boot I) | fresh boot; Muse-Glimmer-30B-AWQ onboarding (TP=1, local venv): smoke PASS + first bench launch (util 0.95 OOM'd at the 532 MiB inductor prefill buffer — allocator-level, not HW) in a 22:23–22:31 clean window |
| 2026-08-26 22:41:28 | 0 | HW | Muse-Glimmer-30B-AWQ bench (util 0.95→0.93) SIGABRT `hipErrorLaunchFailure` at weight-load shard 1/5→2 → `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(1) succeeded` on 0000:0b:00.0. Preceded by the 22:30 util-0.95 OOM abort (532 MiB inductor prefill buffer, free: 0) | reset = OOM-teardown collateral, not an independent wedge (cf. 2026-08-23 ~08:51: OOM abort → next boot `hipErrorLaunchFailure` → "closed: clean re-run … OOM-teardown collateral, NOT a wedge"); VRAM 0%/rocm-smi clean after |
| 2026-08-26 22:55:42 | 0 | SW | Muse bench retry (util 0.93, warm inductor cache) booted clean (load 58 s, capture OK, KV 5.17 GiB) then OOM'd again on the first prefill (532 MiB inductor buffer, free: 0 ×2) → HSA runtime `HSA_STATUS_ERROR_OUT_OF_RESOURCES` abort in `_fwd_kernel` (rocdevice.cpp:4207), no kernel reset in journal | warm-cache profiling peak ~0.16 GiB lower than cold → KV pool oversized for the runtime inductor buffer (same 27B-dense failure mode, but the 532 MiB buffer is larger than 27B's 356 MiB so 0.93 is not enough). Fix: explicit KV cap (`kv_cache_memory_bytes=4634016400` per engine log suggestion) + `BENCH_KV_MEM` harness hook; SIGTERM'd, VRAM 0% after |
| 2026-08-26 ~23:38 | 0 | HW | all-CUSTOM Muse bench probe (eager, 2 GiB KV cap) SIGABRT `hipErrorLaunchFailure` at weight-load shard 3/5; journal clean (no BACO/reset), rocm-smi 0% after | 2nd launch-failure this boot, but ~60 s after the previous run's OOM-teardown exit (23:37) — OOM-collateral pattern (cf. 22:41 entry, 2026-08-23 08:51 precedent); one retry per house recipe |
| 2026-08-26 ~23:59 | 0 | HW | all-CUSTOM Muse bench (graph, 0.75 GiB KV, BENCH_BATCHED_TOKENS=2048) SIGABRT `hipErrorLaunchFailure` at weight-load shard 3/5; journal clean, rocm-smi 0% after | 3rd launch-failure this boot, again immediately after an OOM-teardown exit (23:57) — both HW events this boot follow OOM aborts, matching the collateral pattern rather than independent wedges; retrying (a 3rd consecutive failure stops the session + reboot) |
| 2026-08-27 ~00:0x | 0 | — | retry of the ~23:59 run: weights loaded clean (100%), graph capture OK, first-prefill OOM at free 128–132 MiB (allocator-level, expected at this KV cap) — then a BENCH_BATCHED_TOKENS=1024 re-run loaded clean and completed the full 4-sample bench (the window-FA gate, 27.90 t/s) | no further launch-failures; both boot-I HW events were OOM-collateral. Session complete |
| 2026-08-27 08:2x–08:3x | 0 | HW | Muse-Glimmer-30B-AWQ B=4 split-K bench arm (2nd launch this follow-up session) SIGABRT `hipErrorLaunchFailure` ("unspecified launch failure") at weight-load shard 4/5, ~11 h 45 min into boot I. **Unusual confounder: a 16-way clang build of the gfx906 FA extension was running concurrently** (first time observed with a heavy CPU build overlapping a weight load). Journal/dmesg unreadable (no root) — BACO/reset status unknown; rocm-smi clean after (both cards 0%/0%, 38/31 °C, normal clocks) | 1st launch-failure since the ~00:0x window-FA session (not OOM-collateral: no OOM precedes it this session — the two prior bench runs this session loaded clean at 08:1x and 08:2x). Isolated → one retry per house recipe at 08:41: loaded clean, full bench completed. No further events |
| 2026-08-27 14:19:25 | 0 | SW | LEGACY=0 Q8-side-buffer smoke (review-round-2 blocker check) OOM SIGABRT `aten::empty` in `gptq_gemm` (AOT runtime) — uncapped 104k-token KV pool + ~1.5 GiB Q8 side buffer + inductor headroom exceeded 32 GiB (allocator-level, expected: side buffer inflates the KV cost ~35%) | teardown recorded for the collateral window; retry with 0.375 GiB KV cap completed (garbage output — the expected desync finding, not HW) |
| 2026-08-27 14:30:4x | 0 | HW | default-config (LEGACY=1) smoke SIGABRT `hipErrorLaunchFailure` at weight-load shard 2/5, ~11 min after the 14:19:25 OOM teardown — the recurring OOM-collateral signature (cf. 22:41, ~23:38, 23:59 entries) | OOM-collateral pattern; rocm-smi responsive + VRAM 0% (silent-wedge variant); one retry per house recipe |
| 2026-08-27 14:32:0x | 0 | HW | retry of the 14:30 smoke SIGABRT `hipErrorLaunchFailure` at engine init (before weight load), ~13 min after the OOM teardown; rocm-smi still clean (0%/0%, 33/31 °C, 938 MHz) |**2 consecutive launch failures = burst per house recipe — STOPPED all GPU work; reboot (root) required. Code work for review round 2 is committed (45/45 suite + pre/post P1 hardware repro completed before the wedge); pending on next clean boot: none blocking (LEGACY=0 desync finding stands, Phase C serving re-gate is downstream of the desync fix) |
| — | | **BOOT** | 2026-08-27 ~14:52 (boot J, per house recipe after the 14:32 burst) | rocm-smi both cards clean on arrival (0%/0%, 32 °C, 938 MHz); mtp2-27B canary **38.8 t/s** (healthy band; prior passing canaries 38.6–38.9) — host clear |
| 2026-08-27 17:40:33 | 0,1 | SW | Muse-Glimmer-30B **TP=2** ngram serve (first launch this boot; uncapped pool 9.02 GiB/GPU → 1.36M tokens, util 0.82) OOM `aten::empty` in `gptq_gemm` (AOT runtime) on the first real 4096-chunk prefill (all prior requests <100 tok): steady state 14.48 (weights+non-torch) + 1.28 (graphs) + 9.02 (KV) = 24.78 GiB of 31.98 physical left <7.2 GiB, and the runtime bt4096 inductor prefill buffer exceeded it (profiled peak only 2.73 GiB — warm/cold gap, larger than the known 0.16 GiB case). Engine **force-killed** the workers (SIGKILL mid TP=2 P2P op) | allocator-level, expected (cf. the TP=1 532 MiB case): the big pool consumed the headroom. Fix: `--kv-cache-memory-bytes 6442450944` (6 GiB/GPU ≈ 900k tok, still 3.5× the 256k max) leaves ~10 GiB headroom. Recorded for the collateral window |
| 2026-08-27 17:45:49 | 0,1 | HW | TP=2 relaunch (with the KV cap) failed at worker init / SetDevice, `hipErrorLaunchFailure` both ranks, ~5 min after the OOM force-kill | OOM-collateral + the documented **TP=2 SIGKILL-mid-P2P → next-init wedges** signature. Discriminator: TP=1 mtp2-27B canary **38.4 t/s healthy** at ~17:49 (no P2P involved) → P2P-path stall, not host degradation; one retry per house recipe |
| 2026-08-27 17:52:06 | 0,1 | HW | retry of the 17:45 launch failed again, `hipErrorLaunchFailure` both ranks, 22 s into weight load; journal unreadable (no root) — BACO/reset status unknown; rocm-smi clean after (both 0%/0%, 32–33 °C, 938 MHz) | **2 consecutive launch failures after the mid-P2P SIGKILL = the documented wedge needing a BACO reset (root) or reboot — STOPPED all GPU work per house recipe.** Pending on next clean boot: Muse TP=2 ngram serving numbers + prefill sweep (pp2048/8192/16384 × tg256/512; grid script `/local/tmp/muse/bench_serve_grid.py`, server args in `/local/tmp/muse/muse_tp2_ngram3.log` header) + the README row. TP=1 work needs a fresh canary first |
| — | | **BOOT** | 2026-08-27 ~18:16 (boot K, after the 17:52 stop) | both cards clean on arrival (0%/0%, 32–33 °C, 938 MHz); mtp2-27B canary **38.8 t/s** (healthy band; prior canaries 38.4–38.9) — host clear |
| 2026-08-27 18:23:30 | 1 | HW | Muse TP=2 serve (1st boot-K launch, 6 GiB KV cap) hit the chronic weight-load hang at shard 2/5→3 (`hipErrorLaunchFailure` in `copy_`→SetDevice, both ranks) → kernel `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(1) succeeded` on 0000:0e:00.0 at 18:23:47, "device wedged, but recovered through reset" | isolated chronic weight-load hang (6th occurrence across boots; same signature as 08-26 06:03/06:52, 08-27 08:2x); rocm-smi clean after reset; retry per house recipe — retry loaded clean (KV pool 904,164 tok = the 6 GiB cap, capture 0.89 GiB, startup OK 18:28:07) |
| 2026-08-27 ~18:29 | 0,1 | SW | the clean retry then died silently as a whole process group ~1 min after startup — no kernel events, no OOM-kill in kern.log, VRAM fully released (0%/0%) | NOT HW: the launching shell call was interrupted by the operator and its process group was SIGKILLed (nohup only guards SIGHUP). Lesson: launch servers detached (setsid) or from a stable terminal; poll readiness in a separate call. The 6 GiB-cap config is validated through load/pool/capture; remaining validation = the first real 4096-token prefill (the boot-J OOM site) |
| 2026-08-27 18:41:32 | 0,1 | SW | operator-relaunched Muse TP=2 serve (128k max, 6 GiB KV cap = 848,301 tok, bt4096) OOM'd on the **first real 4096-chunk prefill** — `aten::empty` in `gptq_gemm` (AOT inductor runtime), both ranks, the identical 254 MiB last-straw alloc as boot J (266,338,304 B), free: 0; no GPU event (kern.log clean) | 2nd allocator OOM of this config family (cf. boot J 17:40:33 at the 9.02 GiB pool): steady 14.48 + 6.0 + 0.93 = 21.4 GiB left 10.6 GiB headroom, and the first-prefill transient exceeded it. The transient scales with the batched-token chunk — all 4 data points fit linear-in-M (TP=1: bt1024 survives / bt2048 OOMs at ~6.8 GiB headroom; TP=2: bt4096 OOMs at 7.2 and 10.6 GiB headroom) — because the startup profile measures the **eager** dummy-run peak (2.73 GiB), not the piecewise-compiled prefill graph's materialized intermediates. Neither `gpu_memory_utilization` (sizes the pool from the same wrong number) nor the 6 GiB cap fixes it. Fix: **bt2048** (halves the transient → ~4–7 GiB margin at the 6 GiB pool) — **confirmed 18:55Z**: bt2048 relaunch cleared the OOM site (4106-token prefill, ttft 10.2 s / 401 prefill t/s, 0 OOM, coherent output); full prefill grid then ran clean (`docs/gfx906/_bench_serve_grid_gfx906.py`, README row updated) |
| 2026-08-27 20:41 | 0 | HW | in-process OOM-attribution probe (custom arm, TP=1, 0.5 GiB KV cap, PP=4097) wedged GPU0 at 20% into weight load: `terminate ... c10::AcceleratorError: CUDA error: unspecified launch failure` (7th chronic weight-load-hang occurrence across boots; same family as 08-26 06:03/06:52, 08-27 08:2x, 18:23) — note this was the **5th in-process probe launch on boot K** (custom3/4/6 died in host-level inductor autotune crashes, see DEVLOG-muse-glimmer round 3; the wedges accumulate on this boot) | driver self-recovered: VRAM back to 10.8 MB baseline on both cards, 0% util, ~2 min after the crash. Isolated → 1 retry per house recipe; a 2nd wedge in the retry = burst → stop + reboot (root) |
| 2026-08-27 21:58 | 0 | HW | OOM-attribution probe re-run (post q_pad-ClassVar fix verification, custom arm, same config as 20:41) wedged GPU0 ~20 s into weight load: same `unspecified launch failure` (8th occurrence across boots; 4th GPU incident on boot K: 18:23 GPU1, 20:40 GPU0, 21:58 GPU0) | first self-recovery (GPU0 back to 11.2 MB by ~22:00, GPU1 clean throughout); a 30 s torch canary passed at ~22:03 |
| 2026-08-27 22:06 | 0 | HW | **retry of the 21:58 probe wedged again** — 2nd consecutive launch failure on this boot's tail (`unspecified launch failure`, same signature), and after this one the driver did NOT release: GPU0 stuck at **24.9 GB zombie VRAM**, 0% util (vs the 20:40/21:58 self-recoveries) | **BURST per house recipe (2 consecutive launch failures) → ALL GPU WORK STOPPED, host needs a REBOOT (root)** — BACO reset needs root and the 2nd-failure rule stops short of even trying it. Pending post-reboot: (1) q_pad-ClassVar fix verification probe (survival + transient at the 0.5 GiB cap — `attr_tp1_custom19_fixed_retry.log` was the wedged attempt), (2) M1 gather-clip e2e A/B (pp8192/B=1, `GFX906_FA_GATHER_CLIP` 1 vs 0), (3) bt4096 TP=2 serving re-validation (the fix should remove the 6.7 GiB/GPU q_pad growth — the bt2048 workaround may be droppable), (4) the gfx906-mem-attribution skill (roadmap Housekeeping) |
| 2026-08-28 12:43 | 0,1 | HW | two in-process harness runs (G1 B=2 / G2 B=4 clip A/B, boot L ~6.5 h in) **both** died at weight load with `c10::AcceleratorError: CUDA error: unspecified launch failure` (Exllama load path, before any FA kernel) — 1st wedge of boot L (earlier noted "clean throughout" is superseded); rocm-smi self-recovered (0%/0% both cards) within ~1 min | isolated (single observation, both cards affected simultaneously — host-level suspicion per the degradation signature); retry G1 once per house recipe — a 2nd launch failure = burst → stop + reboot (root). Note: the canary was skipped on operator instruction ("card was working just now"); if the retry's t/s land low, suspect the host, not the code |
| 2026-08-28 12:54 | 0,1 | HW | 2nd observation of boot L: the NEXT in-process pair (G1 n=2 / G2 n=4 clip A/B relaunch, same Exllama weight-load path) both aborted `unspecified launch failure` again; rocm-smi self-recovered to the 10.8 MB baseline both cards. The 12:43 pair's retry (G1, ~12:52) had loaded clean and produced record-matching numbers (6.06 t/s @ B=1/pp8192 vs 6.042 record) — host was healthy mid-interval, so these are recurring isolated wedges, not a full wedge. Note: both double-wedge pairs this boot were CONCURRENT two-card launches; the clean runs were single-card — untested hypothesis, launches now serialized | isolated (2nd observation; the 12:52 success broke the 12:43 chain) → G1 retried once per house recipe; launches serialized from here (no concurrent two-card in-process work). A 3rd wedge observation OR this retry failing = burst → stop + reboot (root) |
| 2026-08-28 14:38 | 0,1 | HW | **G3 LEGACY=0 TP=2 bake retry** wedged at weight load: `hipErrorLaunchFailure` in `copy_`→SetDevice, Worker_TP1 both ranks (`/tmp/g3_legacy0_serve.log`). The 1st G3 attempt (~14:20) died first with a CODE error — a capture-unsafe D2H sync (`int(cu[...])` loop) in the direct-paged Sq>1 Q-pad, hit for the first time in production by LEGACY=0 + ngram spec + B≥2 decode (Sq=6 under FULL capture); fixed in-tree (uniform fast paths, 57/57 suite) — so this wedge is either collateral to that teardown (cf. the documented mid-P2P-teardown → next-init wedge) or a 3rd accumulation | **3rd boot-L wedge observation = BURST per the 12:54Z protocol entry → ALL GPU WORK STOPPED; REBOOT (root) required.** Pending post-reboot: the G3 LEGACY=0 TP=2 serving bake (recipe in degradation_details item 9; control = boot L LEGACY=1 records 111.5/99/46.7 @2k/8k/B=4). Pattern: all 3 boot-L wedges were two-card launches; all 6 single-card runs clean |
| — | | **BOOT** | 2026-08-28 ~14:52 (boot M, post-burst reboot) | both cards clean on arrival (0%/0%, 33 °C, 938 MHz); mtp2-27B canary **38.9 t/s** (healthy band; recent canaries 38.4–38.9) — host clear. Pending: the G3 LEGACY=0 TP=2 bake |
| 2026-08-28 15:00 | 0,1 | HW | 1st G3 attempt on boot M (LEGACY=0 TP=2 serve) wedged ~5.5 min in, at weight load: `hipErrorLaunchFailure` both ranks (chronic weight-load-hang family, 10th occurrence across boots; TP=2 load is the usual trigger); canary 38.9 t/s at ~14:58 (2 min prior) → host healthy at launch | isolated (1st boot-M observation) → driver self-recovered to the 10.8 MB baseline within ~4 min; one retry per house recipe. A 2nd launch failure on boot M = burst → stop + reboot (root) |
| 2026-08-28 17:18 | 0,1 | HW | M6 Part B bake arm 1 (LEGACY=0 + `DIRECT_PAGED_Q8=1` TP=2 serve — 2nd two-card launch this boot) wedged at weight load shard 4/5→5: `hipErrorLaunchFailure` in `copy_`→SetDevice, Worker_TP0 (both ranks); kernel `GPU reset(2) succeeded` on 0000:0e:00.0 (GPU1) + `GPU reset(1) succeeded` on 0000:0b:00.0 (GPU0) at 17:18:27, "device wedged, but recovered through reset" (chronic weight-load-hang family, 11th occurrence across boots) | the ~2 h clean window since 15:00Z (unit suite 60/60 + in-process B=4 bench + canary **38.4 t/s** ~16:59Z, all GPU0) breaks the 15:00 chain (cf. boot L 12:52 precedent) → isolated observation; driver self-recovered to 0 % VRAM both cards. One retry per house recipe — **a retry failure = burst → stop + reboot (root)** |
| 2026-08-29 ~05:5x–06:00:02 | 0 | HW | M2 e2e A/B first arm (in-process Muse, 2×130k prefill, GPU0) hung: GPU 0 % use + main thread 200 % CPU spin; kernel `qcm fence wait loop timeout expired` (05:59:59) → `GPU reset(2) succeeded` (06:00:02) on 0000:0b:00.0, "device wedged, but recovered through reset". First boot-M wedge on a SINGLE-CARD launch and on running inference (not load). NOT reproducible on the pre-M2 path — later bisection: pp16384 completes, pp32768/65536 pass-1 ran ~214–256 t/s (see the resolution row below) | boot-M 3rd wedge observation, ISOLATED (clean runs broke the 17:18 chain); driver self-recovered, VRAM 0 % verified. E2e A/B re-scoped to pp16384 (within-boot ratio gate) |
| 2026-08-29 ~07:55–08:08 | 0 | — | **Resolution of the ~240 t/s prefill question (fresh boot N, ~2 min old, 0 wedges, canary 38.9 t/s)**: same Muse 32k in-process prefill shape — warmup 135.48 s + sample 136.51 s, matching boot M's 136 s exactly (no pass-to-pass drift) | **the ~240 t/s in-process TP=1 prefill rate is the TRUE rate, not degradation** — boot M's prefill was normal; its only genuine anomaly is the 05:59Z fence wedge. Consistency: the ~500 t/s records are TP=2 (32.8k/500 = 65.6 s ≈ 135/2.06 — prefill scales ~2× with TP). Baseline now in the README Muse row. (`/tmp/m2iso_32k_bootN.log` boot-volatile; `/local/tmp/m2iso_32k_bootN.log` persists) |
| — | | **BOOT** | 2026-08-29 ~15:38 (boot O, post-burst reboot of boot N) | both cards clean on arrival (0%/0%, 10.8 MB baseline); mtp2-27B canary **39.3 t/s** (healthy band; boot N canaries 38.9 ×4) — host clear. Pending: bake arms B (LEGACY=0) and C (LEGACY=0 + direct-paged) on `feat/fa-legacy0-b1-decode`; arm A control 39.76/40.12 t/s already recorded on boot N (`/local/tmp/b1ab_armA.log` — same model/recipe; note it was a DIFFERENT boot, so the A/B/C bake runs on boot O for a true same-boot comparison) |
| 2026-08-29 15:51:02 | 1 | HW | B=1 bake arm A 1st attempt (LEGACY=1 TP=2 Qwen3.8-27B, boot O, canary 39.3 t/s clean ~2 min prior) wedged at weight load (shard 5 transition): kernel `qcm fence wait loop timeout expired` + `unsuccessful queues preemption` on 0000:0e:00.0 (GPU1) → PSP UNLOAD_TA fail (0x117) → BACO reset → `GPU reset succeeded` + resume; userspace `unspecified launch failure` both ranks (chronic load family, 14th occurrence across boots) | isolated (1st boot-O observation; a bare two-card torch/RCCL probe passed clean ~4 min later, then arm A's retry loaded + served + benched clean) → NOT a burst; retry per house recipe. Pattern note: 4th consecutive boot (L, M, N, O) with two-card vLLM-load wedges, always GPU1 (0000:0e:00.0) in boot O |
| 2026-08-29 16:09:11 | 1 | HW | B=1 bake arm C 1st attempt (LEGACY=0 + DIRECT_PAGED=1 + DIRECT_PAGED_Q8=1 TP=2 serve) wedged at weight load: same GPU1 (0000:0e:00.0) `qcm fence wait loop timeout` → queue-evict fail → KFD quiesce fail → GPU reset (chronic load family, 15th occurrence) | NOT a burst: clean runs between the 15:51 and 16:09 wedges (bare two-card RCCL probe; arm A retry full cycle incl. serving + bench; arm B full cycle incl. serving + bench + clean teardown) break the chain per the boot-L 12:52 precedent → arm C retried once. If the retry wedges too, treat the boot as degraded: stop, report, reboot |
| 2026-08-29 13:40:36 + ~13:42:40 | 0,1 | HW | B=1 LEGACY A/B bake, arm B (LEGACY=0 TP=2 Qwen3.8-27B serve, boot N) wedged at weight load TWICE: 1st attempt 13:40:36, retry 13:42:05→13:43:04 — both `hipErrorLaunchFailure` on both ranks at the load path (chronic weight-load-hang family, 12th+13th occurrences across boots). Context: boot N's first wedges; arm A (same model, LEGACY=1) had loaded and served clean ~13:25–13:39 on the same two cards; in-process probes (GPU0) clean since ~13:00; all recent clean runs single-card, all recent wedges two-card (the boot-L pattern) | **BURST per house recipe (2 consecutive launch failures) → ALL GPU WORK STOPPED; REBOOT (root) required.** Driver self-recovered both cards to the 10.8 MB baseline / 0 % after each. Pending post-reboot (branch `feat/fa-legacy0-b1-decode`): the B=1 LEGACY=1-vs-0 serving bake — arm A control numbers already recorded (B=1 39.76/40.12 t/s @ pp2048/tg256, `/local/tmp/b1ab_armA.log`); arms B (LEGACY=0) and C (LEGACY=0 + DIRECT_PAGED=1 + DIRECT_PAGED_Q8=1) still to run, plus the canary first. Kernel-level probe is done and in the branch (LEGACY=0's Q8-gather path is 22–45% FASTER than LEGACY=1 per step; M5-era direct-paged B=1 is +8–35% slower — the serving gate adjudicates transfer) |
| 2026-08-29 18:45:32–18:46:33 | 0 | HW | (found post-hoc in kern.log 2026-08-30 during the Nemotron TP=2 investigation) 8× `Fence fallback timer expired on ring page1` on 0000:0b:00.0 (GPU0); no userspace incident was logged at the time and rocm-smi looked healthy afterwards | UNRECORDED incident on boot O (last table row before it was 16:09:11). Nemotron TP=1 validation at ~08-30 05:4x–05:5x (same boot, mamba SSD path incl. `get_device_name`) ran clean AFTER this event, so the amdsmi breakage below started later — but this is the only unexplained kernel event in the window; keeping it on the record per protocol |
| 2026-08-30 13:45 | 0,1 | SW | Nemotron-3.5-Lightning TP=2 + `--enable-expert-parallel`: rank-1 worker died in the first mamba SSD config lookup (`selective_state_update → get_ssm_configs → get_device_name`, cudagraph-profile dummy run) — `AMDSMI_STATUS_NOT_INIT` from `with_amdsmi_context`'s `finally: amdsmi_shut_down()` masking the query; rank-0 then spun 100 % CPU + GPU (NCCL) until SIGTERM | **Same signature as the 2026-08-22 C2-V 35B-MoE entry** (rank-1 `get_device_name` death in `profile_run`, no kernel events, rank-0 hang) — pure software. Fresh-process probe: post-torch `amdsmi_init()` returns success with 0 handles and `shut_down()` raises NOT_INIT, deterministic both single- and dual-GPU. amdsmi was still working at ~08-30 05:5x (Nemotron TP=1 validation) — the state flipped between 05:5x and 13:3x (int8 probes ran 06:40–12:5x in between; no kernel events in that window found). Fixed in-tree: `with_amdsmi_context` cleanup no longer masks a successful query (rocm.py, `tests/test_rocm_amdsmi_context.py`) — the permanent fix the 08-22 entry called for. TP=2+EP then booted and served coherent output (5/7 greedy A/B token-identical vs TP=1). Boot O now logs the `Failed to get total memory via amdsmi` fallback in every run → treat boot O as degraded for perf purposes |
| 2026-08-30 14:2x | 0,1 | DEG | Same-boot perf anomaly during the above A/B: TP=1 Nemotron decode ran ~7 tok/s on the first request warming to ~35, while TP=2+EP on the same boot ran ~100 tok/s B=1 (steady) — a TP-dependent pattern (degradation docs' open question on TP-dependence; prior DEG entries hit spec-decode on both TPs) | NOT adjudicated: needs the mtp2-27B canary (skipped — no canary has run on boot O since the 15:38 boot canary; per the 08-22 "Canary this boot" protocol a canary reading settles host health) or a clean boot. Numbers recorded in `DEVLOG-nemotron-h.md` (2026-08-30 entry). Do not use this boot's TP=1 numbers as baselines |
| 2026-08-30 18:3x–18:5x | 0 | DEG | NH-5 serving A/B (Nemotron **TP=1**, same boot O, ~4 h after the 14:2x collapse): three consecutive server boots (OFF/ON/OFF arms) all steady **106–115 t/s** B=1 decode (~2048-tok prompt / tg256) — the TP=1 collapse did **not** reproduce; amdsmi-fallback warning lines still present in all three server logs | The 14:2x TP=1 collapse was NOT a stable boot-O state — TP=1 numbers from the 18:3x–18:5x window are usable (they carry the NH-5 A/B). The collapse itself remains unexplained (TP-dependence question stays open; the window-to-window flip on one boot is a new data point for it). No canary has been run on boot O in this window either |
| — | | **BOOT** | 2026-08-30 20:51:32 (boot P) | fresh boot. Two chronic two-card load wedges early in the boot (below), then the C2 combined A/B session. (An 8932 qwen38-27b serve came up 08-31 07:49 during this boot and was down by ~11:0x — no longer running; agent inference is served by a separate machine, so local GPUs are free for dev/bench work.) |
| 2026-08-31 02:55:13 | 0 | HW | (interactive session, pre-cron) qcm fence wait loop timeout → BACO reset on 0000:0b:00.0; `GPU reset(1) succeeded`, "device wedged, but recovered through reset" | chronic weight-load-hang family (occurrence across boots); isolated; work continued clean after |
| 2026-08-31 04:24:55 | 1 | HW | (interactive session, pre-cron) same signature on 0000:0e:00.0 (GPU1) → BACO → `GPU reset(1) succeeded` | isolated (different card, ~1.5 h after the GPU0 event); clean windows between — intermittent pattern, not a burst |
| 2026-08-31 08:49:34 | 0 | HW | C2 combined A/B TP=1 `MOE_M1=1` arm (Qwen3.5-35B-A3B-AWQ, in-process harness) wedged at weight-load shard 9/9: `qcm fence wait loop timeout expired` + unsuccessful queue preemption → BACO reset on 0000:0b:00.0; userspace `unspecified launch failure` (chronic weight-load-hang family, ~16th occurrence across boots). **NOT a MOE_M1 kernel fault**: the flag only affects decode-time gemm2 dispatch — the engine died during load, before any MoE GEMM ran; the v2 tile source is unchanged since C2-V measured it clean (08-22/23) | 3rd reset this boot but intermittent (clean windows + a full TP=1 off arm at 82.37 t/s between events) → isolated, one retry per house recipe; post-reset canary both GPUs 16 TFLOP/s. **TP=2 arms moved to systemd user services (MemoryMax=infinity)**: the first in-process TP=2 engine was OOM-killed by the hermes worker cgroup (4 GiB cap) at NCCL init — software, no GPU event; under systemd the off arm completed clean (81.58 t/s, fp-matched to TP=1, 0 resets). Full record: `degradation_details.md` "2026-08-31 boot P C2 combined A/B" |
| 2026-09-01 14:00:45 | 1 | HW | (interactive MTP-1 session) first Qwen3.8-27B TP=2 **MTP** serve launch this boot (venv, util 0.85, maxlen 131072): worker-init `hipErrorLaunchFailure` → kernel `qcm fence wait loop timeout expired` + unsuccessful queues preemption → BACO reset on 0000:0e:00.0 (GPU1) at 14:00:45, `GPU reset(2) succeeded`; VRAM clean after; retry (14:03) loaded clean and served the full sweep | chronic weight-load-hang family (~17th occurrence across boots); **4th reset this boot** but intermittent (clean windows + ~1 h of healthy C2 A/B work between events) → isolated, NOT a burst. NOTE: amdsmi-fallback warnings present in MTP-1 server logs (boot-P state, cf. boot O 08-30 marker); MTP-arm sweep numbers matched S9 within noise but the greedy arm ran ~20–35% below S9 at long ctx — **reboot requested to rule out host-state degradation before trusting the crossover data** |
| 2026-09-01 20:29:04 | 0 | HW | (interactive MTP-1 session, **post-reboot boot Q**) in-process TP=2 **profiler** launch (`LLM()` util 0.90 maxlen 131072) wedged at weight-load phase: userspace `hipErrorLaunchFailure` → kernel `GPU reset succeeded` on 0000:0e:00.0 (**GPU0**) at 20:29:04, `VRAM is lost due to GPU reset!`, PSP resume + `GPU reset(1) succeeded`; both GPUs pass matmul probe post-reset; profiler retry pending | chronic weight-load-hang family (~18th occurrence across boots); **first HW reset on boot Q** (fresh reboot ~16:35) — the in-process weight-load trigger recurs even on a clean-booted host. Does NOT invalidate MTP-1 sweep data (both arms completed 20:09, pre-event). Reinforces house practice: prefer serve-based measurement for load-sensitive work |
| 2026-09-02 03:12:52 | both | HW | (interactive MTP-1 session, boot Q) **old-vLLM docker image** `aiinfos/vllm-gfx906-mobydick:v0.23.1rc0.x-rocm7.2.1-pytorch2.11.0` TP=2 serve launch (ROCm 7.2 userland inside container, host kernel driver 6.8.12-acso): both GPUs `HW Exception ... GPU Hang` at weight-load shard ~2/5 → kernel: `VRAM is lost due to GPU reset!` + PSP resume → `GPU reset(1/2) succeeded`, `[drm] device wedged, but recovered through reset` on **both** 0000:0b:00.0 (GPU0) and 0000:0e:00.0 (GPU1); userspace segfault in `__clone`; post-reset both GPUs pass matmul canary | **first dual-GPU simultaneous hang on this host** — signature differs from the chronic single-GPU weight-load-hang family (~19th occurrence across boots). Strong suspect: old ROCm 7.2 container userland vs modern kernel driver mismatch (flagged risk before launch; docker GPU path uses explicit /dev/kfd+/dev/dri mapping, no CDI) |
| 2026-09-02 06:10:17 | 0 | HW | (interactive MTP-1 session, boot Q) **old-vLLM 0.23.1 in-process** serve via PYTHONPATH on MODERN ROCm 7.14 userland (platform pin via sitecustomize; all env issues fixed): GPU0 `unspecified launch failure` at weight-load shard ~2/5 → kernel BACO reset on 0000:0e:00.0, `[drm] device wedged, but recovered through reset`, `GPU reset(3) succeeded` | **same signature as the 03:12 docker wedge (old 0.23.1 code + this AWQ model at weight load, ~40% shard)** on BOTH userlands → root cause is the old vLLM code path (predates our gfx906 weight-load/FA fixes), NOT userland or my env. **NEW: zombie KFD handle** — dead worker PID 44222 (gone from /proc, kill -9 no-op) still holds 10.99 GB GPU0 per rocm-smi --showpids; kernel-level amdgpu leak post-reset that only a reboot clears. GPU0 now ~23 GB free → cannot host TP=2 @0.93 (~29.7 GB). **Old-vLLM A/B abandoned as not-runnable on this host without reboot**; remaining MTP-1 work (rocprofv3 decode breakdown, docs) also blocked by the leaked handle |
| 2026-09-02 08:54:47 | 0 | HW | (interactive MTP-1 session, **boot R** post-reboot) phase-profiler run #1: in-process TP=2 `LLM(...)` with **compilation mode NONE** + util 0.90 + max_model_len 131072: GPU0 `unspecified launch failure` (`hipErrorLaunchFailure`) during worker init/weight-load → kernel BACO reset on 0000:0e:00.0, `[drm] device wedged, but recovered through reset`, `GPU reset(1) succeeded`; matmul canary passed after | **isolated by control**: identical in-process TP=2 canary (stock compile config, util 0.85) passed clean at 39.2–39.3 t/s both BEFORE (08:31) and AFTER (09:2x). Config delta under suspicion: mode-NONE / util 0.90 / max_model_len 131072 — but boot-Q 20:29:04 wedge was ALSO in-process util 0.90 maxlen 131072 with STOCK compile mode, so **mode-NONE is not the common variable; high-util in-process weight load is** (serve-based TP=2 @0.93 never wedges). NOT yet isolated to util vs maxlen (one data point each way; uncontrolled variable: rocprofv3 hung children killed -9 at 08:45–47 in between); profiler retry drops to util 0.85. Also this session: **all GPU device-tracing paths confirmed dead on this ROCm build** — torch.profiler 0 events, rocprofv3 kernel-trace SIGABRT+empty CSV, sys-trace/PMC child hangs, `--attach` SDK failure (see /local/tmp/mtp1/profiling_paradox.md) |
| 2026-09-02 10:56:21 | 0 | HW | (interactive MTP-1 session, boot R) phase-profiler run #3 (v3 plugin, mode-NONE, util 0.85): engine init + weight load CLEAN this time; GPU0 `unspecified launch failure` during **cold prefill @122880** — last log line before crash: `qwen_gdn_linear_attn.py:507 Falling back to the Triton GDN decode path: the fused CUDA kernel requires a BF16 GDN m[ask]` (AWQ-INT4 → FP16 activations, so the Triton GDN decode path is active) → kernel BACO reset on 0000:0e:00.0, `device wedged, but recovered through reset`, `GPU reset(2) succeeded`; matmul canary passed after | **GPU0 has now wedged 3× in this session** (08:54 init, 10:56 prefill) + boot-Q 20:29 — chronic single-GPU weight/prefill-hang family on GPU0 specifically; run #2 (same code path, same shape) completed the identical prefill clean minutes earlier → non-deterministic HW event, not a reproducible software bug. GDN-Triton-decode-at-120k noted as the active kernel at crash time (coincidence vs cause undetermined). One retry authorized before escalating to user for reboot decision |
| 2026-09-02 15:4x | 0 | HW | (interactive MTP-1 session, **boot S** post-reboot) phase-profiler diagnostic run (v4-diag plugin, mode-NONE, util 0.85): `hipErrorLaunchFailure` at engine/worker init → kernel BACO reset on 0000:0e:00.0; VRAM clean after, no zombie PID | GPU0 wedge #4 today (session); same in-process-LLM()+mode-NONE family as 08:54/10:56 |
| 2026-09-02 16:43:34 | 0 | HW | (interactive MTP-1 session, **boot S** post-reboot) phase-profiler **MTP arm** (v5 plugin, mode-NONE, util 0.85): `hipErrorLaunchFailure` at engine/worker init → kernel: `qcm fence wait loop timeout expired` + **"The cp might be in an unrecoverable state"** + `Failed to evict process queues` + `Failed to quiesce KFD` → BACO reset on 0000:0e:00.0, `GPU reset begin`; VRAM clean after, no zombie PID; both GPUs pass matmul canary | **GPU0 wedge #5 today (session)** — same in-process-LLM()+mode-NONE family; the "cp unrecoverable" kernel wording is a step stronger than prior events. NOTE the pattern: ALL 5 wedges this session are in-process `LLM()` + mode-NONE (+120k ctx); serve-based TP=2 (sweep, canary) has NEVER wedged. Greedy arm of the SAME v5 config completed clean at 16:2x minutes earlier → non-deterministic race, not deterministic code bug. **Decision point reached — see /local/tmp/mtp1/RESUME_PHASE_PROFILE.md** |
| 2026-09-02 19:57:57 | 0 | HW | (interactive MTP-1 session, boot S) **MTP-1b k=1 arm**: standard `vllm serve` startup (STOCK compilation config with cudagraph capture [1,2,3,4], util 0.85, maxlen 131072, TP=2) wedged at CUDA-graph-capture phase: userspace `unspecified launch failure` → kernel **"The cp might be in an unrecoverable state"** + BACO reset on 0000:0e:00.0 (GPU0), `GPU reset succeeded`; VRAM clean, no zombie PID; matmul canary passed after; retry (20:0x) started the same server clean and it serves | **GPU0 wedge #6 today — BREAKS the pattern**: first serve-based TP=2 wedge on this host (all 5 prior were in-process LLM()+mode-NONE or old-vLLM). Same "cp unrecoverable" kernel wording as #5. Six non-deterministic GPU0 wedges across two boots with canaries passing between each → **strong evidence of genuine HW degradation, not a config-family bug**. Per protocol: one retry authorized (taken); if the k=1 sweep wedges again, STOP and escalate for RMA/replace decision |
| 2026-09-02 23:24:15 | 0 | HW | (interactive MTP-1 session, boot S) **MTP-1b-0 causation check**: k=2 server on the kv-split-fixed build with `GFX906_FA_KVSPLIT=1` drop-in, standard serve startup; worker init failed at `SetDevice` (`hipErrorLaunchFailure`) then same "cp might be in an unrecoverable state" + BACO reset | **GPU0 wedge #7 today.** Hit at SetDevice - before ANY FA kernel ran - so unrelated to the kv_split change. Matmul canary passed after; VRAM clean. **Seven non-deterministic GPU0 wedges across two boots, canaries passing between each.** Per protocol: STOP further serve-startup poking this session; the causation question is answered by the already-completed A/B (2.38-2.80x gain with the fix) + unit-level correctness instead of a KVSPLIT=1 serve re-run |
| 2026-09-03 06:47:08 | 0 | HW | (post-reboot gate, boot T) **PPL-gate control run** (`GFX906_FA_KVSPLIT=1`, in-process TP=2): mtp2-arm startup wedged at ~75 s after the greedy arm of the same run had completed cleanly. Kernel: "device wedged, but recovered through reset" (BACO), VRAM lost + reclaimed. Canary passed both GPUs immediately after (3.71/10.35 TFLOPS fp16). **8th non-deterministic GPU0 wedge; 4th at a server/engine startup since boot R.**
| 2026-09-03 11:29:03 | 0 | HW | (interactive SYV-4 session, boot T) SYV-4 e2e A/B arm-A launch (in-process TP=2 dense 27B, util 0.93, stock compile config) wedged at weight-load shard 3/5: `hipErrorLaunchFailure` both ranks → kernel `Fence fallback timer expired on ring comp_1.0.0` + BACO reset on 0000:0b:00.0 (GPU0), "device wedged, but recovered through reset"; rocm-smi --showpids clean after (no zombie KFD handle) | **9th non-deterministic GPU0 wedge** — same chronic in-process TP=2 weight-load-hang family as 06:47 this boot. Host accepted HW (2026-09-03 header). One retry per house recipe; if the retry wedges, stop + escalate for reboot |
| 2026-09-03 16:45:26 | 1 | HW | (interactive SYV-3 session, boot T) in-process TP=2 phase-profile `LLM()` launch (util 0.90, maxlen 131072, mode-NONE, drafter-lm_head hooks) wedged at weight load: userspace `c10::AcceleratorError: unspecified launch failure` → kernel `Fence fallback timer expired on ring comp_1.0.0` + BACO reset on 0000:0e:00.0 (GPU1), "device wedged, but recovered through reset"; VRAM back to 10.8 MB baseline both cards, no zombie PID | **10th non-deterministic wedge — first on GPU1 this boot** (boot T had 2× GPU0 at 06:47/11:29). Same chronic in-process-LLM() weight-load-hang family; note the J2G-1 serve-based TP=2 launches all booted clean today (3×) — in-process remains the trigger family. SYV-3 measurement switched to a standalone GEMV microbench (no engine boot, no weight load) to avoid further in-process boots on this boot |
| 2026-09-03 18:20:29 | 0 | HW | (interactive SYV-9 session, boot T) in-process TP=2 prefill-phase-profile `LLM()` launch (util 0.85, maxlen 131072, mode-NONE, chunked-prefill hooks) wedged at weight load: kernel `VRAM is lost due to GPU reset!` + `GPU reset(1) succeeded` on 0000:0b:00.0 (GPU0), "[drm] device wedged, but recovered through reset"; VRAM back to 10.8 MB baseline both cards, no zombie PID | **11th non-deterministic wedge — 3rd in-process-LLM() weight-load hang this boot** (06:47, 11:29 GPU0; 16:45 GPU1). Two earlier SYV-9 boots today loaded clean → coin flip, not a deterministic code bug. **Decision: stop in-process profiling on this boot; SYV-9 moves to serve-based TP=2 + file-armed CUDA-event plugin** (serve path has booted clean 6× today) |
| 2026-09-03 22:34:52 | 0 | HW | (SYV-3 session, boot T) **serve-based** TP=2 MTP arm0 weight load (`mtp1srv@mtp`, util 0.85, k=2, skinny-GEMV drop-in OFF) wedged ~60s into weight load: userspace `c10::AcceleratorError: unspecified launch failure` both ranks → kernel `Fence fallback timer expired on ring comp_1.0.0` + `GPU reset(5) succeeded` on 0000:0e:00.0 (GPU0), "device wedged, but recovered through reset"; VRAM back to baseline, no zombie PID | **12th wedge — first serve-based TP=2 weight-load wedge this boot** (all prior serve launches today were clean). **New confounder vs every prior entry:** a standalone Triton GEMV microbench was running on the same GPUs concurrently during the weight-load window (agent self-review step) — possible contributor, unproven. Process survived the reset but never became healthy; driver killed it at its 675s health timeout |
| 2026-09-03 22:45:56 | 0 | HW | (SYV-3 session, boot T) arm1 weight load (`mtp1srv@mtp`, same config, skinny GEMV default ON — final reviewed code) wedged ~56s into weight load → kernel `Fence fallback timer expired on ring comp_1.0.0` + `GPU reset(6) succeeded` on 0000:0e:00.0 (GPU0), "device wedged, but recovered through reset"; VRAM back to baseline, no zombie PID | **13th wedge — same signature as #12, ~11 min later, NO concurrent GPU work this time** → the concurrent-bench confounder is not required; plain weight-load coin-flip continues. Driver killed arm1 at its 675s health timeout; A/B run voided. Canary clean after (39–41 t/s). **Lesson: no concurrent GPU work during a TP=2 weight-load window (still good hygiene); expect occasional wedges on any serve launch — retry, don't reboot.** Retried clean |
| 2026-09-04 00:52:04 | 0 | HW | (SYV-3 A/B arm1 re-run, boot T) `mtp1srv-syv3arm1` worker init wedged at `c10::cuda::SetDevice` ~60s in (first attempt; env correct via run_server.sh wrapper — an earlier raw launch had failed on a *different* error, missing FLASH_ATTENTION_TRITON_AMD_ENABLE) → kernel `Fence fallback timer expired on ring comp_1.0.0` + `GPU reset(7) succeeded` 00:52:07 | **14th wedge — same SetDevice/weight-load signature.** Canary immediately after: **38.2–38.3 t/s, down from 41.2 at 22:3x** (degradation trend on this boot) |
| 2026-09-04 01:02:36 | 0 | HW | (SYV-3 A/B arm1 re-run #2, boot T) identical `SetDevice` wedge at worker init ~70s in, after a 3-min cooldown + canary recheck (38.2 t/s, clean KFD — no zombie handles) | **15th wedge — 4th GPU0 wedge since 22:34 and 3rd consecutive launch failure; every reset "recovered" but canary trend is downward (41.2 → 39.2 → 38.2).** Decision: reboot via ~/bin/hermes-reb.sh for a clean state before the definitive arm1 run (code fix verified in-process meanwhile). If SetDevice wedges recur on a fresh boot, escalate per this file's guidance |
| 2026-09-04 12:2x | 0,1 | HW | (CAT-1 pilot session, **boot U**) `mtp1srv@mtp` TP=2 MTP serve (util 0.85, k=2, stock config — arm0 of the draft-vocab pilot A/B) wedged MID-BENCH at pp=122880: last server log line 12:24:35 (`Running: 1`), both GPUs pinned 100% ~3 min, bench client stuck in poll; **no kernel amdgpu event in journal window** (silent hang, no BACO reset) — first serve-based wedge NOT at weight-load/SetDevice/prefill: this one hit during DECODE at 120k ctx | **18th non-deterministic wedge — new signature location (mid-decode @120k, silent, no kernel event)** vs the chronic weight-load/SetDevice family. Long-context zone per `degradation.md` risk note; cf. boot-R 10:56 prefill@122880 crash (GDN-Triton-decode path active at 120k). Recovered by SIGTERM teardown (GPU quiesced, no zombie KFD handle — only the next arm's PIDs on /dev/kfd). Per Kevin's 2026-09-03 ruling: accepted HW-related; canary was 39.2 t/s minutes earlier (healthy band). Pilot consequence: arm0 pp=122880 point lost (pp=65536 complete: median 38.85 t/s n=3); re-run that single point after arm1 |
| 2026-09-04 15:57:36 | 1 | HW | (S1 startup-time session, **boot U**) TP=2 MTP serve with `--language-model-only` (S1 L3 lever probe; stock config otherwise) wedged at ~15:57:35 during worker init / profile stage: `hipErrorLaunchFailure` (`unspecified launch failure`) on Worker_TP1 → kernel `Fence fallback timer expired on ring comp_1.0.0` + BACO `GPU reset(2) succeeded! device wedged, but recovered through reset` on 0000:0e:00.0 (**GPU1**); flag itself logged clean ("All limits of multimodal modalities ... set to 0, running in text-only mode") immediately before the crash; VRAM baseline, no zombie KFD handle post-recovery | **19th wedge — profile/init stage with `--language-model-only` on TP=2.** NOT attributable to the flag's code path (it only zeroes modality limits before any GPU work; the stock boot 10 min earlier ran the same init cleanly). Chronic TP=2 HW instability family (cf. #16/#17 SetDevice, #18 mid-decode); per Kevin's 2026-09-03 ruling accepted HW-related on this dev system. S1 consequence: L3 (language-model-only) startup win NOT yet measured — the 213s vision-profiling gap is nonetheless PROVEN by baseline stack dumps (Worker_TP0 pinned in `profile_run → embed_multimodal → vit_flash_attn_wrapper` for 15:15:26→15:18:51); retry L3 after a canary, or measure via the text-only TP=1 path |
| 2026-09-04 17:20:31 | 2 | HW | (S1 session, **boot U**) L3 re-run (`--language-model-only`, stock otherwise) wedged at ~17:20:30 during worker init / profile stage — identical signature to #19: `c10::AcceleratorError: CUDA error: unspecified launch failure` @ SetDevice on both workers → kernel `Fence fallback timer expired on ring comp_1.0.0` + BACO `GPU reset(3) succeeded! device wedged, but recovered through reset` on 0000:0e:00.0 (**GPU0**); stack dumps confirm the vision path was NOT running (0 vision frames across all 233 dumps; "text-only mode" logged by both workers) so the crash is in the TEXT profile stage, not the removed ViT forward | **20th wedge — and the second consecutive L3 boot to die at the same GPU/stage.** Two-for-two with the flag on vs clean stock baseline 90 min earlier is NOT enough to blame `--language-model-only` (its code path only zeroes modality limits before any GPU work; a software fault there would give an assertion/shape error, not a fence timeout). Leading hypothesis: **post-reset degradation of GPU0** — it was BACO-reset at 15:57 (#19) and wedged again ~83 min later without a reboot in between; cf. GPU1's triple reset (01:31/15:12 + earlier). Per Kevin's ruling: accepted HW-related on this dev system. S1 consequence: L3 end-to-end number still unmeasured — the 213 s vision-profiling gap remains PROVEN by baseline dumps; next attempt should be preceded by a canary (and if stock also wedges, stop + reboot) |

| — | | **BOOT** | 2026-09-04 01:06:52 (boot U, post-wedge-reboot) | both cards clean on arrival (31/32 °C, 0%/0%, 938 MHz); mtp1canary@mtp **38.3 t/s ×2** (within the host's known passing band 38.2–39.3; below the ~40–47 nominal but consistent with every recent boot). syv3_final_test.py ALL PASS on GPU |
| 2026-09-04 01:31:41 | 0 | HW | (SYV-3 A/B arm0, **boot U**) `mtp1srv@mtp` TP=2 MTP serve (util 0.85, k=2, skinny-GEMV drop-in OFF) worker init wedged at `c10::cuda::SetDevice` ~68s in → kernel `Fence fallback timer expired on ring comp_1.0.0` + BACO reset on 0000:0b:00.0 (GPU0), "device wedged, but recovered through reset"; VRAM back to baseline, no zombie KFD handle (rocm-smi --showpids clean) | **16th wedge — FIRST SetDevice/weight-load wedge on a fresh boot: the boot-T recurrence pattern has crossed the reboot boundary.** This is the single event that triggers the escalation clause from #15; per protocol one retry is still authorized (driver's built-in retry), but a 2nd consecutive launch failure on boot U = burst → stop + report. NOTE: BDF↔GPU mapping flipped vs boot T — this boot `Initialized ... 0b:00.0 on minor 0` = GPU0 (rocm-smi GUID 10709), so the log's historical "0e=GPU0" labels are boot-T-specific; always re-derive per boot |
| 2026-09-04 01:39:21 | 1 | HW | (SYV-3 A/B arm0 **retry**, boot U) identical `SetDevice` wedge at worker init → kernel `qcm fence wait loop timeout expired` + BACO reset on 0000:0e:00.0 (**GPU1**), "device wedged, but recovered through reset"; VRAM baseline, no zombie KFD handle | **17th wedge — 2nd consecutive launch failure on boot U = BURST per house recipe → ALL GPU WORK STOPPED; escalation clause from #15 TRIGGERED (SetDevice wedges recurring on a fresh boot).** Post-stop canary 38.3 t/s (flat, no DEG); both cards probe-clean. arm0 SKIPPED by driver; the auto-continued arm1 launch (01:42) loaded weights CLEANLY but hung at shm-broadcast (known one-off init-deadlock family), stopped per the burst decision — not a wedge. **SYV-3 A/B blocked; code fix intact + verified in-process; awaiting Kevin's RMA/replace call** |
| 2026-09-04 22:20:16 | 0 | HW | (T-1 session, boot U) T-1 in-context probe launch — in-process TP=2 `LLM()` (util 0.85, mode-NONE, MTP k=2, t1_phase plugin armed) wedged at weight-load shard ~2/5: userspace `c10::AcceleratorError: unspecified launch failure` both ranks → kernel `qcm fence wait loop timeout expired` + `Failed to evict process queues` + GPU reset on 0000:0b:00.0 (GPU0); VRAM back to 0% baseline, KFD clean (only gpuagent) | **21st wedge — same chronic in-process-LLM() weight-load-hang family as #9/#10/#11** (boot U's first since the 01:39 burst; canary 39.2 t/s passed ~4 min earlier, so clean state going in). House recipe: one retry; if the retry wedges → stop + reboot, then move the probe to serve-based (cleaner family) |

| 2026-09-04 22:50:19 | 0 | HW | (T-1 session, boot U) T-1 in-context probe launch — **serve-based** TP=2 `vllm serve` (util 0.85, mode-NONE eager, MTP k=2, t1_phase plugin armed) worker init wedged at `c10::cuda::SetDevice` ~59s in: userspace `c10::AcceleratorError: unspecified launch failure` both ranks → kernel `qcm fence wait loop timeout expired` + `Failed to evict process queues` + BACO reset on 0000:0b:00.0 (GPU0), "GPU reset succeeded, trying to resume"; VRAM back to 0% baseline, KFD clean (only gpuagent), no zombie vllm PID | **22nd wedge — serve-based family** (distinct from #21's in-process-LLM() family). Kernel SELF-recovered this time (BACO reset succeeded, no zombie handle → not reboot-only). Canary 39.2 t/s passed ~4 min earlier (clean state going in); the plugin attaches hooks only AFTER load_model returns, so it cannot affect weight loading. House recipe: post-reset canary gate, then ONE serve-based probe attempt (first of its family on boot U); if it wedges again → full stop + report to Kevin |
| 2026-09-05 08:52:1x | 0/1 | HW | (T-1 A/B session, boot U) arm1 = **serve-based** TP=2 MTP k=2 (`mtp1srv-t1on@mtp`, T1_INT8_MASS=1 armed via unit env) wedged ~70s into weight load: userspace `c10::AcceleratorError: CUDA error: unspecified launch failure` (hipErrorLaunchFailure) both ranks → EngineCore init failed, exit 1. **The "T-1 int8 mass armed" log line never appeared — the transform runs post-load and was never reached**, so no T-1 code executed; env flag is read only at load_model time (single code path, grep-verified). arm0 of the same A/B (identical boot config, flag OFF) booted + served 9/9 sweep reps clean ~75 min earlier on this boot | **23rd wedge — serve-based weight-load family.** NOT a T-1 fault: crash pre-transform; only code delta vs arm0 is gated behind a check that was false. KFD clean after (only gpuagent), VRAM 10.8 MB baseline both cards, no zombie PID → driver self-recovered. House recipe: canary gate, then ONE retry of arm1; if it wedges again → stop + report |
| 2026-09-05 09:00:1x | 0/1 | HW | (T-1 A/B session, boot U) arm1 **retry** (post-canary 39.3 t/s PASS at 08:58): progressed further than attempt 1 — main-model weights loaded clean ("Loading weights took 31.69 seconds", both ranks past the attempt-1 SetDevice crash point) — then `c10::AcceleratorError: CUDA error: unspecified launch failure` both ranks during post-load init (drafter load / graph-capture window); EngineCore init failed, exit 1. "T-1 int8 mass armed" line **again absent** → transform never reached, T-1 still not implicated | **24th wedge — 2nd consecutive arm1 launch failure (wedges #23/#24) = BURST per house recipe → ALL GPU WORK STOPPED.** Canary passed between the two attempts (39.3 t/s) → non-deterministic HW, not host degradation and not code. GPUs clean after (0%, KFD only gpuagent). T-1 A/B: arm0 COMPLETE (38.83/30.43/26.14 t/s @64k/96k/120k medians, acc 2.0 all reps); **arm1 BLOCKED — needs a fresh boot** (reboot via ~/bin/hermes-reb.sh per Kevin's call) |

| 2026-09-05 15:4x | 0/1 | HW | (T-1/k=4 session, boot U) T-1 @ k=4 arm (`mtp1srv-t1on@mtp4`, T1_INT8_MASS=1 + num_speculative_tokens=4 + capture [1..5]) — first attempt failed on port 8128 contention (mtp4 teardown race, my stop-wait bug), second attempt on clean GPUs wedged ~60s into worker init: `c10::AcceleratorError: unspecified launch failure` (hipErrorLaunchFailure) at `c10::cuda::SetDevice`, both ranks → EngineCore init failed. **T-1 transform never reached** (post-load hook; "int8 mass armed" line absent). mtp4 arm (same config, T-1 OFF) had served 9/9 reps clean ~35 min earlier on this boot | **25th wedge — serve-based worker-init family.** Non-deterministic HW per house pattern (clean canaries + clean serves bracketing it). GPUs clean after (0% VRAM, KFD only gpuagent, temps 32–40 C idle) → driver self-recovered. Reboot required to clear GPU0 wedge state before retry |
| 2026-09-06 17:56:1x | 0/1 | HW | (A3 A/B session, boot V, up since 09-05 ~15:49) arm a3on = mtp4 config + `VLLM_GFX906_FUSED_DRAFT=1` (TP=2, k=4, capture [1..5]), attempt 1 ~65s into worker init: Worker_TP1 weight-load `at::native::copy_` → `c10::cuda::SetDevice` `c10::AcceleratorError: unspecified launch failure` (hipErrorLaunchFailure) → EngineCore init failed, exit 1. **A3 code inert at crash time**: the flag only sets a builder instance attribute at engine init; the fused draft loop matters only at decode-graph capture (post-load, never reached). arm a3off (same binary, flag OFF) served 9/9 reps clean 16:49–17:52 on this boot; SIGTERM teardown clean (VRAM 0/0) 30s before the a3on launch. Canary 38.9 t/s PASS at 16:36 | **26th wedge — serve-based weight-load family (3rd occurrence of the post-long-serve second-launch pattern, cf. #23/#25).** House recipe: ONE retry |
| 2026-09-06 17:56:1x | 0/1 | HW | arm a3on attempt 2 (~17:55:05 launch, clean 0% GPUs): same crash ~65s into worker init — Worker_TP1 weight-load `copy_` → `SetDevice` hipErrorLaunchFailure, EngineCore init failed | **27th wedge — 2nd consecutive launch failure (wedges #26/#27) = BURST per house recipe (cf. #23/#24) → ALL GPU WORK STOPPED.** GPUs clean after (0% VRAM both cards, 32–34 °C, sclk 938 / mclk 350 idle). **A3 A/B state: a3off COMPLETE (medians 45.45/36.42/31.73 @64/96/120k, acc 4.0 all reps — matches today's 09:07 kv32 arm 45.4/36.48/31.74, confirming the A3 commit is a no-op flag-OFF); a3on BLOCKED on the burst.** Reboot needed (root; kread has no sudo) then re-run `/local/tmp/a3/run_ab.sh` a3on arm only + compare vs the a3off numbers on file |
| 2026-09-06 22:04:3x | 0/1 | HW | (post-reboot arm sequence, boot W, up since ~18:11) mtp5 arm (k=5 depth, capture [1..6]) attempt 1 died ~65s into worker init at weight load (shard 1/5, `at::native::copy_` → `SetDevice` hipErrorLaunchFailure, `c10::AcceleratorError` both ranks). 3 clean TP=2 arms preceded it on this boot (mtp4 18:21–19:34, a3on 19:34–20:48, a5 20:48–22:02 — all served 9/9 reps). k=5 config never engaged (crash pre-capture, pre-spec-decode) | **28th wedge — chronic serve-based weight-load family (4th occurrence on this host of the post-long-serve pattern, cf. #23/#25/#26).** House recipe: ONE retry |
| 2026-09-06 22:06:1x | 0/1 | HW | mtp5 attempt 2 (clean 0% GPUs): same crash ~65s in, weight-load `copy_` → `SetDevice` hipErrorLaunchFailure | **29th wedge — 2nd consecutive (wedges #28/#29) = BURST per house recipe → driver ABORTED all GPU work (by design).** GPUs clean after (0% VRAM, 31–32 °C idle, checked 2026-09-07 05:42; driver self-recovered). **Boot-W arm sequence state: mtp4/a3on/a5 COMPLETE (verdicts: mtp4 in DEVLOG-mtp-depth-matrix.md, a3on in archive/a3-fused-draft, a5 in DEVLOG-t1-int8-fp16-mass.md); mtp5, mtp7, cat1k4 BLOCKED — need a fresh boot** (reboot per Kevin's call; then `run_arms.sh` mtp5/mtp7/cat1k4 only) |
| 2026-09-07 08:35:53 | 0 | HW | (agent-corpus arm sequence, boot X, up since ~05:49) mtp4ag arm attempt 1 (mtp4 config, TP=2, k=4, capture [1..5]; corpus name is client-side only — the server config is identical to the mtp4@s9 arm that served 9/9 reps clean 06:01–07:14 this boot): launched 08:35:04, died ~49s into worker init: userspace `c10::AcceleratorError` hipErrorLaunchFailure → kernel `qcm fence wait loop timeout expired` + "cp might be in an unrecoverable state due to an unsuccessful queues preemption" + `Failed to evict process queues` + `Failed to quiesce KFD` + BACO reset on 0000:0b:00.0 (GPU0), "GPU reset succeeded, trying to resume" + VRAM lost 08:35:55. Boot-X BDF map (re-derived per boot): 0b:00.0 = GPU0 (minor 0), 0e:00.0 = GPU1 (minor 1) | **30th wedge — chronic serve-based weight-load family (5th occurrence of the post-long-serve second-launch pattern, cf. #23/#25/#26/#28).** Two clean TP=2 arms (mtp4@s9, mtp5@s9) + 2 canaries (38.8/38.9 t/s) bracketed it. House recipe: ONE retry |
| 2026-09-07 08:37:23–32 | 0,1 | SW | mtp4ag attempt 2 (launched 08:36:41 on verified-clean GPUs): config init 08:36:55, weight load started 08:37:12 (shard 0/5) → `c10::AcceleratorError` hipErrorLaunchFailure both ranks; kernel: fence timeout + cp-unrecoverable + queue-evict failure + `GPU reset begin` on GPU0 (0b:00.0) at 08:37:23 and GPU1 (0e:00.0) at 08:37:32, both BACO reset, both self-recovered | **31st event — RECLASSIFIED 2026-09-07: DOUBLE-LAUNCH artifact, not a HW wedge.** The attempt-2 log carries TWO full stacks (2 APIServers + 2 EngineCores; two "ROCm switched" lines at the log head) — the run_arm retry loop in run_arms2.sh launched the server at BOTH the top and bottom of the for-loop body, so "attempt 2" ran two `vllm serve` instances concurrently. Two concurrent weight loads on both GPUs are consistent with the fence/cp-unrecoverable events on BOTH cards; a clean single instance measures 15.29 GiB available KV (boot-X mtp4), i.e. has ~10× the margin that died here. **Genuine boot-X wedge remains #30 only.** (Original entry text: 2nd consecutive launch failure → burst → abort; that abort was correct as a stop, but the 2nd failure was self-inflicted software, not HW.) |
| 2026-09-07 11:37:13 | 1 | HW | (mixed-corpus arm sequence, **boot Y**, up since ~11:31) mtp4ag attempt 1 (mtp4 config, TP=2, k=4, capture [1..5]; corpus=mixed is client-side — server config identical to the arms that served clean on boots V/W/X): launched 11:36:07 after canary 38.4 t/s PASS (11:33–11:36, TP=1 GPU0 only), died ~66 s into weight load: kernel `qcm fence wait loop timeout expired` + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + BACO reset on 0000:0e:00.0 (**GPU1**, minor 1 this boot), "GPU reset succeeded, trying to resume" 11:37:15, self-recovered. Single-instance launch (the only process on the GPUs; the double-launch bug fired only on the retry) | **32nd wedge — chronic serve-based weight-load family (GPU1; note this was the FIRST TP=2 launch of the boot, so the "post-long-serve" pattern does not hold for this one).** Single-instance, clean pre-state (canary PASS, VRAM 0/0) |
| 2026-09-07 11:38:10–11:40:29 | 0,1 | SW | mtp4ag "attempt 2": the run_arm double-launch bug (see #31) started two `vllm serve` instances at once (APIServer 4084+4085, EngineCore 4304+4319, two "ROCm switched" lines). Both engines' KV-cache checks failed (4.74 GiB needed vs 4.28 / 3.54 available — a clean single instance measures 15.29). **Journal clean in the window (no fence/reset events) — pure software failure.** Driver ABORTED per the mechanical 2-failures rule; reranker check: **no llama-server process exists on boot Y** (KFD gpuagent only, VRAM 0/0) — nothing to stop, not a factor. Post-abort: both GPUs probe clean (9.9/9.8 TFLOPS fp16 matmul) | **NOT a wedge — self-inflicted (script bug).** Fix landed in run_arms3.sh (one launch per attempt + pre-launch alive guard + "ROCm switched" count logged at ready). **Procedural lesson (supersedes the mechanical rule wording): the burst rule counts GENUINE single-instance launch failures; a diagnosed software failure is fixed and retried on the same boot — a reboot is only for genuine wedge pairs.** If the re-run wedges twice more on boot Y, stop + reboot per rule |
| 2026-09-07 14:08:45 | 0 | HW | mtp5ag attempt 1 (mtp5 config, TP=2, k=5, capture [1..6]; launched 14:07:21 after mtp4ag's clean 9/9 sweep + teardown, VRAM 0/0 pre-state): died ~84 s into weight load: kernel fence timeout + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + BACO reset on 0000:0b:00.0 (GPU0, minor 0 this boot), reset succeeded 14:08:47, self-recovered + "Fence fallback timer" 14:08:48. Single-instance launch (bugfix verified: 1 "ROCm switched" line on all clean launches this boot) | **33rd wedge — chronic serve-based weight-load family (GPU0).** Attempt-2 server log lost to the abort-path `rm -f` ordering (run_tail/run_plain patched: abort check before rm) — journal carries the full story |
| 2026-09-07 14:10:26 | 1 | HW | mtp5ag attempt 2 (launched ~14:09:22 after full teardown + verified clean): same family on 0000:0e:00.0 (**GPU1**), fence timeout + cp-unrecoverable + BACO, reset succeeded 14:10:28, self-recovered. Single instance | **34th wedge — 2nd consecutive GENUINE single-instance launch failure (with #33) = BURST per house rule → driver ABORTED 14:10:33 (by design); watcher held the tail queue for a human decision (as designed). Boot Y now carries THREE genuine wedges (#32 GPU1, #33 GPU0, #34 GPU1) — REBOOT per rule (Kevin executes).** GPUs clean after (VRAM 0/0, no stragglers, checked 14:17). mtp5ag's numbers are moot (skipped by decision — k=4 already a loss on the real payload); boot Z runs the re-pointed queue: canary → mtp2ag → mtp3ag → cat1k4ag → final canary → plainag (no-MTP baseline) → chat replay → mixed v2 |
| 2026-09-07 17:20:59 | 1 | HW | (boot Z, up ~14:47; canary→mtp2ag→mtp3ag→canary→plain all served clean — 5/5 TP=2-equivalent launches) chat-replay server attempt 1 (plain TP=2, port 8132, maxlen 65536; launched 17:19:47 after plain sweep 6/6 + clean teardown, VRAM 0/0): rank 0 finished its weight load clean (29.45 s); ~17:20:59 kernel on 0000:0e:00.0 (**GPU1**, minor 1) `qcm fence wait loop timeout expired` + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + PSP UNLOAD_TA 0x117 + BACO reset, "GPU reset succeeded" 17:21:01, self-recovered. `hipErrorLaunchFailure` surfaced in rank-1's SetDevice copy. Single instance (1 "ROCm switched" line, the only process on the GPUs) | **35th wedge — chronic serve-based weight-load family (GPU1).** Boot Z's FIRST genuine launch failure (the 6th launch). House rule: one retry for the arm; a 2nd consecutive genuine failure = BURST → reboot (Kevin). Post-reset state verified before retry: no stragglers, VRAM 0/0, GPU1 matmul probe 9.7 TFLOPS OK. NOTE: this boot has now done 6 TP=2-equivalent weight loads — the family's per-boot wear pattern is holding |
| 2026-09-07 17:38:41 | 1 | HW | (boot Z) chat-replay server attempt 2 (launched 17:37:37 by run_replay_retry.sh after full post-#35 verification: alive-guard 0 procs, VRAM 0/0, GPU1 matmul 9.7 TFLOPS OK): rank 0 finished its weight load clean (5/5 shards); the other rank's SetDevice copy threw `hipErrorLaunchFailure` at 17:38:43; kernel on 0000:0e:00.0 (**GPU1**, minor 1) 17:38:41: fence timeout + cp-unrecoverable + `Failed to quiesce KFD` + BACO, "GPU reset(2) succeeded" + "device wedged, but recovered through reset" 17:38:44. Single instance (alive guard + one setsid launch) | **36th wedge — chronic serve-based weight-load family (GPU1 again, same phase, same GPU as #35, 17 min apart). 2nd consecutive GENUINE single-instance launch failure on boot Z (with #35) = BURST per house rule → ALL GPU WORK STOPPED; REBOOT required (Kevin executes). Nothing is running (verified: no vllm/replay procs, VRAM 0/0, attach driver never launched — I had the attach driver written but did NOT launch it after seeing the death). BOOT Z DATA IS COMPLETE AND SAFE: canaries 38.8/38.8, mtp2ag 30.95/20.71 @64/120k, mtp3ag 24.80 @120k, plain 19.76/13.11 — the depth verdict is in. **Procedural finding (watcher bug, affects the #35 timeline): `run_replay_server.sh` `exec`s vllm, so the watcher's `pgrep -f "run_replay_server[.]sh"` death-check can NEVER match after exec — on attempt 1 the watcher false-positived "died during startup" in the same second (17:19:47) and exited, orphaning the (setsid'd, still-alive) server, which then wedged at 17:20:59 = #35. The #35 "died during startup" line in replay_driver.log is a FALSE POSITIVE; the genuine event is the 17:20:59 journal wedge. Fixed the pattern in run_replay_retry.sh; run_replay_attach.sh (attach-to-existing, no launch) written but never needed.** |
| 2026-09-07 20:28:55 | 1 | HW | (boot W', up since ~17:46; replay 18:13 + canary 19:33 + mtp3 19:35 all served clean — mtp3v2 attached to the running mtp3 server, no new load) mtpv2 attempt 1 (mtp config: TP=2, k=2, capture [1..4], launched 20:27:56 after mtp3v2's clean 6/6 sweep + verified teardown, VRAM 0/0): both ranks died at safetensors shard 0/5 — `hipErrorLaunchFailure` at the SetDevice copy; kernel on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + PSP UNLOAD_TA 0x117 + BACO, "GPU reset succeeded" 20:28:57, self-recovered. Single instance (1 "ROCm switched" line, the only process on the GPUs) | **37th wedge — chronic serve-based weight-load family (GPU1).** Boot W''s FIRST genuine launch failure (after 3 clean TP=2-equivalent loads). House rule: one retry |
| 2026-09-07 20:30:26 | 0 | HW | (boot W') mtpv2 attempt 2 (launched 20:29:25 after full post-#37 verification: alive-guard 0 procs, VRAM 0/0, both-GPU matmul probes 10.0/9.8 TFLOPS OK): same family at safetensors shard 0/5 — `hipErrorLaunchFailure` both ranks; kernel on 0000:0b:00.0 (**GPU0**, minor 0): fence timeout + cp-unrecoverable + `GPU reset begin (Source 4)` 20:30:26. Single instance |**38th wedge — chronic serve-based weight-load family (GPU0). 2nd consecutive GENUINE single-instance launch failure on boot W' (with #37) = BURST per house rule → continuation driver ABORTED 20:30:50 (by design); REBOOT required (Kevin executes). Post-stop state verified: no vllm procs, VRAM 0/0, BACO self-recovered both cards. BOOT W' DATA SAFE: mtp3v2 COMPLETE (medians 27.44 @64k / 24.76 @120k — k=3 on the 20 %-chat v2 corpus, beating k=4 v1's 21.87 @120k by +13.2 %), replay jsonl (205k tokens), v2 corpus, SYV-12 generation-time gate GO (25.3 % hit_frac). Remaining queue: mtpv2 6 reps + final canary (post-reboot driver). |
| 2026-09-07 22:23:1x | 1 | HW/SW | (boot W'', up since ~20:47; canary 38.8 + mtpv2 6/6 + clean SIGTERM teardown 21:35 all clean) SYV-12 in-process probe arm 1 (ON; in-process TP=2 `LLM()`, k=2, util 0.93) launched 22:23:02, SetDevice `hipErrorLaunchFailure` ~10s in. **Trigger was self-inflicted:** arm 0 (OFF) had completed its 512-token run but its multiproc shutdown HUNG ("workers still running after grace period; sending SIGTERM" at 22:22:51) — the driver waited only 10 s + a VRAM check (0/0) and launched arm 1 11 s after the workers' SIGTERM, i.e. on GPUs whose driver state the killed workers were still releasing. Kernel 22:24:15 on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + cp-unrecoverable + queue-evict + `Failed to quiesce KFD` + UNLOAD_TA 0x117 + BACO reset, self-recovered. Single instance | **39th event — chronic-family signature, HARNESS-INDUCED (mid-op kill of the previous arm's workers), not a spontaneous launch failure.** The AGENTS.md TP=2 teardown hazard (kill mid-op wedges the next init) fired via a hung-shutdown SIGTERM instead of a server SIGKILL. Post-state verified 22:32: VRAM 0/0, matmul probes 9.9/9.8 TFLOPS both cards (BACO self-recovered). Per the boot-Y refined rule (diagnosed software trigger → fix + retry on the same boot; the burst counts GENUINE spontaneous failures) this does NOT count toward the burst pair — but the driver is fixed to (a) wait for ALL prior-arm worker procs to be gone, not just VRAM, and (b) re-probe both GPUs before the next launch. ONE retry authorized; if the retry wedges spontaneously, that + any further spontaneous pair = burst → reboot. Arm 0 result is safe (512 tokens, identity reference saved). |
| 2026-09-07 22:36:28 | 1 | HW | (boot W'') SYV-12 probe RE-RUN arm 0 (SYV-12 OFF; in-process TP=2 `LLM()`, k=2, util 0.93) launched 22:35:32 on verified-clean GPUs (pre-arm check 22:35:28–32: no stray procs, VRAM 0/0, matmul 10.0/9.7 TFLOPS both cards — the #39 driver fix in place): worker SetDevice `hipErrorLaunchFailure` ~56 s in; kernel on 0000:0e:00.0 (**GPU1**, minor 1) 22:36:28: fence timeout + cp-unrecoverable + queue-evict + `Failed to quiesce KFD` + UNLOAD_TA(0x2) 0x117 + BACO reset, self-recovered. Single instance, clean pre-state — SPONTANEOUS | **40th wedge — GENUINE spontaneous launch failure (the 1st genuine one on boot W'' after #39's harness-induced event; does not by itself form a burst pair).** **Decision: STOP GPU work (wear-based, pre-emptive) — not a formal burst, but the same boot now shows TWO chronic-family BACO resets on GPU1 15 min apart (22:24, 22:36) after 5 TP=2-equivalent loads (canary, mtpv2, probe×3 attempts); the remaining verification needs 4 more loads (probe off+on, A/B off+on). One authorized retry exists per rule, but spending it (and the 3 loads after it) on a demonstrably wearing GPU1 is not worth the wedge risk; the probe's purpose (implementation sanity) is also served by the OFF reference already captured (22:05 run: 512 tokens + wall time, log kept). Post-stop state verified 22:46: VRAM 0/0, matmul 10.0/9.8 TFLOPS, no stragglers. REBOOT (Kevin) → next boot runs the staged SYV-12 verification (probe driver + `run_syv12_ab.sh`, ~4 weight loads — inside the per-boot budget on a clean boot). |
| 2026-09-08 07:49:51 | 1 | HW | (boot Y2, up since ~06:31; probe arm 0 (OFF) ran a full clean 120k prefill + 512-decode and shut down gracefully 07:48:39) SYV-12 probe arm 1 (ON) attempt 1 (in-process TP=2 `LLM()`, k=2, util 0.93) launched 07:48:57 after the hardened post-drain (all procs gone, VRAM 0/0, both-GPU matmul probes 9.9/9.7 TFLOPS OK): both ranks died at safetensors shard 2–4/5 — `hipErrorLaunchFailure` at the SetDevice copy; kernel 07:49:51 on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + UNLOAD_TA(0x2) 0x117 + BACO, "GPU reset succeeded" 07:49:53, self-recovered. Single instance, verified-clean pre-state — **SPONTANEOUS** | **41st wedge — GENUINE spontaneous launch failure, chronic serve-based weight-load family (GPU1 again — the recurring victim).** Boot Y2's 1st genuine failure (after 1 fully clean load). (The retry it authorized hit a SOFTWARE bug — scheduler spec-stats sizing, fixed — then a second software bug — the GDN state-slot under-provisioning, fixed; both diagnosed and verified on a 16k debug probe, no further GPU launches counted.) |
| 2026-09-08 08:57:24 | 1 | HW | (boot Y2; since #41: 4 fully clean loads — probe ON attempts 2+3 [the latter a full 120k run], 2× 16k debug probes) SYV-12 probe 120k ON arm RE-RUN (post-GDN-fix) launched 08:56:19 after the hardened pre-checks (VRAM 0/0, both-GPU matmul OK): both ranks died at safetensors shard 2–4/5 — `hipErrorLaunchFailure` at the SetDevice copy; kernel 08:57:24 on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + UNLOAD_TA(0x2) 0x117 + BACO, "GPU reset succeeded" 08:57:27, self-recovered. Single instance, verified-clean pre-state — **SPONTANEOUS** | **42nd wedge — GENUINE spontaneous launch failure, chronic weight-load family (GPU1 again, 68 min after #41).** NOT a formal burst pair (4 clean launches between #41 and #42), but the #40 pre-emptive criterion (TWO spontaneous chronic-family resets on one boot) is met. Per-boot wedge rate ≈ 2/8 launches; the remaining verification needs ≥3 more loads (120k ON probe + A/B off+on) — P(no wedge across 3) ≈ 42 %. **Decision: STOP GPU work (wear-based, pre-emptive) — same call as #40.** Post-stop state verified 08:59: VRAM 0/0, no stragglers, BACO self-recovered. Nothing lost: OFF reference + identity reference persist at /local/tmp/syv12/ (tmpfs-safe); V1 port + GDN fix + stats fix are committed (inert at default OFF). **Next boot (Kevin reboots): re-run ONLY the 120k ON arm (run_arm1_retry.sh; identity vs the preserved OFF reference) + the guarded serving A/B — 3 loads, inside a clean boot's per-boot budget.** |
| 2026-09-08 13:14:37 | 1 | HW | (boot Y3, up since ~09:56; 4 clean loads since — SYV-12 probe ON [full 120k], A/B OFF + ON [full sweeps], agdn server attempt 1 [loaded clean; its later crash was the harness wrapper arg bug, not a load failure]) A6 GDN-profile server attempt 2 (`run_agdnsrv.sh`, k=3 MTP, TP=2, EAGER, util 0.85) launched 13:13:50: both ranks died at safetensors shard ~40 % — `hipErrorLaunchFailure` at the SetDevice copy; kernel 13:14:37 on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + cp-unrecoverable + `Failed to evict process queues` + `Failed to restore process queues` + UNLOAD_TA(0x2) 0x117 + BACO, "GPU reset succeeded" 13:14:39, self-recovered. Single instance, clean pre-state (VRAM 0/0, no procs, prior run torn down 13:13) — **SPONTANEOUS** | **43rd wedge — GENUINE spontaneous launch failure, chronic serve-based weight-load family (GPU1 again — the recurring victim).** Boot Y3's 1st genuine failure. House rule: one retry for the arm — retry launched 13:21:57 after both-GPU matmul probes (10.0/9.8 TFLOPS OK) + full process/VRAM drain. **If the retry wedges spontaneously: 2nd consecutive GENUINE launch failure on boot Y3 (with #43) = BURST → stop + reboot (Kevin).** Note: the agdn plugin's first crash (13:11, attempt 1) was a diagnosed software bug (wrapper arg shift — fixed + unit-checked), not a launch failure; it does not count. |
| 2026-09-08 21:16:40 | 1 | HW | (boot Y3, up since 09:57; 5 clean loads since #43's retry) instrumented SYV-12 s9 probe attempt 1 (in-process TP=2 `LLM()`, k=2, util 0.93, EAGER + per-step debug dump) launched 21:15:23 after prelaunch both-GPU matmul probes (10.0/9.8 TFLOPS OK): both ranks died at safetensors shard 1/5 — `hipErrorLaunchFailure` at the SetDevice copy; kernel 21:16:40 on 0000:0e:00.0 (**GPU1**, minor 1): `qcm fence wait loop timeout expired` + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + UNLOAD_TA(0x2) 0x117 + BACO, "GPU reset(2) succeeded" + "device wedged, but recovered through reset" 21:16:43. Single instance, verified-clean pre-state — **SPONTANEOUS**. NOT attributable to the SYV-12 fill-kernel fix (landed in the working tree same day): the wedge hit during weight load, before the first model step; the new kernel code only launches at decode time and had already compiled + run 16/16 clean in a standalone GPU unit test at ~21:12 | **44th wedge — GENUINE spontaneous, chronic serve-based weight-load family (GPU1 again — the recurring victim). Boot Y3's 2nd spontaneous chronic-family reset (1st: #43, 8 h earlier, clean loads between) — meets the #40 pre-emptive wear criterion in form, but the standing rule still authorizes ONE retry for the arm, the user directed this exact probe (resurrection gate), and the retry's failure mode is the reboot the wear criterion would otherwise force — with nothing at risk (fix + probe staged under /local, all state persistent). DECISION: execute the authorized retry; if it wedges spontaneously, that + #44 = 2nd consecutive GENUINE single-instance launch failure = BURST → stop + reboot (Kevin). Post-state verified 21:19: no procs, VRAM 0/0, both-GPU matmul 10.0/9.8 TFLOPS OK |
| 2026-09-08 21:22:12 | 1 | HW | (boot Y3) instrumented SYV-12 s9 probe RETRY (the one authorized by the #44 entry) launched 21:21:22 after the hardened pre-checks (no procs, VRAM 0/0, both-GPU matmul probes 10.0/9.8 TFLOPS OK at 21:21:17–22): both ranks died at safetensors shard ~1/5 — `hipErrorLaunchFailure` at the SetDevice copy; kernel 21:22:12 on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + BACO, "GPU reset(3) succeeded" + "device wedged, but recovered through reset" 21:22:15. Single instance, verified-clean pre-state — **SPONTANEOUS** | **45th wedge — GENUINE spontaneous, chronic weight-load family (GPU1, 50 s after #44's recovery). 2nd consecutive GENUINE single-instance launch failure (with #44) = BURST per house rule → ALL GPU WORK STOPPED 21:24; REBOOT required (Kevin executes — kread has no sudo).** Nothing lost: the SYV-12 fill-contract fix + debug instrumentation, the SYV-13 close, and the B=4 harness extension all landed in the tree this session (commits below); the resurrection-gate probe is staged and its purpose is now VALIDATION, not localization (root cause found statically + by unit test, pre-wedge). Post-stop state verified 21:24: no procs, VRAM 0/0, KFD only gpuagent, BACO self-recovered. Boot Y3 final tally: 3 spontaneous chronic-family wedges (#43 13:14, #44 21:16, #45 21:22) across ~9 weight loads ≈ 33 % per-load wedge rate |
| 2026-09-09 06:45:03 | 1 | HW | (boot Y4, up since 2026-09-08 21:31; canary 21:36 clean @38.7 t/s, then ~9 h idle due to an ssh outage) instrumented SYV-12 s9 VALIDATION probe attempt 1 (in-process TP=2 `LLM()`, k=2, util 0.93, EAGER + per-step debug dump; the fill-contract fix from commit 7b2fba0754 in tree) launched 06:43:34 after prelaunch both-GPU matmul probes (10.0/9.8 TFLOPS OK): both ranks died at safetensors shard ~1/5 — `hipErrorLaunchFailure` at the SetDevice copy; kernel 06:45:03 on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + BACO, "GPU reset(1) succeeded" + "device wedged, but recovered through reset" 06:45:06. Single instance, verified-clean pre-state — **SPONTANEOUS**. Note: this is the 3rd weight load of the in-process-TP=2 probe config to wedge (#44, #45 on boot Y3; every chronic-family wedge on this host is a TP=2 launch, canaries are TP=1) | **46th wedge — GENUINE spontaneous, chronic weight-load family (GPU1 — the recurring victim; first genuine failure on boot Y4).** House rule: ONE retry authorized (post-drain verified 06:47: no procs, VRAM 0/0, both-GPU matmul 10.0/9.8 TFLOPS OK). If the retry wedges spontaneously: 2nd consecutive GENUINE single-instance launch failure (with #46) = BURST → stop + reboot (Kevin). Fallback staged: a serve-based probe variant (server + client; the serve family has repeated clean TP=2 launches this month) for the next boot. **OUTCOME (06:47–07:15): the retry was a diagnosed SOFTWARE failure, not a wedge** (clean weight load; Triton compile assert in the fixed fill kernel's mixed int32/int64 match loads — fixed `fb971c54a0` + unit test updated to the live int64 draft dtype, 16/16). The re-run after the fix PASSED: the SYV-12 validation probe is clean on all three links (pos3 0.987, mean ~3.99, identity perfect, eager 31.65 t/s; dev log boot-Y4 entry). No burst; GPU work continued to the production A/B queue |
| 2026-09-09 07:29:13 | 1 | HW | (boot Y4; since #46: 2 clean loads — the probe retry [software failure, not a load failure] + the successful validation probe) SYV-12 v2 production A/B, OFF arm (mtp2o4: `mtp` k=2 server, TP=2, util 0.85, capture [1,2,3,4], mixed-v2 64k+120k x3 — the boot-Y3 protocol) attempt 1 launched 07:28:12 after prelaunch both-GPU matmul probes (10.0/9.8 OK): died 71 s in at the weight-load phase; kernel 07:29:13 on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + "GPU reset begin" + BACO, "GPU reset(2) succeeded" + "device wedged, but recovered through reset" 07:29:16. Single instance, verified-clean pre-state (preflight 07:28:08) — **SPONTANEOUS**. Log kept: /local/tmp/a3/server_mtp2o4_syv12.log.attempt1 | **47th wedge — GENUINE spontaneous, chronic weight-load family (GPU1, the recurring victim). Boot Y4's 2nd spontaneous chronic-family reset (#46 06:45, this 07:29; one successful load between — NOT a burst pair; the driver's authorized retry loaded clean 07:31:17, KV 15.56 GiB).** Wear note: 2/5 TP=2 loads this boot. The A/B needs exactly ONE more load (the ON arm) to complete the boot's primary goal — proceeding; if the ON arm wedges, its single retry is the last load this boot gets (2nd consecutive on that arm = burst per the driver). The B=4 campaign (6-7 loads) is deferred to the next boot under the #40/#42 wear criterion regardless |
| 2026-09-09 08:24:06 | 1 | HW | (boot Y4; since #47: the OFF arm's clean retry load + its full 6-rep sweep + clean teardown) SYV-12 v2 production A/B, ON arm (mtp2n4: same `mtp` k=2 server + GFX906_SYV12=1) attempt 1 launched 08:23:00: died 66 s in at the weight-load phase; kernel 08:24:06 on 0000:0e:00.0 (**GPU1**, minor 1): fence timeout + "GPU reset begin" + BACO, self-recovered 08:24:08. Single instance, verified-clean pre-state (teardown VRAM 0/0 at 08:22:59) — **SPONTANEOUS**. Log kept: /local/tmp/a3/server_mtp2n4_syv12.log | **48th wedge — GENUINE spontaneous, chronic weight-load family (GPU1, the recurring victim). Boot Y4's 3rd spontaneous chronic-family reset (#46 06:45, #47 07:29, this 08:24) — 3 of 8 TP=2 loads this boot ≈ 38 % per-load; the boot is in the #40/#42 wear regime. The driver's authorized retry loaded clean 08:26:14 (KV 15.42 GiB); per the pre-decision recorded with #47, this retry was the LAST load this boot gets — after the ON sweep completes, ALL GPU work stops (the B=4 campaign, 6-7 loads, defers to the next boot)** |
| 2026-09-09 10:56:27 | 1 | HW | (boot Y4; since #48: the ON arm's clean retry load + its full sweep + clean teardown, then — per Kevin's 09-09 directive, OVERRIDING the #48 last-load pre-decision — the B=4 campaign: mtp3 B=1 anchor arm loaded clean 10:09 + full 5-rep bench (64k×3 + 120k×2; decode 26.7/31.1/39.0 @64k, 23.6/24.7 @120k) + clean SIGTERM teardown 10:51, VRAM 0/0) cProfile TTFT-stall diagnostic probe (in-process TP=2 `LLM()`, k=3 mtp, util 0.93, capture [1,2,3,4]; the §6.2 step of `ttft-prefill-stall.md`) launched ~10:52:30 after the clean teardown: both ranks died at the weight-load phase; kernel 10:56:27 on 0000:0e:00.0 (**GPU1**, minor 1): `qcm fence wait loop timeout expired` + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + UNLOAD_TA(0x2) 0x117 + BACO, "GPU reset succeeded, trying to resume" 10:56:29. Single instance, verified-clean pre-state (10:51 teardown check) — **SPONTANEOUS**. Log kept: /local/tmp/b4/prof_64k.log. Note: 4th spontaneous chronic-family reset on boot Y4 (#46 06:45, #47 07:29, #48 08:24, this 10:56); per-load rate now 4/9 TP=2 loads ≈ 44 % — the boot is deep in the #40/#42 wear regime. This was a DIAGNOSTIC load (my initiative, not campaign data) | **49th wedge — GENUINE spontaneous, chronic weight-load family (GPU1 — the recurring victim).** Decision: do NOT spend a retry on the diagnostic (cProfile step stays queued in `ttft-prefill-stall.md` for the next agent/boot); go straight to the campaign's next arm (greedy4) as this boot's next load under Kevin's standing B=4 directive. House rule still in force: greedy4's first attempt wedging spontaneously = 2nd consecutive GENUINE single-instance launch failure (with #49) = BURST → stop + reboot (Kevin). Post-state verified 10:58: no procs, VRAM 0/0, rocm-smi nominal both cards (32/33 °C, 16-18 W); the driver's own pre-launch matmul re-probe follows |
| 2026-09-09 19:06:4x | 1 | HW | (boot Y4; since #49: ONE clean in-process TP=2 load — the TTFT matrix load A [prof_64k.py, 18:14 launch, full run1+run2 data, §12 committed; its 3 earlier attempts were self-inflicted profiler bugs / a VRAM rejection against my own orphans — not counted]) TTFT kernel-breakdown probe (in-process TP=2 `LLM()`, mtp3 k=3, util 0.93, pp=1024, 8k traced prefill — `ttft-prefill-stall.md` §12.6 next step) launched 19:06:10 on verified-clean GPUs (no procs, VRAM 10.9 MB/card, KFD only gpuagent): weights ~60 % (shard 3/5); both ranks then `c10::AcceleratorError: unspecified launch failure` (hipErrorLaunchFailure), terminate() both workers. Self-recovered (19:07: VRAM baseline, KFD clean, no stragglers). Single instance, verified-clean pre-state — **SPONTANEOUS**, chronic weight-load family (5th on boot Y4; 5/11 loads ≈ 45 %). Log: /local/tmp/b4/kb_run.log (dmesg unreadable to kread — BDF not recorded) | **50th wedge — GENUINE spontaneous, chronic weight-load family (recurring victim GPU1 per family pattern).** 1st consecutive GENUINE single-instance launch failure since #49 (the clean matrix load resets the pair). House rule: ONE retry after post-state verification (no procs, VRAM baseline, both-GPU matmul probes 15.9/15.6 TFLOPS). **OUTCOME (19:17:15–19:18:08): the retry WEDGED — same family, same weight-load phase, self-recovered (log /local/tmp/b4/kb_run2.log). 2nd consecutive GENUINE single-instance launch failure = BURST per house rule → ALL GPU WORK STOPPED; REBOOT required (Kevin executes — kread has no sudo).** Post-stop verified 19:21: no procs, VRAM baseline, KFD only gpuagent. §12 committed (e867cdb1c0); kernel-breakdown step stays queued for the next boot (script staged /local/tmp/b4/kb_probe.py) |
Maintained by: whoever hits the next one. Update BOTH files.
| 2026-09-09 20:55:25 | ? | HW | (boot Y5, fresh boot post-#50 burst; canary 38.8 t/s PASS 19:26, then 4 clean TP=2 loads — kb_run3 in-process, kb_run4 in-process, kb-serve serving, pp256 serving, all clean SIGTERM teardowns) D1+D2+D3 combined discriminator session (`ttft-prefill-stall.md` §13.5: mtp3 TP=2 serve, util 0.93, pp=1024, port 8152; holder+probe slope-ownership probe + per-TID CPU + strace + clocks) attempt 1 launched 20:54:25 on verified-clean GPUs (no procs, VRAM baseline, KFD only gpuagent): TP0 died at safetensors shard 3/5 (60 %), both ranks `c10::AcceleratorError: unspecified launch failure` (hipErrorLaunchFailure), EngineCore init failed. Self-recovered (21:01: VRAM 0 % both cards, 31/33 °C, sclk 938/mclk 350 idle, rocm-smi nominal, no stragglers). Single instance (the only process on the GPUs) — **SPONTANEOUS**. dmesg unreadable to kread — BDF not recorded. Log: /local/tmp/b4/stall3_server.log | **51st wedge — GENUINE spontaneous, chronic serve-based weight-load family (1st genuine failure on boot Y5; GPU attribution unknown — dmesg unavailable, GPU1 per family pattern).** Boot Y5: 1/5 TP=2 loads ≈ 20 %. House rule: ONE retry after post-state verification (no procs, VRAM baseline, both-GPU matmul probes). If the retry wedges spontaneously: 2nd consecutive GENUINE single-instance launch failure (with #51) = BURST → stop + reboot (Kevin). The session is fully staged (scripts under /local/tmp/b4/); a failed retry costs nothing beyond the load |
| 2026-09-09 21:04:36 | ? | HW | (boot Y5) D1+D2+D3 session RETRY (the one authorized by the #51 entry; launched 21:03:29 after full post-state verification: no procs, VRAM baseline, both-GPU matmul probes 15.9/15.6 TFLOPS OK at ~21:02): progressed FURTHER than attempt 1 — weights 100 % (5/5 shards, both ranks) — then post-load init (drafter load / graph-capture window): both ranks `c10::AcceleratorError: unspecified launch failure` (hipErrorLaunchFailure), EngineCore init failed 21:04:36. Self-recovered (21:07: VRAM 0 % both cards, 31/33 °C, sclk 938/mclk 350 idle, rocm-smi nominal, 0 vllm procs). Single instance, verified-clean pre-state — **SPONTANEOUS**. dmesg unreadable to kread — BDF not recorded. Log: /local/tmp/b4/stall3_server.log | **52nd wedge — GENUINE spontaneous, chronic weight-load/post-load-init family (2nd on boot Y5). 2nd consecutive GENUINE single-instance launch failure (with #51, 9 min apart, one verified-clean inter-check) = BURST per house rule → ALL GPU WORK STOPPED 21:07; REBOOT required (Kevin executes — kread has no sudo).** Boot Y5 final tally: 2 spontaneous chronic-family wedges (#51 20:55, #52 21:04) across 5 TP=2 loads (4 clean: kb_run3, kb_run4, kb-serve, pp256) ≈ 40 % per load — worse than Y4's 36 %; the post-reboot #50 burst boot did NOT reset the wear. Nothing lost: D1 design (corrected holder+probe) + D3 preliminary (shim = yield-poll synonym, covers event syncs; matrix-A 100 %-burner re-attribution) + all handover #2/#3 doc absorption are committed (839509fe8a); the session is fully staged under /local/tmp/b4/ for the next boot (one launch + ~5.5 min wall) |
| 2026-09-10 10:45:17 | ? | HW | (boot Y6, fresh boot post-#52 burst; canary 38.8 t/s PASS 10:07, then 2 clean TP=2 loads: stall3 D1a/D2/D3 session 10:08-10:16, stall4 D1c/idle/burner session 10:31-10:40, both with clean SIGTERM teardowns) stall5 burner-identity session (same server config + VLLM_STALL_PROF periodic stack dumps; the short-session variant: one 2k-in/4096-out request, ~5 min wall) launched 10:43:38 on verified-clean GPUs: Worker_TP0 died at ~80 % weights (shard 4/5), `c10::AcceleratorError` terminate both ranks, EngineCore init failed 10:45:17; EngineCore exit path also segfaulted in `__hipUnregisterFatBinary` (secondary, post-failure). Self-recovered (10:50: VRAM 0 % both cards, 31/33 °C, idle clocks, 0 procs). Single instance, verified-clean pre-state — **SPONTANEOUS**. dmesg unreadable to kread — BDF not recorded. Log: /local/tmp/b4/stall5_server.log | **53rd wedge — GENUINE spontaneous, chronic weight-load family (3rd phase variant: #51 60 %, #52 100 %/post-load, this 80 %). 1st genuine failure on boot Y6.** House rule: ONE retry after post-state verification. If the retry wedges spontaneously: 2nd consecutive GENUINE single-instance launch failure (with #53) = BURST → stop + reboot (Kevin). Note: the periodic stack-dump fix works (worker dumps survived the unclean exit — 10:44 files) |
| 2026-09-10 10:52:xx | ? | HW | (boot Y6) stall5 RETRY (the one authorized by the #53 entry; launched 10:51:15 after post-state verification: 0 procs, VRAM baseline, both-GPU matmul probes 15.9/15.6 TFLOPS OK ~10:50:50): died EARLIER than attempt 1 — safetensors shard 1/5 (20 %) → ~40 % boundary, both ranks `c10::AcceleratorError` terminate, EngineCore init failed. Self-recovered (10:54:20: VRAM 0 % both cards, 31/33 °C, idle clocks, 0 procs). Single instance, verified-clean pre-state — **SPONTANEOUS**. dmesg unreadable to kread — BDF not recorded. Log: /local/tmp/b4/stall5_server.log | **54th wedge — GENUINE spontaneous, chronic weight-load family (earliest phase yet: ~20-40 %). 2nd on boot Y6. 2nd consecutive GENUINE single-instance launch failure (with #53, 9 min apart, verified-clean inter-check) = BURST per house rule → ALL GPU WORK STOPPED 10:54; REBOOT required (Kevin executes — kread has no sudo).** Boot Y6 final: 2 spontaneous wedges (#53 10:45, #54 10:52) across 4 TP=2 loads (stall3, stall4 clean) ≈ 50 %/load — worst boot yet (Y4 36 %, Y5 40 %, Y6 50 %): the chronic family is accelerating across boots. Nothing lost: D1a+D1c+idle+onset all landed (S13.8, committed); only the burner-thread FRAME name slips to the next boot (stall5 script ready; periodic-dump mechanism proven) |
| 2026-09-10 15:03:11 | 1 | HW | (boot Y7, fresh boot post-#54 burst, up since 14:09; canary 38.7 t/s PASS ~14:11, then 2 clean TP=2 loads: stall5 burner-identity session 14:14-14:16 [51.3 s request, 0 errors] and stall6 wchan-sniffer session 14:24-14:27 [51.4 s request, 0 errors]) stall7 burner-identity session attempt 1 (same mtp3 TP=2 serve + VLLM_STALL_PROF sitecustomize v4 [per-thread names + /proc census + OMP probe] + 1 Hz per-TID sampler) launched 15:02:01 on verified-clean GPUs (no procs, VRAM baseline, KFD only gpuagent): Worker_TP0 (pid 5229) died at safetensors shard 4/5 in flight (~60 % weights); kernel 15:03:11 on 0000:0e:00.0 (**GPU1**): `qcm fence wait loop timeout expired` + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + UNLOAD_TA(0x2) 0x117 + BACO, "GPU reset succeeded" 15:03:13; userspace `c10::AcceleratorError` both ranks 15:03:13. Self-recovered (15:06: VRAM 10.8 MB both cards, 0 vllm procs). Single instance, verified-clean pre-state — **SPONTANEOUS**. **Note: `journalctl -k` WAS readable to kread this boot (first BDF-confirmed recent boot — GPU1 victim, 0e:00.0).** Log: /local/tmp/b4/stall7_server.log | **55th wedge — GENUINE spontaneous, chronic serve-based weight-load family (GPU1 — the recurring victim; 6th phase observation: #51 60 %, #52 100 %/post-load, #53 80 %, #54 20-40 %, #55 60 %). 1st genuine failure on boot Y7 (after 2 clean TP=2 loads + 1 clean canary).** House rule: ONE retry after post-state verification. If the retry wedges spontaneously: 2nd consecutive GENUINE single-instance launch failure (with #55) = BURST → stop + reboot (Kevin). The 20 s periodic-dump mechanism worked again (worker census dumps survived the unclean exit — load-phase thread layout captured); the session's request phase never ran |
| 2026-09-10 15:29:44 | 1 | HW | (boot Y7; since #55: ONE clean TP=2 load — the stall7 retry [15:13:38 launch, full idle+request+post session, 51.3 s request, clean SIGTERM teardown]) stall8 shim-OFF A/B session (same mtp3 TP=2 config + VLLM_STALL_PROF + 1 Hz per-TID sampler, **plus `VLLM_GFX906_HIP_BLOCKING_SYNC=0`** — the .pth blocking-sync shim disabled for the D3 burner causality test) launched 15:28:34 on verified-clean GPUs: progressed PAST the weight load (5/5 shards) to post-load init — mamba page-size padding / attention block-size setup 15:29:35; kernel 15:29:44 on 0000:0e:00.0 (**GPU1 — 2nd BACO on this GPU this boot, 26 min after #55**): `qcm fence wait loop timeout expired` + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + UNLOAD_TA(0x2) 0x117 + BACO, "GPU reset" then userspace `c10::AcceleratorError` (SetDevice) Worker_TP0 (pid 7450). Self-recovered (15:33: VRAM baseline both cards, 0 vllm procs). Single instance, verified-clean pre-state — **SPONTANEOUS** (the shim-OFF env var changes only which process creates the HIP context at startup; the chronic family has hit every phase/config on this host). Log: /local/tmp/b4/stall8_server.log | **56th wedge — GENUINE spontaneous, chronic serve-based weight-load family (GPU1 — 2nd on this boot). Meets the #40/#42 pre-emptive wear criterion (TWO spontaneous chronic-family resets on one boot). DECISION: STOP GPU work (wear-based, pre-emptive — same call as #40/#42; the authorized retry is NOT spent: the remaining experiment [shim-OFF A/B, 1 load] is a D3 nicety, not core, and the boot is actively wearing on GPU1).** REBOOT recommended (Kevin executes). Nothing lost: the D3 burner IDENTITY is CLOSED on the shim-ON data (first-spawned native thread per worker, bistable ~0.96 core request-driven; see ttft-prefill-stall.md §13.9); only the shim-OFF A/B (stall8 scripts staged) slips to the next boot |
| 2026-09-11 10:10:03 | 1 | HW | (boot Y8, up since 09-10 ~16:23; ~5 clean TP=2 loads before today's session [S13.10 ×2, S13.13 campaign server, then today's stall5 server 08:29-08:33 clean and MBT bt2048 server 08:38-10:04 clean]) MBT-1 bt4096 arm attempt 1 (mtp3-free greedy TP=2, `--max-num-batched-tokens 4096`, port 8161), launched 10:08:5x on verified-clean GPUs (driver pre-probes 10.0/9.7 TFLOPS): Worker_TP0 died ~90 s in, mid-weight-load; kernel 10:10:03 on 0000:0e:00.0 (**GPU1**): `qcm fence wait loop timeout expired` + cp-unrecoverable + `Failed to evict process queues` + `Failed to quiesce KFD` + UNLOAD_TA(0x2) 0x117 + BACO ("GPU reset begin!. Source: 4"); userspace `hipErrorLaunchFailure` (SetDevice→copy_) both ranks. Self-recovered (driver post-check probes 10.0/9.8 TFLOPS, VRAM 0). journalctl -k READABLE (0e:00.0 BDF-confirmed). Log: /local/tmp/b4/server_mbt_bt4096_0911_1008.log | **57th wedge — GENUINE spontaneous, chronic serve-based weight-load family (GPU1). 1st genuine failure on boot Y8 (after ~5 clean loads).** House rule: ONE retry authorized. If retry wedges: 2nd consecutive = BURST → stop + reboot (Kevin) |
| 2026-09-11 10:12:4x | 1 | HW | (boot Y8) MBT-1 bt4096 attempt 2 (the authorized retry; same config), launched 10:11:4x on verified-clean GPUs (post-#57 probes 10.0/9.8): Worker_TP0 died again mid-weight-load (~70 s in); kernel ~10:12:4x on 0000:0e:00.0 (**GPU1 — 2nd BACO this boot, ~2.5 min after #57**): same fence-timeout/BACO signature; userspace `hipErrorLaunchFailure` (SetDevice→copy_). Self-recovered (post-check probes 10.0/9.8, VRAM 0/0). Log: same file, attempt-2 section | **58th wedge — GENUINE spontaneous, chronic family (GPU1, 2nd this boot, ~2.5 min apart). 2nd consecutive genuine single-instance launch failure (with #57) = BURST per house rule → ALL GPU WORK STOPPED; REBOOT required (Kevin).** Boot Y8 final: 2 spontaneous wedges (#57, #58) across ~7 loads ≈ 29 %/load (first wedges after ~5 clean loads — same late-boot-wear shape as Y7). NOT an OOM and NOT bt=4096-specific: the failure is the standard weight-load fence-timeout at the standard phase; bt=4096 itself is untested (both attempts died before first chunk). MBT-1's bt=2048 arm completed cleanly earlier this boot (4868.7 s, E1 result recorded in prefill-multibatch-tax.md); seqs2 (MBT-2) not attempted (queued post-reboot) |
| 2026-09-11 12:05:14 | ? | HW | (boot Y9, Kevin power-cycle post-#58; fresh boot ~1 min old, VRAM 0/0, kernel log clean, canary 38.8 t/s PASS 12:02 in the 38.7–38.9 band) MBT-2 seqs2 arm attempt 1 (mtp-free greedy TP=2, `--max-num-batched-tokens 1024`, `--max-num-seqs 2`, port 8162), launched 12:04:1x: Worker died ~60 s in — userspace `hipErrorLaunchFailure` (CUDA error: unspecified launch failure) from c10::cuda::SetDevice, the chronic family signature, now on a FRESH boot's FIRST TP=2 load (earliest phase yet; breaks the late-boot-wear pattern). Self-recovered (driver post-check probes 10.0/9.8, VRAM 0/0). Log: /local/tmp/b4/server_mbt_seqs2_0911_1204.log | **59th wedge — GENUINE spontaneous, chronic weight-load family, first-load-of-fresh-boot variant.** House rule: ONE retry authorized. If retry wedges: burst. NOTE: host RAM is 31 GB total (27 free) vs the 19.6 GB checkpoint — the loader's "exceeds 90% of available RAM" prefetch warning fires every load; a host-memory-pressure co-factor in this family is plausible and untested |
| 2026-09-11 12:07:53 | — | — | (boot Y9) seqs2 attempt 2 (the authorized retry): **LOADED CLEAN** (5-6 min, KV pool 589,440 tokens / 4.50× concurrency), full MBT-2 session completed: 4×122880 prefill + 256 decode ×4, wall 3598 s (60.0 min), clean SIGTERM teardown. (rocm-smi post-bench shows only the 0-B VRAM gpuagent stub; the driver's "GPUS NOT CLEAN" abort at 13:08:03 was the teardown-race false positive — VRAM drains >3 s) | **NO burst — retry clean. Boot Y9 continues: 1 wedge (#59) across 2 loads.** MBT-2 result: seqs2 wall 60.0 min vs seqs4 75.3 min = **−20%, A-halving confirmed directionally** (tax ∝ live contexts); benefit concentrated in requests #3–4. Cross-request + per-token × live-context behavior (E1 + D1c) unchanged |
| 2026-09-12 05:16:4x | 1 | HW | (boot Y10 — new day/power state; first TP=2 load attempt of the day, VRAM was 0/0 pre-launch) hold-trace session server (mtp3b4 arm, port 8143, under py-spy record wrapper) died during weight load: `terminate called ... c10::AcceleratorError: CUDA error: unspecified launch failure` / hipErrorLaunchFailure, Workers 22024/22025, ~30-40 s after Worker init (05:16:2x). VRAM self-drained to 0/0 by 05:26. NOTE: py-spy 0.4.2 `record --subprocesses --nonblocking` as the launcher wrapper had ALREADY bailed at ~52 s ("process exited" false positive at the EngineCore/Worker spawn, 25 sampling errors) — passive process_vm_readv, not a plausible GPU-fault cause; the crash signature matches the chronic load-wedge family (#26–#59) | **1 attempt failed. One authorized retry (blocking-mode py-spy or none). Burst = stop + reboot request.** |
| 2026-09-12 05:43:47 | 1 | HW | (boot Y10, attempt 3 of the hold-trace session, py-spy-FREE launch, port 8144) Worker_TP0 died at CUDA init — `torch.AcceleratorError: CUDA error: unspecified launch failure` raised from **SetDevice** (HIPFunctions.cpp:334), i.e. the GPU fails at the very FIRST device touch, before any weight load. EngineCore aborted (WorkerProc init failed). **2 GPU-wedge-class failures this boot (#60 load-fault + this init-fault) → BURST → GPU work stopped, reboot requested.** Matches the AGENTS.md degraded-boot signature (ROCr faults at init on degraded hosts; only a reboot clears) | **BURST — STOP. Reboot required before any further GPU session.** |
| 2026-09-12 05:33→05:38 | — | SW? | (boot Y10, attempt 2, py-spy record --subprocesses WRAPPING the server) APIServer loaded the engine CLEAN (KV 501,558 tokens, GPUs 91%) then stalled: "Application startup complete" printed but the uvicorn socket was NEVER created ("Uvicorn running on ..." absent, nothing on :8143, /proc/22536/net/tcp empty), main thread idle in do_epoll_wait for 7+ min until teardown. py-spy captured only early-load samples (idle threads excluded by default → stall stacks NOT captured; 25 sampling errors). Possible py-spy ptrace interference OR an intrinsic async-scheduler stall — flavor matches the hold anomaly (everything alive, one awaited event never fires). Discrimination attempt 3 (py-spy-free) never reached the API stage (wedge at SetDevice), so the question stays open | Open — retest on a clean boot, py-spy-free first, then py-spy as a variant |
| 2026-09-12 07:25:40 | 1 | HW | (boot Y11) in-process offline probe (mb_attr.py, TP=2, no HTTP layer) died at SetDevice — hipErrorLaunchFailure at first device touch, ~75 s into init. **2 GPU-wedge-class failures on Y11 (#63 SetDevice + #64 SetDevice) with 2 clean loads between → burst → GPU work stopped.** Pattern note: today's wedges hit at BARE CUDA INIT (SetDevice), not weight load; Y10 same (its #62). Kevin: after reboot, run the degradation canary BEFORE any session; 2 consecutive bad boots (Y10, Y11) is new — if the canary reads slow on a fresh boot, host-side investigation needed | **BURST — STOP. Reboot + canary gate.** |
| 2026-09-12 07:45 | — | ERRATUM | **#61 REATTRIBUTED (agent error, not vLLM):** srv_hold.sh bound port 8144 (stale sed) while the driver/sockwatch probed 8141 — attempts 5/6/7 on Y11 were "stalls" only in my probes; the servers were healthy and I killed them. Y10 attempt 2 remains the only possibly-real API-stall observation (its probes matched ports correctly; confounded by py-spy+profiler-config on a boot that wedged around it). #61 demoted from open-engine-bug to unproven-once-seen; retest only after a canary-pass boot, with port sanity (single source of truth for the port) | Corrected in dev log §13.21 |
| 2026-09-12 10:12:4x | 1 | HW | (boot Y12, canary PASSED 38.8 t/s at 07:44; FIX arm loaded+ran clean 09:26-10:12) BASE-arm launch (M3 gate OFF — the pre-M3 code path) wedged at init: hipErrorLaunchFailure. VRAM self-cleared | #65 |
| 2026-09-12 10:17:4x | 1 | HW | (boot Y12) authorized BASE retry: same hipErrorLaunchFailure at init. **2 consecutive = burst on Y12 → GPU work stopped.** Note: both wedged arms were BASE (old path); the M3-gated FIX arm had just run 40 min clean on the same boot — M3 exonerated. Wedges keep clustering on the 3rd/4th CUDA init of a boot session (Y10: #62 init-wedge; Y11: #63/#64 both SetDevice) — pattern for the host investigation: fresh boots pass the canary, then init-fault probability rises within hours | **BURST — STOP. Reboot + canary gate.** |
| 2026-09-12 20:10:26 | 1 | HW | (boot Y13, uptime 6 h, FIRST GPU work of this boot) canary PASSED 38.9 t/s (in-process TP=2 MTP k=2, CUDA init #1) 20:09:21; VRAM cleared to baseline (10.9 MB) both GPUs before the next launch. The *next* CUDA init (#2 — `mtp1srv@cat132k` launch 20:09:22, CAT-1 A/B session) wedged ~65 s in at engine init: `c10::AcceleratorError: CUDA error: unspecified launch failure` (hipErrorLaunchFailure) in Worker_TP*, EngineCore aborted. Kernel at 20:10:26: BOTH GPUs `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(1) succeeded!` → `device wedged, but recovered through reset` (0b:00.0 = GPU0, 0e:00.0 = GPU1). VRAM self-cleared, no zombie procs. Authorized single retry (matmul probes post-reset GPU0 10.0 / GPU1 9.8 TFLOPS, limit 7.0): init #3 loaded clean 20:14:20, and the real arm (init #4, MTP k=2 + 32,768-row CAT-1 head) loaded clean 20:21:28 → **single event, not a burst; chronic weight-load family (comp_1.0.0), 1st on Y13** | #67 |
| 2026-09-12 21:29:16 | 2 | HW | (boot Y13) CAT-1 ctrl-list arm launch (init #7 of the boot, `mtp1srv@cat1ctrl`): worker threw `hipErrorLaunchFailure` / "unspecified launch failure" during engine init, after weights+GDN fallback logs and while the weight-prefetch decision was being made (RAM 19.37 GiB free vs 19.89 GiB checkpoint → auto-prefetch disabled, then the fault). EngineCore aborted, VRAM self-cleared (10.9 MB both, no zombies). Prior init #6 (`mtp1srv@cat1`, syv-40K list) loaded and served 3×64k cleanly immediately before, and inits #4/#5 (cat132k + baseline) were clean too. **2nd wedge-class event on Y13 (#67 at 20:10, this at 21:29) with 4 clean inits between → NOT a burst; chronic weight-load family**; per house rules this arm gets one authorized retry | #68 |
| 2026-09-13 06:13:13 | 3 | HW | (boot Y13, ~9 h of idle after the 21:29 event) ctrl-list arm **authorized retry** (init #8, `mtp1srv@cat1ctrl`, the wedge-#68 arm): same `hipErrorLaunchFailure` at engine init; this time the driver escalated — `amdgpu 0000:0e:00.0: GPU reset begin!. Source: 4` → **BACO reset** → `VRAM is lost due to GPU reset!` → `GPU reset(3) succeeded!` → `device wedged, but recovered through reset`. VRAM self-cleared (10.9 MB both). **2 consecutive failures for the SAME arm work-dir (#68/#69) with 4 clean inits in between on other work-dirs (cat132k, cat1-40K, baseline) → per house rules: GPU work STOPPED, reboot before the next session.** Note for the record: the two failing launches both used `/local/tmp/mtp1/cat1_32768ctrl`; the three clean ones used `cat1_32768`, `cat1_pilot` and the pristine snapshot. Cannot distinguish "work-dir-specific" from "boot-wear init #7/#8" without a fresh boot — retest the ctrl dir FIRST on the next boot (cheapest discriminator: one load) | #69 — STOP |
| 2026-09-13 07:49:29 | 1 | HW | (boot Y14, fresh ~40 min: canary PASS 38.9 t/s, one clean arm loaded+served) `mtp1srv`-style launch of the **pristine snapshot** (init #3 of the boot, direct-launch variant of the MTP k=2 arm) wedged at engine init: `hipErrorLaunchFailure`; kernel escalated immediately — `amdgpu 0000:0e:00.0: GPU reset begin!. Source: 4` → **BACO reset** → `VRAM is lost due to GPU reset!` → `GPU reset(1) succeeded!` → recovered. VRAM self-cleared. **First wedge on Y14; chronic weight-load family (GPU1), same signature as #69.** One authorized retry per house rules | #70 |
| 2026-09-13 08:16:07 | 2 | HW | (boot Y14) `pristine5` arm (snapshot, MTP k=2) init **failed again** — same signature: `hipErrorLaunchFailure` → `GPU reset begin! Source: 4` → **BACO** → `VRAM is lost` → recovered. This is the 2nd init-failure of the *same* pristine arm on Y14 (#70 at 07:49) with a *work-dir* arm loading cleanly in between (cat1ctrl served 5×64k + 3 captures) — i.e. the failures follow the boot's load lottery, not the model directory (Y13 was the mirror image: the work dir failed twice, pristine clean). Authorized retry (attempt 2) launched immediately | #71 |
| 2026-09-13 10:33:45 | 1 | HW | (boot Y16, V2-runner smoke: VLLM_USE_V2_MODEL_RUNNER=1 forced on Qwen3.8-27B-GDN, scrub tree) WorkerProc init wedge — hipErrorLaunchFailure. Qwen3.5/3.8-GDN is NOT in DEFAULT_V2_MODEL_RUNNER_ARCHITECTURES; forcing V2 on this arch is untested-by-design — likely V2-incompatibility rather than random wedge (Y16 also had one canary init-fail at 07:1x; a clean green canary + clean V1 smoke sat between). VRAM self-cleared. NOT retried | V2-forced on GDN arch recorded as unsupported; V1 (production path) smoke PASSED on the same tree |
| 2026-09-13 12:08:55 | 0 | HW | (boot Y16, up since 07:10) **README-perf-refresh session**, first big-model load of the boot: `_bench_gfx906.py` on `/data/models/qwen/Qwen3.5-27B-AWQ` (in-process, TP=1 GPU0, util 0.95) died during safetensors shard load (0/8) — `hipErrorLaunchFailure` from `c10::cuda::SetDevice` (the chronic weight-load family). Kernel: `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(2) succeeded!` → `device wedged, but recovered through reset` (no `VRAM is lost` line this time). Preceding: canary PASS 38.8 t/s at 12:05; the previous arm aborted on a **software** error (stale model path) ~40 s before, so the wedge followed a failed launch, not a heavy run. VRAM back to 10.9 MB/GPU, no zombies | **#72 — session wedge #1; one retry authorized** |
| 2026-09-13 12:14:33 | 0 | HW | (boot Y16, **#72's authorized retry**, same session) post-wedge probe CLEAN (gpu0 10.0 / gpu1 9.8 TFLOPS fp16) + canary PASS 38.9 t/s, then the MoE arm (`/data/models/QuantTrio/Qwen3.5-35B-A3B-AWQ`, 23.71 GiB, shard 0/9) died with the same `hipErrorLaunchFailure`; kernel escalated to `GPU reset begin!. Source:  4` → **`BACO reset`** → `VRAM is lost due to GPU reset!` → `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(3) succeeded!` → recovered. 2nd consecutive genuine load failure on GPU0 (§6 min after #72) → **BURST: all GPU work stopped** (the script's 3rd arm was killed mid-load at 62 %); VRAM 10.9 MB/GPU, no zombies | **#73 + BURST — session stopped; reboot recommended before further GPU work** |
| 2026-09-13 15:51:39 | 1 | HW | (boot f27e8058, README-perf-refresh session; ~90 min of clean benching behind it: canary 39.0, probes 10.0/9.8, MoE+dense cold and warm runs all mclk-verified) first launch of the **CAT-1 agentic headline session** (TP=2 `run_server.sh greedy`, both GPUs loading): GPU1 `0e:00.0` — `PSP is resuming...` → `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(1) succeeded!` → `device wedged, but recovered through reset`; the GPU1 worker died → arm 'failed to start', **authorized retry (attempt 2) loaded clean in 1.7 min** and the session proceeded (1 retry per wedge; only 2 consecutive failures = burst) | **#74 — session wedge #1 of this boot; retry PASSED** |
| 2026-09-13 16:51:05 | 1 | HW | (boot f27e8058, CAT-1 headline session, **arm 3 of 3** first launch — TP=2 `run_server.sh cat1v3k3`, the manifest-carrying CAT-1 work dir) GPU1 `0e:00.0` — `BACO reset` → `GPU reset succeeded, trying to resume` → `VRAM is lost due to GPU reset!` → `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(2) succeeded!` → recovered; worker died → 'failed to start'. **Authorized retry (attempt 2) ready in 5.7 min** and the sweep proceeded (1 row in at the time of writing) | **#75 — 2nd wedge of this boot, same load family (GPU1 again, after #74 on the same GPU 60 min earlier); retry PASSED, session continues** |
| 2026-09-13 18:17:32 | 0 | HW | (boot f27e8058, 0.29.0 validation session: FA suite 89 passed at 18:16, then the PPL probe on the fresh merge) GPU0 `0b:00.0` wedged during **weight load** (shards 4/5 of Qwen3.8-27B): `hipErrorLaunchFailure` → `Fence fallback timer expired on ring comp_1.0.0` → `GPU reset(2) succeeded!` → recovered. The probe process **hung after the reset and kept 20.8 GB allocated**; SIGTERM ignored, SIGKILL released it, VRAM back to the 11 MB baseline | **#76 — session wedge; one retry authorized after the VRAM check** |
| 2026-09-13 18:19:51 | 0 | HW | (boot f27e8058, 0.29.0 validation) **BURST**: the V1 serving smoke (`vllm serve` from the merge worktree, V2 pinned off, Qwen3.8-27B) died during engine-core init with the same `hipErrorLaunchFailure`/`SetDevice` signature ~2 min after #76 — no clean load in between. Kernel escalated via BACO: `GPU reset succeeded, trying to resume` → **`VRAM is lost due to GPU reset!`** → `GPU reset(4) succeeded!` → recovered. VRAM back to 10.9 MB/GPU, no hung holders | **#77 + BURST — 2 consecutive genuine load failures ⇒ all GPU work stopped; reboot before further GPU work** |
| 2026-09-13 19:20:5x | 0 | HW | (boot a27a894e, 0.29.0 validation: canary 38.9 PASS at 18:56, then **0.28 PPL 10.5516** and **0.29 PPL 10.5516** — bit-identical — loaded clean) the V1 serving smoke (`vllm serve` from the merge worktree, V2 pinned off) wedged at engine-core init, 4th load of the boot: `hipErrorLaunchFailure` from `SetDevice` → fence fallback → reset, recovered. Kernel: 5 reset/fence lines in the window | **#78 — 1st wedge of this boot; one retry authorized** |
| 2026-09-13 19:57:48 | 0 | HW | (boot eefacc1e, V2 bring-up Session A) **A1: V2 eager** — first V2 launch, wedged at engine-core init: `hipErrorLaunchFailure` from `SetDevice` → `BACO reset` → `VRAM is lost` → `GPU reset(1) succeeded` → recovered. **A2 (V2 graph, the same config path) then loaded clean and served** | **#79 — load-lottery wedge; the V2 question was answered by A2, so this wedge is NOT arch evidence** |
| 2026-09-13 20:36:29 | 0 | HW | (boot eefacc1e, Session B parity restamp) 4th run (`dense_cold`, the 0.29 branch) died during weight load: `hipErrorLaunchFailure` → `GPU reset succeeded, trying to resume` → `VRAM is lost due to GPU reset!` → `GPU reset(2) succeeded` → recovered. Runs 1-3 of the session (MoE warm/cold, dense warm) had completed cleanly, and V2 PPL + V2 graph serve before them | **#80 — 2nd wedge of this boot, both load-family; one retry authorized for this arm** |
| 2026-09-14 06:58:15 | 0 | HW | (boot eefacc1e, ~11 h up; agentic MTP k=3 session on the 0.29 line) `catmtp3` arm attempt 1 died during weight load: `hipErrorLaunchFailure` (`SetDevice`) → `BACO reset` → `VRAM is lost` → `GPU reset(3) succeeded` → recovered. **Authorized retry (attempt 2) loaded clean in 1.8 min** and the sweep ran. Same boot also carries yesterday's #79 (V2 eager) and #80 (dense cold) | **#81 — load-lottery wedge; retry PASSED, session continued** |
| 2026-09-14 07:29:4x | 0 | HW | (boot eefacc1e, agentic session on the 0.29 line) `cat1v3k3` arm attempt 1 died during weight load (same signature as #81: `hipErrorLaunchFailure` → BACO/VRAM-lost reset → recovered); **authorized retry (attempt 2) READY in 5.6 min** and the sweep ran clean, with the CAT-1 shortlist marker logged | **#82 — 2nd load wedge of the day on this boot; retry PASSED** |
| 2026-09-14 10:45:02 | 1 | HW | (boot eefacc1e, V2 bring-up session B) V2 `v2catmtp3` arm attempt 1 died at engine-core init: `GPU reset begin! Source: 4` → `BACO reset` → `VRAM is lost` → `GPU reset(2) succeeded` (0e:00.0 — **GPU1**, unlike #81/#82 which hit 0b:00.0), recovered in 3 s. **Authorized retry (attempt 2) loaded clean and the arm ran at V1 parity** (64k 34.38/32.86 acc 2.05/1.93). Third wedge of this boot; the session script's per-attempt log preservation again separated the wedge from the software path | **#83 — load wedge on the second GPU; retry PASSED, session continued** |
| 2026-09-14 14:23:13 | 0+1 | HW | (boot eefacc1e, flip smoke) **Both** GPUs wedged at the same time — 0b:00.0 `GPU reset begin! Source: 4` → BACO → 0e:00.0 the same 9 s later — during the first V2 launch after ~1 h idle: `hipErrorLaunchFailure` → engine-core init failed. Recovered, VRAM back to 10.9 MB. Fourth wedge of this boot (#81/#82 GPU0, #83 GPU1, #84 both). Also a script lesson: the smoke's liveness check raced the launch (false "SERVER DIED") and it exited *before* teardown, leaving a live server for the next check to kill — the teardown now runs from a `trap` | **#84 — load wedge, both decks, retry authorized** |
| 2026-09-14 14:34:43 | 1 | HW | (boot eefacc1e, flip smoke retry) Second consecutive load failure: `hipErrorLaunchFailure` at engine-core init with a 0e:00.0 BACO reset (`GPU reset(5) succeeded`, recovered in 3 s) — 11 min after #84. The wedge hit **after** V2's model_runner had already logged, so the flipped default did select V2 (1 bare `[model_runner.py:*]`, 0 `gpu_model_runner.py`); no completion could be returned. Wedges this boot: #81 GPU0, #82 GPU0, #83 GPU1, #84 both, #85 GPU1 — **5 wedges in ~8 h, the last two consecutive** | **BURST — GPU work STOPPED. Reboot required before any further GPU session** |
| 2026-09-14 14:54:34 | 1 | HW | (fresh boot, up 12 min; flip smoke) V2 launch wedged ~15 s into model load (`hipErrorLaunchFailure` right after V2's `model_runner.py` "Loading model from scratch"): 0e:00.0 `GPU reset begin! Source: 4` → BACO → `reset(1)` → recovered. **Diagnostic use of the authorized retry:** the *V1-pinned* arm of the identical recipe loaded and served cleanly at once (READY 110 s, KV 496,693, completion returned, 0 resets), and the V2 retry after it also came up clean (READY 100 s, 7 bare `model_runner.py` tags, completion **text-identical** to the V1 arm) — so the wedge was the load lottery, not a V2 regression | **#86 — 1st wedge of the new boot; both runners have now wedged today (#81/#82 V1, #83–#86 V2)** |
| 2026-09-14 16:37:2x | 0 | HW | (boot 14:42, A3 gate) first `mtp7` (k=7) launch wedged at load: `hipErrorLaunchFailure` + GPU0 BACO | **#87 — load wedge; retry authorized** |
| 2026-09-14 16:39:46+16:39:55 | 0+1 | HW | (same session) the `mtp7` retry wedged the same way, both decks this time (GPU0 then GPU1 9 s apart, `reset(2)` succeeded). The k=3 arms had loaded four times in a row just before. **2 consecutive load failures = burst** → GPU work stopped (A3's k=7 gate left unmeasured) | **#88 — BURST; reboot required before the next GPU session** |
| 2026-09-14 17:40:26 | 1 | HW | (fresh boot, canary 38.6 t/s, A3 k=7 arm) first `mtp7` launch wedged ~13 s after "Loading model from scratch": `hipErrorLaunchFailure` + 0e:00.0 BACO `reset(1)`; the authorized retry then loaded normally (11.7 GiB held, no resets) but was still initialising when the session's own 360 s startup budget expired — the first load of a new spec config pays a cold compile/capture, so that arm was killed by the harness, not by the GPU (startup budget raised to 900 s + a teardown trap added) | **#89 — load wedge; the retry was fine** |
| 2026-09-15 12:16:30 | 1 | HW | (boot 06:20, TRITON-1 accept experiment) a360 arm (fork 3.6.0+gfx906) first launch wedged at load: `hipErrorLaunchFailure` + 0e:00.0 BACO `reset(1)`. The preceding arm (stock triton 3.8.0) had loaded and swept cleanly minutes earlier | **#90 — load wedge** |
| 2026-09-15 12:18:27 | 1 | HW | (same session) the authorized retry wedged identically 2 min later (`hipErrorLaunchFailure` + 0e:00.0 BACO `reset(2)`) → **2 consecutive genuine load failures = BURST; GPU work stopped**, the fork's after-sample and the cross-build 128-token greedy comparison left unmeasured pending a reboot | **#91 — BURST** |
| 2026-09-15 18:27:08 | 0 | HW | (boot 13:20, MUSE-1 session) the V2 PPL arm died with `hipErrorLaunchFailure` + 0b:00.0 `reset(1)` 2 s into the failure; the next arm loaded and ran cleanly right after, so this is the load lottery | **#93 — load wedge; the interleaved arms after it passed** |
| 2026-09-15 19:08:55 | 0 | HW | (boot 13:20, MUSE-1 gate) the V1 arm's first load crashed with a core dump (`hipErrorLaunchFailure` + 0b:00.0 BACO `reset(2)`); the next arm then failed with `RuntimeError: No CUDA GPUs are available` (the reset's aftermath, not a second independent failure), and the arm after that loaded and ran normally | **#94 — first-load wedge; recovery on the following arm** |
| 2026-09-15 20:39:0x | 0 | HW | (boot 13:20, DFL2-1 arm C relaunch after the triton_matmul fix) attempt 1 of the DFlash2 load died as "WorkerProc initialization failed due to an exception in a background process": one GPU reset in the same window, i.e. the load lottery. The retry loaded and ran (the software assert from the first session is gone - the fix holds) | **#95 - load wedge; retry passed** |
| 2026-09-16 07:10:05 | 1 | HW | (the long-running 2026-09-15 13:20 boot, INT8-PACKED-1 first INT8 load; wedges #93-#95 already on this boot) first `vllm serve` of the W8A16 pack-quantized checkpoint (TP=2, fp16 KV, no spec decode) died during weight load with `CUDA error: unspecified launch failure` after "Resolved architecture: Qwen3_5ForConditionalGeneration" | `0e:00.0 GPU reset(4) succeeded!` + "device wedged, but recovered through reset" (07:10:05); VRAM back to 0.00 GB on both GPUs; one authorized retry next. Not a `weight_packed` error - the embedding quant_config wiring fix was past that point |
| 2026-09-16 20:04:53 | 0 | HW | (boot 07:31, FA-D96 serving gate arm B = `GFX906_FA_PAD96=1`) the second of three server loads died at 60% of the safetensors shards ("3/5") with `terminate called ... c10::AcceleratorError: CUDA error: unspecified launch failure` / `hipErrorLaunchFailure` from `c10::cuda::SetDevice` inside an at::copy_ during weight load, i.e. **before any attention/graph code ran**; the APIServer then reported "Engine core initialization failed". The arm immediately before (pad0) and the arm immediately after (pad0) both loaded and served cleanly | **#96 — chronic weight-load family (load lottery), as #93-#95: one authorized retry, taken immediately (arm B re-run launched); no burst** |
| 2026-09-16 20:05:28 | 0 | HW | (boot 07:31, FA-D96 arm B — teardown of the 20:04:53 load wedge) the failed load's teardown hit the known signature: `psp gfx command UNLOAD_TA(0x2) failed ... (0x117)` -> BACO reset -> `GPU reset(2) succeeded!` -> "device wedged, but recovered through reset" (0b:00.0). The arm immediately after (pad0, 20:05:48) then loaded and served cleanly | **#96a — reset following the load wedge; the next load passed** |
| 2026-09-16 20:13:52 | 0 | HW | (boot 07:31, FA-D96 arm A-repeat = the third arm of the A-B-A order control) same family: the load died at ~65 s (`Engine core initialization failed`), and the kernel logged `UNLOAD_TA(0x2) ... (0x117)` -> BACO reset -> `GPU reset(3) succeeded!` + "device wedged, but recovered" (0b:00.0). **Second BACO reset in 8 min (20:05:28, 20:13:52) = a BURST**, so the session stopped there per the standing rule — no further loads, no canary spend. Post-state: no vllm processes, GPUs idle, `rocm-smi` briefly unusable (its own python init failed with MemoryError twice) and recovered on retry | **#97 — burst (2 resets / 8 min); session stopped, arm A-repeat dropped, reboot before the next GPU session** |
| 2026-09-16 20:46:18 | 0 | HW | (fresh boot 20:38, first GPU load of the boot = the FA-D96 promotion arm A) the VL 27B load stalled at 60 % of the safetensors shards with the EngineCore CPU-spinning at ~98 % and the kernel logging `Fence fallback timer expired on ring page1` on 0b:00.0 every ~30 s from 20:46:18; SIGTERM did not drain (escalated to KILL per the campaign recipe), VRAM drained to ~11 MB, then `GPU reset(1) succeeded!` + "device wedged, but recovered through reset" at 20:48:48 | **#98 — load-lottery hang + reset on a 10-minute-old boot; one authorized retry (session re-ordered: the local-model C2 arm first, the NFS-model FA-D96 arms after)** |
| 2026-09-16 21:55:07 | 0 | HW | (boot 20:38, C2-BM>=2 gate, arm B = `VLLM_GFX906_MOE_BM=2`, in-process harness) the 35B MoE load died at 89 % of the safetensors shards with `terminate called ... c10::AcceleratorError: CUDA error: unspecified launch failure` from `c10::cuda::SetDevice` (rc=134, core dumped). The arm immediately before (BM unset, 85.7/85.2/85.5 t/s) and the retry after both ran cleanly | **#99 — chronic weight-load family (load lottery), as #93-#98; one authorized retry taken** |
| 2026-09-17 05:58:46 | 0 | HW | (boot 20:38 on 09-16, still up; first GPU load after the MUSE-2 arms = the FA-NONCAUSAL verification arm: Muse assistant + graphs, TP=2) the load died with `terminate called ... c10::AcceleratorError: CUDA error: unspecified launch failure` (`hipErrorLaunchFailure`) while the workers were initialising, i.e. the chronic weight-load family, before any of the changed attention code ran; the next load (retry) was clean | **#100 — load-lottery, one authorized retry taken** |
| 2026-09-17 11:28:43 | 0/1 | HW | (boot 20:38 on 09-16, ~15 h up; FA-NONCAUSAL A-B-A middle arm = the graph arm) the load died with `hipErrorLaunchFailure` / `unspecified launch failure` (4 occurrences in the log) during worker init; the kernel logged `GPU reset(1) succeeded!` + "device wedged, but recovered" on 0e:00.0. The arm before it (control, 30.5 t/s) and the arm after it (control repeat) both loaded and ran | **#101 — load lottery** (the reset landed on 0e:00.0, i.e. the *other* GPU than #100's 0b:00.0); one authorized retry queued after the session's third arm** |
| 2026-09-17 11:38:02 | 0/1 | HW | (same boot) FA-NONCAUSAL graph arm, retry: identical signature to #101 — `hipErrorLaunchFailure` in a `copy_`/`SetDevice` right after the **drafter's** 4.76 GiB shard finished loading (target 5/5 then drafter 1/1, then the abort); one reset in the window | **#102 — load family; second graph-arm attempt lost** |
| 2026-09-17 11:45:58 | 0/1 | HW | (same boot) FA-NONCAUSAL clean re-measure, graph arm (first of the B-A-B): identical signature again — target 5/5, drafter 1/1, then `hipErrorLaunchFailure` from `copy_` (traceback frames: `at::native::copy_` -> `c10::cuda::SetDevice` -> `HIPFunctions.cpp:334`). The control arms in the same window loaded 5/5 cleanly. Session stopped there (3 resets in 25 min = burst; "2 consecutive → stop + reboot" honoured) | **#103 — load family; burst; GPU work stopped for this branch** |
| 2026-09-19 23:44–23:56 | 0-3 | SW | (4× gfx906 box; 2026-09-19 session on Qwen3.8-Flash-Next-AWQ-INT4, TP=4 + EP, first boot **with MTP k=3** added to `run-vllm`) all 4 TP workers died during engine init with the faulthandler banner `!!!!!!! Segfault encountered !!!!!!!`; innermost frames were C-level GC (`gc_collect_main` / `update_refs` / `deduce_unreachable`) with **no Python frames**, inside `Worker.determine_available_memory` → `profile_cudagraph_memory()` → `capture_model()` → `gc.collect()` (`model_runner.py:919`) → `speculator.capture()` (`:944-946`). No `Graph capturing finished`, no KV allocation, so `Executor: Worker proc VllmWorker-2 died unexpectedly (exit code: None)` → `EngineCore failed to start`; nothing ever served | **not a wedge** — no kernel reset in the window (`dmesg -T` clean, no OOM kill), no VRAM left pinned, and a spec-free boot of the same binary + checkpoint came up fine 9 min later. Use-after-free style corruption in the speculator's CUDA-graph capture, MTP-only, cold `torch.compile` cache. Details: `/ai/engines/gfx906/unverbraucht/mtp-cudagraph-profile-crash.md` §1 |
| 2026-09-20 02:25–02:30 | 0-3 | SW | (same box/binary/checkpoint, MTP k=3 again, warm caches) different face, same phase: 1 s after `speculator.py:151 Capturing model for speculator...`, all 4 ranks died with `ProcessGroupNCCL: process group watchdog thread terminated ... CUDA error: operation not permitted on an event last recorded in a capturing stream` (`hipErrorCapturedEvent`) → `terminate called after throwing an instance of 'c10::DistBackendError'`. Stack: `Watchdog::runLoop` → `WorkNCCL::isCompleted` → `c10::hip::Event::query` → `C10_CUDA_CHECK` at `c10/hip/HIPEvent.h:112` (torch 2.11.0a0+git70d99e9, ROCm 7.2). The *target's* PIECEWISE (4/4) and FULL (2/2) captures succeeded first; only the drafter's capture triggers it, because it enqueues collectives inside the capture region (attention backend `QWEN4_EXP_EXP_QSA_STATE` has no fused multi-step draft decode → `speculator.py:120` rebuilds attention metadata between draft steps) | **not a wedge** — pure software/host-side abort, no reset, GPUs healthy afterwards. PyTorch/ROCm gap: `hipErrorCapturedEvent` is handled nowhere in `c10/` or `torch/csrc/distributed/`, so an event recorded on a capturing stream is fatal instead of "not ready". Same defect as the 23:44 entry, seen warm. Details: same file §7 |
| 2026-09-20 02:31–02:34 | 0 | SW | (same session, boot attempt **without** speculation — `speculative_config=None`) worker raised `list index out of range` from `torch.ops.vllm.qwen4_exp_amd_ple_ngram_embedding` inside the AOT-compiled piecewise graph during the profile dummy run (`determine_available_memory` → `execute_model` → `piecewise_backend.py:380`) → `Engine core initialization failed`. The retry 7 min later had **byte-identical** non-default args and the same AOT artifact (`torch_aot_compile/b637f4ddf562ed1f…`) and booted normally | **not a wedge** — no reset. *Verdict superseded 04:40 UTC:* this is the `list index out of range` variant (`self._shards[shard]`, `ple_layer.py:118`) of the PLE ngram pinned-buffer race described in the 04:26 row below — same op, same file, and a spec-free boot hitting it is what proves it is independent of MTP. Not load-lottery, not post-crash flakiness: the identical retry passes because the fault depends on what the host reads before the D2H copy lands. Details: `/ai/engines/gfx906/unverbraucht/mtp-cudagraph-profile-crash.md` §8, §12 |
| 2026-09-20 03:48–04:02 | 0-3 | SW **resolved** | (same box, MTP k=3 re-run with `TORCH_NCCL_BLOCKING_WAIT=1`, which makes `ProcessGroupNCCL` skip `watchdog_->start()` — `ProcessGroupNCCL.cpp:909 + :974-981` — so the illegal `hipEventQuery` is never issued) **first MTP boot this box has ever completed**: both speculator captures survived (23:53:27 and 23:53:54 local), KV allocated (`Available KV cache memory: 6.06 GiB` = 407,385 tokens, max concurrency 3.11x), `Application startup complete` after 5m44s, ≥14 min serving, `SpecDecoding metrics` acceptance length 2.32–3.39 (typ. 2.7), median decode **45.0 t/s vs 22.5 t/s** spec-free (≈2.0x). Zero segfaults, zero watchdog aborts | **workaround validated — MTP on this box requires `NCCL_BLOCKING_WAIT=1`.** Opt-in guard in `run-vllm` (off by default); costs: 1.42 GiB/GPU of captured graphs → `MAXSEQS` 4→3, blocking NCCL completion, no NCCL hang detection / flight dumps. Real fix is upstream (both halves). Details: same file §10 |
| 2026-09-20 04:26:42 | 0 | SW | (same box, MTP k=3 + `TORCH_NCCL_BLOCKING_WAIT=1` — the boot that had served ~38 min at ~45 t/s) **first runtime failure, while serving**: `Worker_TP0_EP0` (pid 620155) raised `IndexError: index out of range in self` at `ple_layer.py:121  out[mask] = tensor.index_select(0, local_idx[mask])`, inside `torch.ops.vllm.qwen4_exp_amd_ple_ngram_embedding(buf33, buf34, 'language_model.model.layers.1.ple')` on a real request; engine core dumped the step (`dump_input.py:72`) then died (`core.py:1376`), 4 in-flight requests failed (`async_llm.py:819`, `serving.py:906` ×2), `Shutting down` | **not a wedge** — kern.log clean (no `amdgpu`/`GPU reset`/PSP), no coredump, `journalctl -p err` empty since 22:00, `rocm-smi` healthy 42-45 °C. Root cause: unsynchronized device→host copy into the persistent pinned ngram-id buffer (`ple_layer.py:1141`, `non_blocking=True`) — the host looks up shard rows with ids it read before the DMA landed. Row-count proof that model-produced ids are always in range (capacity == on-disk rows == 2,500,012; ids < 320,001,446) is in the report; introduced by `ac5a93841 "fix: offload PLE table to system RAM via mmap"`. Also implies **silent** incorrect lookups in steady state (stale ids are in range). Fix prepared, not applied: `ple-ngram-race.patch`. Details: `/ai/engines/gfx906/unverbraucht/mtp-cudagraph-profile-crash.md` §12 |
| 2026-09-20 04:27–04:32 | 0-3 | SW | restart attempt (00:27 local, byte-identical non-default args incl. `speculative_config: {'method': 'mtp', 'num_speculative_tokens': 3}`) died at startup: `Worker_TP2_EP2` (pid 632347) raised the same `IndexError: index out of range in self` 1 s after `torch.compile took 8.80 s` — the first execution of the freshly loaded AOT artifacts | **not a wedge** — same PLE race as 04:26, startup variant: `_pinned_ngram_ids` is `torch.empty(...).pin_memory()` (`ple_layer.py:239-241`) and is never zeroed, so the first step reads uninitialized ids. The *next* restart booted fine, which is the expected signature of a race rather than a corrupt artifact |
| 2026-09-20 04:32 → | 0-3 | SW **open** | third restart (pid 633898) reached `Application startup complete` 00:37:56 local and served through 05:13+ with MTP k=3; engine stats normal (`Mean acceptance length: 2.77`) | **open, not fixed** — every step still races, and in steady state the stale ids are in range, so wrong PLE rows are silently served instead of crashing. Needs one restart with the patched `ple_layer.py` to close. Until then this box can die at any step |
| 2026-09-20 10:25 / 10:37 / 10:59 | 0-3 | SW | three MTP k=3 boots (`max_model_len` 147456 then 131072, `mode 3` + capture sizes `[4,8,12]`) died identically ~3 min in, 1 s after `torch.compile took 10.09 s`: `torch.AcceleratorError: CUDA error: operation not permitted when stream is capturing (hipErrorStreamCaptureUnsupported)` from `pinned_ids.copy_(ngram_ids, non_blocking=False)` (`ple_layer.py:1151`) called from inside the AOT-compiled piecewise graph (`cudagraph_utils.py:463 run_pw_graph` → `capture_model`). Cause: the `ple-ngram-race.patch` hot-copy was in place while CUDA graphs were enabled — a blocking copy cannot sync inside a capture | **not a wedge** — self-inflicted by deploying the PLE patch into a graph-enabled run, and the first hard evidence that `vllm::qwen4_exp_amd_ple_ngram_embedding` **is** captured (it is absent from `splitting_ops`; `no_compile_layers` is a name→layer side table, not a capture exclusion). Reverting the one line restores boot. Graph mode needs the op in `splitting_ops` **and** `cudagraph_mode` PIECEWISE: report §13.3 |
| 2026-09-20 10:49 / 18:29 / 18:39 / 18:46 | 0-3 | SW **open** | MTP k=3 with CUDA graphs (patch reverted) segfaulted again, 5/5 attempts, same face as 2026-09-19 23:44: `!!!!!!! Segfault encountered !!!!!!!` ×4 in `_PyGCHead_NEXT` → `update_refs` → `deduce_unreachable` → `gc_collect_main`, from `model_runner.py:919 gc.collect()` inside `capture_model`, each time immediately after the drafter (`.../eagle_head`) finished compile + `Initial profiling/warmup run` | **not a wedge** — CPython GC corruption (use-after-free), crash site ≠ cause; flaky, not deterministic: 23:48 and 00:32 on 09-19/09-20 served with the same graphs+MTP config. `TORCH_NCCL_BLOCKING_WAIT=1` does not cover this face (it only removes the watchdog thread). Practical rule until fixed upstream: **MTP runs with `--enforce-eager` on this box** (2/2: 10:09 and 18:03 local. The 5 h 30 m 45.8 t/s run at 00:32 used graphs + MTP and survived the same coin flip). Report §13.4 |
| 2026-09-20 18:52 → | 0-3 | SW **open** | boot with graphs and **no** speculation (pid 854777, `cudagraph_mode FULL_AND_PIECEWISE (2,1)`, capture sizes `[1,2,3]`, `splitting_ops` = 19 default entries without the PLE embedding op) captured `PIECEWISE 3/3` + `FULL 2/2` + `FULL 3/3` and reached `Application startup complete`; serving now at `max_model_len 147456` | **not a wedge, but silently wrong output** — the PLE op's host-side gather runs once at capture and CUDA graph replay cannot re-run it, so every step re-serves the capture-time layer-1 PLE embedding (and the capture-time read is itself the §12 race). "Serving fine" is not evidence of correctness here. Fix = op in `splitting_ops` + `cudagraph_mode: 1` + the PLE patch, or run eager. Report §13.2 |
| 2026-09-20 20:17 / 20:22 | 0-3 | SW **open** | MTP k=3 with the **new** graph config (`GRAPHMODE=piecewise`: `cudagraph_mode 1` + 20-entry `splitting_ops`) + the patched `ple_layer.py` + custom `--chat-template`: worker killed by a signal during `determine_available_memory()`, `Worker proc VllmWorker-0 died unexpectedly (exit code: None)`, Worker_TP0's last line `Initial profiling/warmup run took 1.15 s`. No Python traceback from the worker, no host OOM-kill today (only 09-19 08:10), no amdgpu reset -> native crash, same CPython-GC face as the 05:29/18:29/18:39/18:46 entries; the victim GC here is the one at the end of the memory-profile run instead of the one inside `capture_model()`. **Piecewise graphs do not protect against it.** The 20:28 boot with the *same template and same graph config but no speculation* started fine and served -> not the template, not the graph mode: MTP. Workaround: `GRAPHMODE=eager ./run-vllm` (MTP+k, 2/2 at 07:09/14:03). |
| 2026-09-20 19:58-20:09 | 0-3 | SW resolved | `jinja2.exceptions.TemplateError: No user query found in messages.` x4 (`vllm.log` 973, 1980, 2038, 2101, all `pid=924727`), raised from `<template>` line 100 while `render_jinja_template` applied the **official** Qwen chat template: it hard-raises when the conversation has no user turn (all-tool-result tails), which kills agentic chains. Request-time failure in the APIServer, not a boot failure. Fixed by `/ai/models/hf.co/froggeric/Qwen-Fixed-Chat-Templates/chat_template.jinja` (mtime 20:16), which replaces the raise with a `last_query_index` fallback (lines 223-238) and is passed via `--chat-template` (`run-vllm:324`). Note `--chat-template` is read once at startup: the fixed file needs a restart to take effect, which is why the error persisted after the file was replaced. 0 occurrences on the 20:28 boot (pid 944312). |
| 2026-09-20 20:59 → | 0-3 | SW workaround in effect | Restart with `GRAPHMODE=eager` (pid 953459, pgid 953448): `--enforce-eager` + MTP k=3 + custom `--chat-template`, `TORCH_NCCL_BLOCKING_WAIT=1`. Booted in 4m47s, `Application startup complete`, 0 graph-capture lines (correct for eager), 0 errors, 0 `TemplateError`. MTP is functioning: `SpecDecoding metrics: Mean acceptance length 2.54 -> 3.63` (max 4), `Accepted throughput 15-21 tokens/s`. KV: 6.9 GiB / 468,137 tokens (~3.2x of 147,456 at `--max-num-seqs 3`). Template verified against the previously fatal shape (messages ending in a `tool` result, no trailing user turn) -> HTTP 200. **Caveat measured:** generation throughput at 1 running request is 20.7-20.9 tokens/s, i.e. *not* better than the graphs/no-speculation boot's 22.2 -- eager's per-step launch overhead is eating MTP's ~3.6x acceptance. The fast configuration remains graphs+MTP (45.8 t/s, 2-of-9 boots). |
| 2026-09-20 late (no restart) | 0-3 | SW investigation + **staged config change** | Root-cause work on the MTP+graphs GC segfault, report §14. Three findings: (1) the `!!!!!!! Segfault encountered !!!!!!!` handler belongs to **tvm_ffi** (`src/ffi/backtrace.cc:168`, bare `std::signal(SIGSEGV, ...)`) and it **replaces faulthandler's**, which is why 11 boots produced C-only backtraces and never a Python traceback; no env knob disables it, so the next diagnostic boot must use `PYTHONMALLOC=debug`. (2) On ROCm only, tvm_ffi JIT-builds a C DLPack addon and installs it as a `torch.Tensor` class attribute (`_optional_torch_c_dlpack.py:97-98,169-176`), reusing it by **filename** with no torch-version hash in the key (`:137,:139`); the on-disk `/ai/root/cache/tvm-ffi/libtorch_c_dlpack_addon_torch211-rocm.so` (09-18 20:44) predates the installed torch (09-19 06:44, 115 files newer) and imports `THPVariable_Wrap`. Candidate corruptor, **not** proven: `libtvm_ffi.so` is mapped in the eager boots that never crash too. Now off by default via `TVMFFI=disable` -> `TVM_FFI_DISABLE_TORCH_C_DLPACK=1`. (3) `"enforce_eager":true` in `--speculative-config` is **dead code** on this build's MTP path (read only by the old `v1/spec_decode/` proposer; zero reads under `v1/worker/gpu/spec_decode/`; never propagated to the draft `ModelConfig` at `config/speculative.py:1258`) -> the 09-19 22:25 boot that "still captured speculator graphs despite the flag" tested nothing. Replaced by `drafter-graphs-off.patch`: one line at `v1/worker/gpu/model_runner.py:667` giving the drafter `CUDAGraphMode.NONE` instead of the target's mode, env-gated by `GFX906_DRAFTER_GRAPHS` (`run-vllm` `DRAFTER_GRAPHS`, default 0 = patched). **Applied to both trees, backups `model_runner.py.pre-drafter-graphs.bak`, `py_compile` clean, `patch --dry-run` verified; it takes effect on the next boot only** -- the running 20:59 eager server is unaffected. Also logged: `run-vllm:53` `export MAXLEN="${MAXLEN:-147456}"` makes the `:-131072` fallback at `:144` dead, so the live context is 147,456 (`Maximum concurrency for 147,456 tokens per request: 3.17x`) while the `MAXSEQS=3` rationale text quotes 131,072 arithmetic; harmless today, left alone because changing it invalidates the AOT compile cache. |
| 2026-09-20 19:28-20:17 | 0-3 | SW **counterexample to the 20:17 row** | Re-parsing every surviving log shows the boot **before** the two 20:17/20:22 deaths used the *same* configuration (MTP k=3 + `GRAPHMODE=piecewise`, i.e. `cudagraph_mode 1` + 20-entry `splitting_ops`) with a **cold** compile cache (2 compile lines, 0 AOT loads, 18 capture lines) and **served ~49 min clean**: `Mean acceptance length 2.40 → 3.67`, 0 errors, **median 34.5 t/s** over 117 busy samples (max 98.8). It was restarted at 20:17 and *that* boot died | **the MTP+piecewise+PLE-patched configuration is correct and fast when it boots** — the blocker is boot reliability only, which is why 14.10 added boot retries. Corrects the 20:17/20:22 row's implicit "piecewise + MTP never comes up": it came up minutes earlier |
| 2026-09-20 21:00 → measured | 0-3 | SW numbers correction | Median decode throughput over real traffic (`Running >= 1` samples only) per boot: **eager + MTP k3 = 15.9 t/s** (911 samples, max 41.1) · **graphs + no speculation = 22.0** (89, max 22.8) · **graphs + MTP = 34.5** (117, max 98.8) · the 09-19 23:49 FULL_AND_PIECEWISE+MTP boot logged 45.0-45.1. So the 20.7-20.9 quoted in the 20:59 row was a peak sample and overstated the eager workaround by a third; **the workaround costs 2.2x**, and eager+MTP (15.9) is *slower* than graphs with no speculation at all (22.0) — the drafter's eager k=3 loop does not pay for itself here | **use these as the baseline figures.** Still not a controlled benchmark (traffic-driven); the fixed-probe A/B is still owed. Report §14.9 |
| 2026-09-20 late (log audit) | 0-3 | SW statistics corrected + 4 refuted theories | All five logs re-parsed and de-duplicated (`vllm.log-error2` and `vllm.log.error3` share five boots, which is why earlier denominators were wrong). **MTP + graphs: 11 boots, 3 served, 8 died at boot. Every other configuration: 5 boots, 5 served, 0 died** — separation is by configuration only. Refuted on the full set, each with the number that kills it: (a) "the `Directly load AOT compilation` path is the trigger" — 18 boots took it, 10 served / 6 crashed, and the compile path crashed too (06:26, 14:30); (b) "AOT artifacts stale vs torch" — all 17 cache dirs post-date the 06:44 torch install; (c) "a crash mid-export leaves a truncated artifact that poisons later boots" — smallest artifact 580 KB, none zero-length, and the crashing config's 172 artifacts were complete at 19:32, before the deaths; (d) upstream **vllm#56521** (ROCm 7.2 profiler queue-interposition heap overflow) — `hsa_amd_queue_intercept_create` is **absent from our libhsa** (`nm -D` and `readelf -sW` both empty), kineto not installed, not a container, variable unset everywhere | **the crash site moves with cache state** (in `determine_available_memory` at 20:18/20:23, after 2 capture lines at 06:38/07:00, after a fresh compile at 06:26/14:30) = heap corruption, not a bad step at a fixed point. Local diagnostics exhausted (tvm_ffi owns SIGSEGV, no gdb/coredumpctl) → next informational step is a `PYTHONMALLOC=debug` boot. Still open upstream: **apache-tvm-ffi 0.1.13 PRs #644/#593/#529** fix the exact DLPack cache-key staleness and object-lifetime class we are seeing, and we run 0.1.10. Also note **vllm#53366**: the compile-cache key omits `num_speculative_tokens`, so changing k without clearing `torch_compile_cache/` benchmarks two configs at once. Report §14.7-§14.8 |
| 2026-09-20 late (no restart) | 0-3 | SW **new knob: `BOOT_TRIES`** | Because the failure is boot-time-only (an engine that reaches `Application startup complete` has served 5h30m+), `run-vllm` can now relaunch a boot that dies *before* it ever serves: `BOOT_TRIES=8 ./run-vllm`. Default `BOOT_TRIES=1` = the old single foreground launch, no polling. At 3/11: ~3.7 attempts ≈ **17 min to a fast boot** (≈4 min each + 45 s between attempts so the crashed workers release their ~21.9 GiB/GPU), 8 attempts fail all eight ≈ 8% — against the 2.2x of §14.9 | **mitigation, not a fix** — the corruptor is still unidentified. Three properties tested on bash 5.2.21 with a stubbed launch: never restarts a server that was serving (after the health endpoint answers it leaves the retry branch for good); refuses to start at all while :9000 already answers, instead of failing into the loop and looking like eight crashed boots; and liveness uses `jobs -rp` because `$!` of a backgrounded *pipeline* is tee's PID and `kill -0` succeeds on an unreaped dead child — hence `> >(tee -a vllm.log) 2>&1` instead of `2>&1 \| tee` |
| 2026-09-20 late (no restart) | 0-3 | SW **finding: what `TVMFFI=disable` actually prevents** | torch `2.11.0a0+git70d99e9` defines `torch.Tensor.__dlpack_c_exchange_api__` **itself**. `_check_and_update_dlpack_c_exchange_api()` honours that and tvm_ffi returns without touching anything on non-ROCm builds (`_optional_torch_c_dlpack.py:98`); only the ROCm branch (`prefer_rocm_override`, `:97-98`) presses on and overwrites the attribute with the JIT addon's table. Forcing that branch in-process without a GPU context (patch `torch.cuda.is_available` -> True; the code only calls it) makes the swap visible: native `0x7ff25f580c20` (inside libtorch) -> `0x7ff25cb50800` (inside the filename-cached, ten-hour-stale `libtorch_c_dlpack_addon_torch211-rocm.so`), `CHANGED: True`. The prebuilt wheel cannot be the fallback: `torch_c_dlpack_ext 0.1.5` ships addons for torch 2.4-2.9 cpu/cuda **only** -- no rocm variant, nothing for 2.11 -- so the ROCm path is JIT by construction | **`TVMFFI=disable` is not a superstition: it declines a downgrade of torch's own implementation with a cached one of unverifiable provenance, at zero measured cost. Still NOT proven to be the segfault fix** -- the installed table is not visibly ABI-incompatible (header qword `0x300000001` matches torch's, same 5 function-pointer slots) and 10,000 `to_dlpack`/`from_dlpack` round-trips driven *through the replaced API* with `gc.collect()` per round did not crash. Third failed local repro; a CPU probe cannot reach the graph/AOT paths, and the eager workers map the addon 5x each without ever crashing. Only a real boot can decide; the next arm if `PYTHONMALLOC=debug` implicates tvm_ffi is `apache-tvm-ffi 0.1.10 -> 0.1.14.post0` (vLLM pins `==0.1.10` in `requirements/rocm.txt:27` "matching tilelang version", but tilelang 0.1.10 only asks `>=0.1.10,~=0.1.0`, so 0.1.14 satisfies *it*; tilelang is not mapped in any live process -- it is `mhc`/`deepseek_v4`-only -- and PyPI is reachable). Report §14.11 |
| 2026-09-20 late (no restart) | 0-3 | SW **most actionable finding of the investigation: the V2 model runner has no GC guard, and both boot-time SIGSEGVs die in its unguarded traversals** | Second independent upstream sweep: **nobody reports this stack** (`deduce_unreachable`, `update_refs` segfault, `gc.freeze` + cuda graph -> 0 hits); CPython guidance says a fault there means a C extension already corrupted the heap -- GC is the victim, not the bug (bpo-31181, cpython#135115). Crash-site provenance: vllm PR#24128 added the post-capture `gc.collect()`, PR#50192 added `gc.disable()` around capture (Triton `CompiledKernel` finalizing mid-capture). Checked the sweep's 8 proposed knobs against this install -- `VLLM_ENABLE_CUDAGRAPH_GC` gates `_freeze_gc()` in the **V1** runner only (`v1/worker/gpu_model_runner.py:6622`, applied `:6808`/`:6959`) and V1 never loads here (every boot logs `Using V2 Model Runner`); `VLLM_USE_V2_MODEL_RUNNER` is already the only runner; `VLLM_DISABLE_SHARED_MTP`/`VLLM_COMPILE_CACHE_HOME`/`VLLM_DISABLE_CUSTOM_ALL_REDUCE`/`VLLM_DISABLE_CUDAGRAPH_GC` **do not exist** (0 files each); `VLLM_GC_DEBUG` does exist (`envs.py:305`,`:2053` -> `utils/gc_utils.py` `gc.callbacks` hook, logs collect times + top object types) as instrumentation only; `TORCHINDUCTOR_FORCE_DISABLE_CACHES` is a **torch** var (`torch/compiler/config.py:101`) with 0 vllm reads. The sweep also independently confirms there is no eager-drafter-with-compiled-target flag (MTP drafters carry `@support_torch_compile`, no opt-out) -- i.e. `drafter-graphs-off.patch` was necessary, not cosmetic | **`gc-freeze-capture.patch` ports V1's `_freeze_gc()` to the V2 runner: `profile_run():897` (the `determine_available_memory()` path -- the 20:17/20:22 deaths) and `capture_model():933` (the 19:44 death) are the two unguarded traversals. `_gc_freeze_around` decorator + `_gc_maybe_collect()`, env-gated by `GFX906_GC_FREEZE` (run-vllm `GC_FREEZE=1`, **default 0 = behaviourally upstream**: decorator pass-through, collect is a plain `gc.collect()`). Applied to repo + site-packages, backups `.pre-gc-freeze.bak`, `py_compile` clean in both trees, artifact re-applies to a pristine copy with no offsets and reproduces the live file byte-identically, gate logic unit-tested (OFF collects, ON freezes/disables inside + single collect on exit + depth unwinds + GC state restored). When ON it *suppresses* the inner collect deliberately -- that traversal is the frame that dies -- and biases the KV memory estimate LOW (safe). **OFF by default on purpose**: `DRAFTER_GRAPHS=0` and `TVMFFI=disable` are already changed since the last boot, and at a 3-in-11 base rate a third simultaneous change makes success unattributable. Removes the victim frame, does NOT repair the corruptor. Report §15/§15.1 (arm order 1-5 there)** |
| 2026-09-20 late (no restart) | 0-3 | SW **tvm_ffi: importer identified, and `TVMFFI=disable` is a backport of an upstream bug fix** | Third sweep's claims verified against artifacts, not changelogs. (1) **Importer is xgrammar, not inductor**: `torch/` has **zero** references to `tvm_ffi`; `xgrammar/base.py:1,7` imports `from tvm_ffi import Object`; xgrammar 0.2.7 is mapped **5x in every live worker** next to `libtvm_ffi.so` **10x** -- which is exactly why "tvm_ffi is mapped" could never be the differentiator (it is in the eager boots too, 16.1). (2) **PR #585 confirmed from the downloaded 0.1.12 wheel**: 0.1.10 computes `prefer_rocm_override = bool(cuda.is_available() and torch.version.hip is not None)` and *skips* the early return when true; **0.1.12 has no `prefer_rocm_override` anywhere** and returns unconditionally as soon as `hasattr(torch.Tensor, "__dlpack_c_exchange_api__")` -- i.e. it never even `dlopen`s the addon on our torch (16.2) | **Upstream classified the ROCm override as a bug and fixed it in tvm-ffi 0.1.12, so `TVM_FFI_DISABLE_TORCH_C_DLPACK=1` is a functional backport of a fix, not a lost feature** -- still unproven as *this* segfault's cause. Upgrade target if ever needed: **0.1.12, not 0.1.14.post0** (has #585, predates tvm-ffi#697: 0.1.13 segfaults in `TVMFFIEnvRegisterCAPI` *via xgrammar*, the very component loaded here); `xgrammar>=0.1.10` unbounded and `tilelang >=0.1.10,~=0.1.0` both permit it, only `vllm` pins `==0.1.10`; wheel cached at `/tmp/tvmffi012/`. (3) `use_aot_compile()` defaults to **"1" for torch>=2.10** (`envs.py:381-390`) so the second-artifact load path is on by default here (16.3); `VLLM_FORCE_AOT_LOAD` exists (`:133`,`:794`). (4) **#56521 finally ruled out with the mistake it exposed**: `librocprofiler-sdk.so.1.1.0` *is* mapped 5x and *does* dlsym `hsa_amd_queue_intercept_create`, but our libhsa `1.18.70200` exports **zero** intercept symbols (kineto 0x) so the shim resolves NULL -- however the *first* "absent" result was produced by probing `libhsa-runtime64.so.1.18.0`, a filename that does not exist, and `strings` on a missing file prints nothing and looks exactly like a negative. Corrected probe agrees, conclusion stands; same class as the 2026-09-19 §9 false positive: **a test that cannot fail is not a test.** Also corrected: `SpeculativeConfig.enforce_eager` has **three** V1-side read sites (adding `gpu_model_runner.py:6261`), not two -- still zero on the V2 path. Closest upstream behavioural match: **vllm#48234** (spec-decode + inductor + PIECEWISE graphs SIGSEGV, eager-safe), no GC frames. Report 16.1-16.5 |
| 2026-09-21 06:02 | 0-3 | SW **RESOLVED (tentative): the fast config is live** -- MTP k=3 + PIECEWISE graphs booted on the first try with both staged mitigations active | Operator restart at 06:02:43 (pid 1088823, engine 1089228). Boot env from `/proc/1088823/environ`: `GFX906_DRAFTER_GRAPHS=0`, `TVM_FFI_DISABLE_TORCH_C_DLPACK=1`, `GFX906_GC_FREEZE=0`. Config: `enforce_eager=False`, `mode=VLLM_COMPILE:3`, `cudagraph_mode=PIECEWISE:1`, `cudagraph_capture_sizes=[4,8,12]`, both PLE ops in `splitting_ops`, MTP `num_spec_tokens=3`, 147,456 ctx. Target captured `Capturing CUDA graphs (PIECEWISE): 100% 3/3`, `Graph capturing finished in 8 secs, took 0.32 GiB` per worker, no FULL capture, no capture-unsupported error, **0 error/segfault/Traceback lines in the boot**, KV cache 435,925 tokens. Drafter logged `speculator.py:151 Capturing model for speculator...` (unconditional message) but produced **no second capture ladder and only 0.32 GiB of graph pool** -- consistent with the drafter-graphs patch taking effect | **Serving decode at 45.9-71.0 t/s with `Running: 2 reqs`** (vs 15.9 median eager, 34.5 median for the 09-20 19:28 lucky boot). `BOOT_TRIES` was not needed -- no attempt markers in the log, default 1, first attempt served. **This is 1-for-1 under the mitigations vs 3-for-11 under upstream behaviour, which is NOT yet evidence of attribution** (p=0.27 for a single chance success at the old base rate); three consecutive successes would be (0.27^3 = 0.02). Note the same config died twice without the mitigations on 09-20 evening (20:17, and 20:23 with four separate `Segfault encountered` reports before the 20:26:41 worker death). If it stays up, the knobs stay on regardless of attribution -- the question only matters for whether they can ever be turned off. Report §14.10, §14.11, §16.2 |
| 2026-09-21 20:33 / 20:47 / 20:54 / 20:55 | 0-3 | SW **`GC_FREEZE=1` shown insufficient + the retry loop was consuming its own attempts on leaked VRAM** | Operator boot of the fast config with `DRAFTER_GRAPHS=1 GC_FREEZE=1 BOOT_TRIES=8`. Both real attempts (20:33:42 pid 1349741, 20:47:38 pid 1356562) logged `GFX906_GC_FREEZE: froze and disabled GC around profile_run` on **all four workers** and still died with `!!!!!!! Segfault encountered !!!!!!!` inside an explicit `gc.collect()` (`gc_collect_impl` -> `gc_collect_main` -> `deduce_unreachable` -> `subtract_refs` -> `tupletraverse` -> `_PyObject_IS_GC`), `Worker proc VllmWorker-0 died unexpectedly (exit code: None)`, at 20:39:27 and 20:52:48. Last lines before each: the `eagle_head` AOT artifact load + `Initial profiling/warmup run took 1.02 s`. The *next two* attempts never reached the engine: `ValueError: Free memory on device cuda:3 (10.17/31.98 GiB) on startup is less than desired GPU memory utilization (0.95)` from `request_memory()` (`v1/worker/utils.py:526`) -- the segfaulted workers still held ~21.8 GiB/GPU 2 and 4 minutes later, and the fixed `sleep 45` + a preflight that only probes :9000 let the loop retry into that. Attempt 4 at 20:59 with defaults (`GFX906_DRAFTER_GRAPHS=0 GFX906_GC_FREEZE=0`, read from `/proc/<pid>/environ`) came up and is serving | **not a wedge** -- `dmesg` shows no amdgpu reset in the window (only the 09-19 08:10 host OOM kills), GPUs healthy, 33.7/34.3 GiB in use by the live server now. Three conclusions: (1) `gc.disable()` suppresses *automatic* collections only and `_gc_maybe_collect()` covers only vLLM's own call site, so a third-party `gc.collect()` still traverses the corrupted heap -- `gc.freeze()` exempts only objects reachable at freeze time, and everything the compiled drafter allocates afterwards is still walked; (2) the fault is **pre-capture** (both deaths precede any `Capturing CUDA graphs`), so `DRAFTER_GRAPHS=1` adds no boot risk of its own -- it is the same ~2/11 coin every MTP+graphs boot pays; (3) retries must be gated on measured VRAM, not on a clock. Fixed both: the guard now also shadows the `gc.collect` attribute for the region (29 attribute-style call sites under `torch/`, 0 `from gc import collect` bindings in `torch/`+`vllm/`, so it reaches all of them; 18-assertion harness: OFF untouched / ON shadowed inside / single real collect on outermost exit / nesting / exception-path restore / enabled-state restore), and `run-vllm` gained `wait_vram_released()` + a `VLLM::` reaper between attempts (`VRAM_GATE_TIMEOUT`, `VRAM_GATE_MAX_USED`), with the first attempt gated but never reaped. 4-case stubbed-launch harness: 3 attempts + exactly 2 reaps + correct rc, serving state still exits the retry branch permanently, `BOOT_TRIES=1` byte-identical to the old single launch, preflight refusal launches nothing. Details: `/ai/engines/gfx906/unverbraucht/decode-launch-bound.md` §6 |
| 2026-09-21 21:54 / 22:01 / 22:07 | 0-3 | SW **the GC-freeze guard is implicated: with it enabled the crash stopped being a coin flip and moved into the guard's own exit collection** | Three MTP boots with `GC_FREEZE=1` and the shadowed `gc.collect()` (21:54:45 pid 1380554, 22:01:39 pid 1384110, 22:07:33/22:07:52 -- two engines launched 19 s apart, which is what produced the `Free memory on device cuda:N (10.12/31.98 GiB)` errors at 22:09:02, not the GC bug). All four workers logged `froze, disabled and shadowed gc.collect() around profile_run` in every boot; `GC restored` -- which is logged immediately AFTER the guard's exit collection -- appears **zero times in vllm.log, vllm.log2 and vllm.log.error4**, i.e. 16 region entries and no exit ever. Each death is the same stack: `builtin_next` -> `gen_iternext` -> `gen_send_ex2` -> <python frame> -> `gc_collect` -> `gc_collect_main` -> `deduce_unreachable` -> `update_refs`. `builtin_next` resuming a generator is how `contextlib.contextmanager.__exit__` finishes a generator, so the frame that dies is `_gc_freeze_guard`'s own restoring `gc.collect()` at the end of `profile_run`. The discriminator is clean: in `vllm.log.error3`, whose crashes predate the guard, `gc_collect_main` appears 8 times and `builtin_next` **0** -- the call path changed exactly when the guard was enabled. Warm vs cold AOT cache made no difference (21:54 logged `Source code has changed since the last compilation` and recompiled; 22:01 and 22:07 loaded AOT directly), so this is not the compile-cache theory. The 21:53:07 boot that the operator believes was "plain ./run-vllm crashing" never started an engine at all: it died at 21:54 with `raise KeyboardInterrupt("terminated")` from `_interrupt_in...` while still in `registry.inspect_model_cls`, and the only plain boot that did reach the engine today (20:59) came up and served | Not a wedge: no amdgpu reset in `dmesg`. Mechanism: upstream `_freeze_gc` (v1/worker/gpu_model_runner.py:6619) can afford `gc.unfreeze(); gc.collect()` on exit because it wraps CUDA graph capture, seconds; this guard was pointed at `profile_run`, five minutes of Dynamo/Inductor/xgrammar allocation churn, so freeze-at-entry + no collection + unfreeze-and-collect-at-exit manufactures exactly one enormous full-heap walk and runs it at the moment the heap is largest. Fixed by making the exit traversal opt-in: default now re-freezes on the way out, which parks the region's objects in the permanent generation that every later collection ignores, so the traversal that dies never runs (cost: boot-time garbage is never reclaimed -- bounded, one-time, host RAM). `GFX906_GC_THAW=1` restores upstream's exit path; `GFX906_GC_DUMP=1` re-arms `faulthandler` inside the regions, because the 499 frames across the segfault dumps in vllm.log contain **zero** `File "*.py"` entries -- tvm_ffi's handler prints C frames only, which is why the Python frame holding the corrupted object has still never been named. 27-assertion harness on the extracted guard (park vs thaw call order, nesting, exception path, enable-state restore, region objects untracked after parking). Spec-free boot at 22:14:28 (pid 1390634, `GFX906_GC_FREEZE=0`, capture sizes [1,2,3]) is serving at 22.5 tok/s -- above eager+MTP's 15.9 from section 14.9, so it is the best reliable config on this box tonight |
| 2026-09-21 23:09 | 0-3 | SW **RESOLVED: park-at-exit boots the fast config on the first try, and it is faster than anything measured on this box today** | Operator boot `GC_FREEZE=1 GC_DUMP=1 BOOT_TRIES=3 ./run-vllm` (pid 1405571 wrapper, APIServer 1405667, EngineCore 1406133, workers 1406432/1406549/1406684/1406820; `GFX906_GC_FREEZE=1 GFX906_GC_THAW=0 GFX906_DRAFTER_GRAPHS=0` read from `/proc/1405667/environ` -- the renamed `VLLM::` procs cannot be read this way because setproctitle clobbers the environ page). Engine init 23:09:38, `Directly load AOT compilation` (warm cache, `torch.compile took 7.07 s`), `left frozen, nothing traversed after profile_run (12.0 s frozen)` x4 at 23:15:18 -- **the exit that has never printed in this box's history: 16 guarded region entries across vllm.log/.log2/.error4 and 0 exits, 5 of 5 guarded boots dead at exactly this call** -- then `froze ... around capture_model` -> `Graph capturing finished in 7 secs, took 0.32 GiB` -> `left frozen ... after capture_model (8.1/7.3 s)` x4, `GPU KV cache size: 408,725 tokens, Maximum concurrency for 147,456 tokens per request: 2.77x`, startup complete 23:15:34. **Zero segfaults in the boot.** Fixed-probe decode after 4 min of serving: **B=1 median 46.80 tok/s** (43.80/46.80/47.15/47.12/46.59), **B=2 73.69 tok/s aggregate**, **B=3 100.24 tok/s aggregate**, degeneration smoke test OK (4/4 distinct lines). Mean acceptance length 2.30-3.15, per-position 0.66-0.81 / 0.41-0.70 / 0.23-0.64 -- unchanged from the 0.62/0.48/0.36 baseline, so nothing is being won by degrading drafts | Same config (`GFX906_DRAFTER_GRAPHS=0`) measured **36.9 / 66.8 / 54.0** tok/s at B=1/2/3 earlier today with `GFX906_GC_FREEZE=0`: +27 % at B=1, +10 % at B=2, and the B=3 collapse (54.0) is **gone** (100.24, +86 %). Two things landed in one boot and they are confounded: (1) park-at-exit, which is why it booted; (2) the freeze being active at all during serving, which is the likelier source of the speedup -- with the whole compiled world parked in the permanent generation, automatic collections during decode walk only newly-allocated young objects instead of re-traversing every graph artifact, on a decode loop already measured at 0.919 cores of Python dispatch. That is a testable hypothesis and the A/B is one boot: same config with `GC_FREEZE=0`. Caveats kept honest: single measurement, self-confounded box (the agent's own traffic is the traffic), warm AOT cache (so this boot exercised a 12 s freeze, not the 5-minute cold-compile case -- the cold path through the parked exit is still unexercised), and `GFX906_GC_DUMP=1` armed nothing because nothing crashed. Baseline for the leak the parked heap can cause: worker VmRSS 24.8 GiB each, 50 Gi host available at 23:25 -- recheck in a few hours. 21 `Triton kernel JIT compilation during inference` warnings (`_causal_conv1d_fwd_kernel`, `_triton_mrope_forward`) confirm the warmup-coverage gap in section 4 Tier 3 |
| 2026-09-21 23:31 | 0-3 | SW **`DRAFTER_GRAPHS=1` also boots clean under the parked guard -- and it is worth only +6 % at B=1, 0 % at B=3, which refutes section 14.9's "+33 % from drafter graphs"** | `setsid env DRAFTER_GRAPHS=1 GC_FREEZE=1 BOOT_TRIES=3 ./run-vllm`: wrapper 1412945, APIServer 1413042, EngineCore 1413508, workers 1413797/1413914/1414049/1414182. `/proc/1413042/environ`: `GFX906_DRAFTER_GRAPHS=1 GFX906_GC_FREEZE=1 GFX906_GC_THAW=0 GFX906_GC_DUMP=0`. Engine init 23:31:32 -> `Capturing model for speculator...` x4 and `Graph capturing finished in 8 secs, took 0.39 GiB` x4 at 23:37:31 -> `GPU KV cache size: 399,419 tokens` -> startup complete. **Zero segfaults** (0 `Segfault|died unexpectedly` lines after 23:30), and **12 of 12 frozen-region exits printed** (profile_run 12.2 s, capture_model 9.0 s target, 7.8 s speculator, each x4). Fixed-probe decode: **B=1 median 49.49** (48.59-49.73), **B=2 75.52 and 78.30** aggregate on repeat runs, **B=3 100.11 and 101.25**; smoke test OK 4/4 distinct; acceptance mean length 2.43-3.01 | Three boots tonight, one variable each, same box: `GC_FREEZE=0` (drafter graphs off) **36.9 / 66.8 / 54.0**, `GC_FREEZE=1` drafter-off **46.80 / 73.69 / 100.24**, `GC_FREEZE=1` drafter-on **49.49 / 78.30 / 101.25** at B=1/2/3 tok/s. So the drafter-graph step -- the one section 14.9 priced at 34.5 -> 45.8 (+33 %) -- is really **+5.8 % at B=1, +2.5 to +6.3 % at B=2, and +1.0 % (noise) at B=3**. The 34.5 -> 45.8 jump tracks the GC state, not the graphs; section 14.9 attributed to the graphs a gain that belongs to the freeze. Cost of the drafter graphs is measured, not guessed: KV cache 408,725 -> 399,419 tokens (-9,306, -2.3 %), max concurrency at 147,456/req 2.77x -> 2.71x | The B=3 per-session rates from the drafter-off and drafter-on boots agree to 0.05 tok/s (34.94/34.16/33.42 vs 34.95/34.14/33.39). Two different launcher configs cannot produce identical per-session rates unless B=3 is not launch-bound at all: at 3 streams the box is step/GPU-bound at ~100 tok/s aggregate and immune to every host-side lever in section 2, so "decode is launch-bound" is true at B=1-2 only and the B=3 wall is elsewhere (RCCL spin + 96 all-reduces is the standing suspect). Worth keeping: +6 % single-stream latency for 2.3 % KV, no boot penalty observed. `fused_moe_kernel` JIT-compiled during inference at 23:39:54 mid-probe (21 such warnings and counting) |
| 2026-09-22 00:20 (no restart) | 0-3 | SW **the joe2gaan "fullbar p2p" amdgpu patch evaluated and rejected: 3 of its 4 hunks are already in our driver, the 4th is inert by config -- and the thing that actually blocks P2P here is ACS redirect, which the patch does not touch** | Asked whether `patches/gfx906-amdgpu-fullbar-p2p-reconstructed-20260619.patch` from `github.com/joe2gaan/localaiservers` can be integrated. SHA-256 `7e08477b0160c0487d0fd24746ee26a577d70990edd537ae45a916b76d4ef4a1` verified against the hash their own doc publishes. It is a **host kernel driver** patch (`amdgpu.h`, `amdgpu_drv.c`, `amdgpu_device.c`, `amdgpu_amdkfd_gpuvm.c`), not vLLM code, and their doc self-describes it as "reconstructed ... the original standalone patch file has not been recovered ... do not apply to rocm-7.2.1 without checking whether the relevant hunks are already present". Checked against our stack (kernel 6.8.0-137-generic, DKMS `amdgpu.ko.zst` **version 6.16.13**, ROCm 7.2.0, torch 2.11.0a0+git70d99e9 / hip 7.2.26015): (a) `/sys/module/amdgpu/parameters/pcie_p2p` -> **`Y`**, so the module param it adds is already there; (b) `readelf -sW amdgpu.ko` finds **`reuse_dmamap`** (FUNC LOCAL, 136 B) -- the GTT/userptr DMA-map reuse it "adds" is upstream; (c) `/boot/config-6.8.0-137-generic` has **`CONFIG_HSA_AMD_P2P=y`**, `PCI_P2PDMA=y`, `DMA_SHARED_BUFFER=y`, so the `#ifdef` branch its fallback sits outside is compiled in and unreachable, and the hunk additionally calls `amdgpu_device_check_iommu_remap()`, **a symbol absent from our module** -> it would not even compile; (d) its large-BAR precondition is already met: every GPU exposes a **32,768 MiB prefetchable 64-bit BAR** (`0x1b800000000..` on 23:00.0, also 0x1a8/0x178/0x168 ranges) and the kernel log already carries `added peer-to-peer DMA memory 0x17800000000-0x17fffffffff` x4 | **Why it is a no-op here regardless:** `ls -d /sys/bus/pci/devices/*/p2p` -> **0 providers published**, while `ACSCtl: SrcValid+ ReqRedir+ CmpltRedir+ UpstreamFwd+` on all four switch downstream ports (`22/25/42/45.0`, AMD/ATI device **14a1**, LnkCap 16GT/s x16). The P2P resource registers but no pair passes `pci_p2pdma_distance()`, and in the patched function `p2p_access` is *still* `!(pci_p2pdma_distance(...) < 0)` -> same `false`, chain unchanged: no published provider -> `amdgpu_device_is_peer_accessible()` false -> no KFD peer access -> **RCCL has no P2P transport for TP=4**. Topology caps any payoff anyway: `lspci -t` shows each GPU behind its **own** root port + own switch DSP (`+-01.1-[21-23]----[22-23]----[23]`, `+-03.1-[24-26]-...-[26]`, `+-01.1-[41-43]-...-[43]`, `+-03.1-[44-46]-...-[46]`), i.e. **no two GPUs share a switch**, so peer traffic crosses the host bridge either way and clearing ACS removes a software eligibility block, not a physical detour. What *does* apply: `pci=noacs` (our `System.map` has `disable_acs_redir_param` + `pci_dev_specific_disable_acs_redir`), each GPU already alone in its IOMMU group (55/58/36/39), Secure Boot disabled/Setup Mode, toolchain complete -- documented as a plan, **not executed**. Deliberately unmeasured and recorded as unknown: which transport RCCL picks today (`NCCL_DEBUG` unset; the 17 `shm` hits in `vllm.log` are `shm_broadcast.py`, vLLM's CPU-side queue, **not** the collective transport) and `can_device_access_peer`, since free VRAM with the engine up is **27-43 MiB/GPU** (`34,314,145,792 / 34,342,961,152 B` used on card0) and 4 fresh contexts need 450-600 MiB each. Its `qwen36-gfx906/overlays/*/minimal-bundle/` (whole-file `linear.py`/`parallel_state.py`/`communication_op.py` replacements + a 57 KB **source-less** `libgfx906_sidecar_tree_ll_ar_multirow_*.so`) is the part that is real integration work, and it is pinned to *their* commit `v0.1.dev21309+g113ca61c6`, would clobber our edits in the same files, and would not even cover our traffic -- our ARs come from `models/qwen4_exp/amd/qsa.py` and `moe_runner.py:453/473` via `tensor_model_parallel_all_reduce`. Writes: `p2p-fullbar-analysis.md`, `p2p-noacs-test-plan.md` (draft, gated on a reboot window), `bench-ar-latency.py` (4-rank AR instrument, guards verified: refuses while a live engine is detected and while free VRAM < `--min-free-mib`; `--dry-run` and report rendering tested, **no GPU touched**). No files in the repo changed by this analysis, no restart |
| 2026-09-22 05:14 | 0-3 | SW **whole `gfx906/qsa-fn` branch submitted as a single PR to `KIntegrated/main`: PR #2** | Operator chose the whole branch over a two-PR split. Pre-flight found the range was **not** lint-clean: `pre-commit run --from-ref origin/main --to-ref HEAD` failed 7 hooks (ruff check/format, typos, markdownlint-cli2, shellcheck, SPDX headers, `torch.cuda` call), because `6da646596`/`e2ea2b217` never went through pre-commit. Fixed in one style commit, **vllm/ edits proven formatting-only by AST compare** (`c4_layer0_moe.py`, `ple_layer.py`, `rocm.py` identical; `utils.py` identical but for isort's `functools`/`os` swap) -- note this reflows `triton_matmul_kernel`'s source, so the next boot from a clean tree does one cold JIT compile of it; numerics unaffected. E501 findings left alone: `origin/main` has the same 13+1 over-length lines, fixing them would balloon the diff for no gain. `git rebase --signoff` added DCO trailers to all commits (content diff empty). Merged `main` in: 4 doc files conflicted (CHANGELOG, ROADMAP, MERGE-0.30.0-review, DEVLOG-v2-mamba-align), all resolved to our side with main's cleanly-merged additions kept, and **the merge touched zero code** (`git diff HEAD^1 HEAD -- vllm/ csrc/ tests/ benchmarks/` empty). Evidence shipped in the PR: 203 CPU-runnable tests passed (`--noconftest`: `tblib` absent from the serving venv), the 3 CUDA-context test files were NOT run here because the box was serving with 27 MiB free per GPU and are deferred to CI, and 9 of 13 changed `vllm/**` files are byte-identical to the tree serving this box with the other 4 AST-identical. PR is `MERGEABLE`; title corrected to the fork's required `[Tag][Tag]` form (the `pr-title` workflow rejects `prefix:` style). |
