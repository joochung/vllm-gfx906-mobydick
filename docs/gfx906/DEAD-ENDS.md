# gfx906 dead ends & open verdicts — index

The one-pass answer to "did we try X and did it work?" Every row is backed
by a source log (or, if not yet archived to the new format, the legacy
DEVLOG). Pattern: **hypothesis → gate → verdict → commit/revert → comment**.

- `VERDICT: DEAD-END` / `NEUTRAL` = tried, doesn't help (kept for the
  negative evidence).
- `VERDICT: SHIPPED` = landed, helps (indexed here only when the *cost*
  or *reverse* matters — the positive record lives in its topic log).
- Rows look like: `y/N hypthesis → gate → verdict → commit → refs` so one
  line per line is grep-able and sortable.

## DEAD-ENDS / REJECTED

| Hyp | Gate | Verdict | Commit/revert | Comment | Refs |
|-----|------|---------|---------------|---------|------|
| gemm1 V1 full-K single-wave direct store | standalone harness | **REJECTED** (2.2× slower; N-split 128/256/512 blocks all slower than v1b's 59.0 µs, best 74.8) | — | Kills atomics but loses all transfer. N-split axis closed 2026-08-31 (C2 finish): every block count ≥2.1× off current; adding blocks to shorten the per-block stream buys nothing (v1a-vs-v1b isolates wavefront config ~2×; N-split confounds stream length with wavefront count — mechanism not cleanly isolated). Residual family cost: per-block full-K loop caps memory-level parallelism. **FULLY DEAD** (DE-1: structural; not registers) — `DEVLOG-moe-c2v.md` | G1 |
| gemm1 NPT=2 (z-split best point) | serving graph, +0.08 t/s | **DEAD-END** (neutral) | `reverted`, flag `VLLM_GFX906_MOE_G1NPT2` removed | census said −186 µs/step; wall-clock neutral in *both* eager & graph — 3rd consecutive transfer failure. **FULLY DEAD** (DE-1: isolated→serving flip; re-tile family 79 VGPR, 0 spills) | G1 |
| gemm1 V3 fp32-scratch K-split | — (closed w/o build) | **DEAD-END** | — | adds ~184 µs/step scratch+launch on top of a design whose best point doesn't transfer. **FULLY DEAD** (DE-1: structural; never built) | G1 |
| gemm1 V4 halve CAS fan-in (NPT=8/z=32) | launch sweep | **DEAD-END** | — | CAS fan-in axis is monotone-worse as count rises (27.8→33.4 z=16→32). **FULLY DEAD** (DE-1: structural; 0 spills across the sweep) | G1 |
| S5 gemm2 V2 M=1 re-tile (lane-based cols) | standalone 1.18×; C2 combined serving A/B | **SHIPPED default-ON** (2026-08-31, C2: +2.72 % M=1 decode, shape-gated) — row was stale: the kernel was renamed `VLLM_GFX906_MOE_M2V2` → `VLLM_GFX906_MOE_M1` and promoted to default-ON in the C2 combined A/B (`da2e9ec3a7`→`461c729420`) | live in `moe_q_gemm_gfx906.cu`; `VLLM_GFX906_MOE_M1=0` is the kill switch | The original "NEUTRAL in model" verdict was for the standalone-gate era; the C2 combined A/B (with gemm1 changes) showed a real +2.72 % and it shipped on | M1, DEVLOG-moe-c2v.md |
| S2 dedicated M=1 topk kernel | in-model | **NEUTRAL** | **REMOVED from tree 2026-09-01** (maintainability sweep; code preserved on branch `gfx906/preserve-dead-kernels`) | mode-dependent; standalone win, graph-replay loss. **FULLY DEAD** (DE-1: isolated→serving flip; 37 VGPR, 0 spills). Removed: `moe_topk_gfx906.cu`, bindings, `_custom_ops` wrapper, dispatch in `fused_topk_router.py`, tests, bench | M1 |
| P2-0b zero-fill launch elimination | design | **DESCOPED** (racy) | — | design is racy, not a kernel dead-end | M0 |
| fused fp16·fp32 casts / h2d micro-copies | — | attributed as upstream (non-blocking) | — | — | M0 |
| dense W4A16 purpose-built GEMV | serving | **REJECTED** | prototype in /tmp (not in-tree) | exllama gptq_gemm beats it 187–230% floor vs its 87–97%; no benefit. **FULLY DEAD** (DE-1: load-layout class, not registers; prototype lost from /tmp) | D |
| dense K=5120 GEMV / lm_head GEMV | serving | **NEUTRAL** | — | LLMM1 already at HBM floor (3114 vs 3128 µs); the "lm_head GEMV lever" does not exist. **FULLY DEAD** (DE-1: HBM floor; family recompiled spill-free) | D |
| P3-2(b) K-split GEMV hypothesis (kc=512 splits) | micro-bench | **REJECTED** (wrong) | — | kc=512 2.4–4.2× slower than LLMM1; M=1 is latency/launch-bound, not occupancy. **FULLY DEAD** (DE-1: occupancy ruled out; 0 spills) | FA/D |
| GEMV V2 (RPT=2+kc=4096) for skinny dense | micro-bench | **NEUTRAL** | kept per-shape | qkv/router win, N=1024 pathological (−533%), based on shape rule | D |
| llama.cpp Q5_K_XL baseline | — | **IMPOSSIBLE** (36 GiB > 32 GB) | — | won't fit; used Q4_K_XL instead (70.3 t/s ref) | M0 |
| FA V2 fused gather (416 WG, barriers) in serving | serving graph | **REJECTED** (V1 wins) | `GFX906_FA_GATHER_V=2` | V2 degrades 7× in serving (285 µs) vs V1 42 — wave-scheduling/low-WG effect. **FULLY DEAD** (DE-1: grid-shape/scheduler, NOT registers — V2 16 VGPR vs V1 12, both 0 spills) | FA |
| salvage LEGACY=0 via a better dot instruction (dp4a/dot2/dot8 swap in the KQ loop) | ISA rate probe + roofline (analytic) | **DEAD-END** (analysis) | — | dp4a/`v_dot4_i32_i8` already the inner loop and measured FULL-RATE (4.44× fp32, 2× packed fp16); B=1 decode is gather-HBM-bound ~2.7× so ALU swaps can't surface; expansion composites 0.17–0.24×. Q4/dot8 = format change, roadmap M6(c). **FULLY DEAD** (DE-1: ALU already full-rate; B=1 gather-HBM-bound — analysis closed) | FA |
| M6 Part A planar Q8 quants/scale repack closes the LEGACY=0 B=1 gap (flip re-open) | microbench hard stop-rule (ISA loader loads ≥2× AND step ≥2 %) | **DEAD-END** (flip question); code **NEUTRAL** — merged as loader hygiene 2026-08-29 (`02d197189f`), flip-question verdict unchanged | same-boot B=1 A/B PASS: slope 36.0→34.4 ns/token (−4.3/−4.8 %), @Sk=2176 83.6→79.1 us, bit-identical (maxerr equal at every Sk), merged suite 74/74 — **contended-boot caveat (post-merge review): same merged .so = 42.0 us @Sk=2048 / 12.86 ns/tok idle; delta directionally supported, merge not perf-dependent** | ISA-verified loader 10→6 loads/tile-row = 1.67× < 2× (plan's ~17/block assumption was wrong — compiler already 4×8-B + 2-B); standalone B=1 step −2.4 % (disjoint bands) both LEGACY modes; bit-identical (64/64). B=1 gap is elsewhere (write path / Q-side / gather traffic) | MG, plan_fa_part_A.md |
| LEGACY=0 B=1 decode gap closes under today's dispatch (Q8-gather) — flip default to LEGACY=0 | same-boot serving A/B (boot O, Qwen3.8-27B TP=2, B=1 pp2048/tg256, 2 samples/arm) | **DEAD-END** (flip question closed 2026-08-29) | branch `feat/fa-legacy0-b1-decode` (unmerged; no code to revert) | A 40.11/40.12 vs B (Q8-gather) 37.61/37.56 (−6.3 %) vs C (direct-paged, M5 era) 37.55/37.54 (−6.4 %) t/s; B≈C in serving despite −36 %/+31 % kernel-level subcomponent deltas → gap is a LEGACY=0-common per-step serving cost, NOT FA/gather (Q8 gather is 22–45 % FASTER per step, growing with Sk); measured append-time Q8 write = +94.6 us/step eager (16 layers) — the ~1.55 ms/step remainder is graph-node/serving-harness interaction (unmeasured, refrigerated: fuse Q8 write into the triton append) | FA-LEG, DEVLOG-fa-legacy0-b1-decode.md |
| C1 stage 2: fused topk+align+count in one 1-CTA kernel (120→40 graph nodes/step) closes the remaining routing cost | A-B-A same-boot serving A/B (boot O, Qwen3.5-35B, B=1 pp2048/tg256, 4 samples/arm) | **DEAD-END** (2026-08-29; stage 1 fused align+count SHIPPED at +1.18–1.73 %) | **REMOVED from tree 2026-09-01** (maintainability sweep; code preserved on branch `gfx906/preserve-dead-kernels`), incl. the orphaned `_fused_align_meta` plumbing it was the only producer of | 3rd instance of the isolated→serving flip (S2 sprint, S2 re-validation, this): 28 % faster per node isolated (10.0 vs 13.8 µs/layer, 40 vs 80 nodes) → −1.03 % (S2 topk swap) / −1.10 % (this) in serving. Mechanism attributed via the stage comparison: REMOVING redundant nodes transfers to serving (stage 1: 207 vs 224 µs/step predicted); REPLACING the proven production topk kernel in place does not. Future topk work must not swap the kernel in place (epilogue-fold designs only). **FULLY DEAD** (DE-1: 3rd isolated→serving flip; fused kernel 38 VGPR / 3 KB LDS, 0 spills) | C1, c1_routing_structural_probe.py |
| C3 Phase A: fold `w1_out.zero_()` into the single-CTA M=1 align kernel (−40 memset nodes/step; stream-ordered, NOT P2-0b's racy in-GEMM clear) | FULL_DECODE_ONLY serving A/B (Qwen3.5-35B, N=1 M=1 pp2048/tg256 TP=1, 3 samples/arm) + fingerprint | **NO-GO** (2026-09-01; wash within noise — on median 85.42 vs off median 85.62 t/s, stdev 0.13/1.54; +0.7% "mean" was one noisy off rep) | branch `gfx906/c3-zeroing-fold` (unmerged; no code on main to revert) | Bit-correct (fingerprint identical; 78/78 unit + 67/67 e2e green) and confirmed firing (~37 MoE layers/step via in-process probe). Root cause: this model's `w1_out` is only **8 KB** (moe_intermediate=512), so ~40 tiny memset nodes ≈ tens of µs/step ≈ 0.3% of a ~15 ms step — below the A/B's ~1.8% noise floor, and the fold adds an 8 KB store to align that offsets it. Node-removal transfer (C1 stage-1 lesson) needs LARGER nodes; this is too small. `output.zero_()` half alias-blocked, not pursued. Reopen only for a larger-moe_intermediate MoE model | C3, DEVLOG-moe-c3-zeroing-fold.md, c3_fold_firing_probe.py |

| A8W8 (int8 activation × int8 weight) prefill GEMM via Triton `tl.dot`, ported as source from the gfx908 int8-vllm fork | standalone Triton GEMM vs same-codegen fp16 and hipBLAS fp16 at 5 production prefill shapes (4096/1024/256 × 34816/14336/5120 × 5120/17408), activation-quant charged | **NO-GO** (2026-08-31 probe; gate was ≥ 1.3×, got 0.59–0.68× of hipBLAS fp16, 1.10–1.18× of Triton fp16) | — (probe only, no engine change; `benchmarks/kernels/gfx906/int8_triton_dot_probe.py`) | dot4 **is** emitted and exact (2048 `v_dot4_i32_i8` vs 4096 `v_dot2_f32_f16` for the same work, maxdiff 0 vs fp64) — but Triton reaches only 19 % of the 25 877 GMAC/s dot4 record vs hipBLAS fp16 at 57 % of its record, so the compiler gap (1.5–1.7×) exceeds dot4's 1.96× ISA edge; and W4A16 already reads half the weight bytes A8W8 would. Reopen only for a hand-written int8 GEMM beating W4A16 head-to-head — and see the next row for why that route is itself scope-capped. Act-quant never the issue (0.13 ms = 0.14 % of the GEMM) | INT8, `/local/tmp/int8-probes/p1-full.log` |
| Hand-written HIP int8 (A8W8) GEMM as the real version of the above (our hot path is HIP, not Triton, so the dot4 1.96× ceiling should be reachable) | free scope arithmetic before any kernel: an int8 GEMM can only touch tensors storable as int8 | **DEAD-END** (2026-08-31, scope-capped — not measured) | — (no code written) | the BF16 mass is only **7.9 % (dense) / 8.9 % (MoE) of per-token GEMM MACs**, so a perfect 1.96× kernel there wins ~3 % wall; int8-ing the int4 mass costs 2× its bytes (+11.4 GB dense / +15.8 GB MoE, TP=2-only, ~178 k tokens of the 445 k KV pool); llama.cpp's dot4-based multi-token GEMM on gfx906 measured only **+21…+51 %** over its own baseline, i.e. ~1.3× in practice. Same tensors ARE the right lever at decode (60.3 % of MoE byte mass) → that is the T1 row below. **FULLY DEAD** (DE-1: scope-capped before any code; not a compiler failure) | INT8 §3.10, DEVLOG-int8-transfer.md |

**Sources:** `M0`=DEVLOG-moe-opt.md · `FA`=DEVLOG-fa-attention.md ·
`D`=DEVLOG-dense-decode.md · `M1`=DEVLOG-moe-m1-sprint.md ·
`G1`=DEVLOG-moe-gemm1-retiling.md · `S`=DEVLOG-spec-decode.md ·
`Q`=DEVLOG-qwen38.md (incident, not a dead-end) ·
`GA`=DEVLOG-gemma4-*.md · `W1`=DEVLOG-gdn-mixed-decode.md ·
`MG`=DEVLOG-muse-glimmer.md ·
`C1`=DEVLOG-moe-c1-routing-fusion.md ·
`INT8`=int8-investigation-qwen.md (gfx908-fork transfer analysis; not a
DEVLOG) + `DEVLOG-int8-transfer.md` (the P1/P2 session record).

| Onboard Qwen3-30B-A3B-AWQ as the next generic AWQ MoE candidate (E=128/topk=8/hidden=2048 may fit the M=1 tile) | — (never started) | **SUPERSEDED** (2026-08-29 user decision: not an active goal — the supported Qwen3.5/3.8 line supersedes the model) | — | candidate removed from the onboarding queue during the roadmap reorg; the generic AWQ queue itself stays open for the next compatible checkpoint | ROADMAP (onboarding queue) |

## OPEN / IN-FLIGHT (verdict not yet recorded)

| Hyp | Gate | Status | Refs |
|-----|------|--------|------|
| gemm1 activation-fusion (fold SiLU·mul into epilogue) | serving wall-clock | low transfer expectation (see §interactions in G1) | G1, roadmap C2 |
| Spec-decode MTP k=2 | serving graph | **SHIPPED** 39.4 t/s (1.41×; 1.50× no-max build), 1.82 tok/step | S |
| Dense W16A16 long-K GEMV (K=17408 down_proj) | serving | **SHIPPED**, at HBM floor (227.6 vs 795 µs, 101% floor) | D |
| T1: int8 (W8A16) the **unquantized BF16 weight mass** — lm_head, FA q/k/v, GDN in_proj/out_proj, shared expert, layer 0, the BF16 MTP draft layer (5.71 GB/step dense, 3.88 GB/step MoE) | P2 probe → serving A/B + KLD/PPL | **PROBE GO** (2026-08-31): 1.93× at lm_head (1717 µs vs the recorded 3114–3193 µs production floor), 2.00–2.42× at the other floor-bound shapes, ≥ 0.96× everywhere; per-channel scale beats per-128-group by 28 %. Ceiling dense +10–13 %, MoE +14–19 %; not yet an engine change. Note this does not contradict the `D` row above ("lm_head GEMV lever does not exist") — that killed *kernel-side* wins at the floor; this is the byte-side lever | INT8, `/local/tmp/int8-probes/p2-full.log` |
| T5: `v_dot8_i32_i4` (49 600 GMAC/s = 3.75× packed fp16, int4 on BOTH operands → weights stay int4) as the only >2× prefill dot on this chip; our W4A16 prefill sits at 5.46 T = 0.93× the *scalar fp32-FMA* record, i.e. ~1 MAC per issued instruction | **P3a** rate probe (LDS-staged BM=64/BN=64/BK=32, dot4 + dot8 inner loops, operand prep charged) vs hipBLAS fp16 7.57 T at [4096,34816,5120]; then **P3b** W4A4 KLD+PPL | **OPEN** (queued 2026-08-31; nothing may be built before the probe). Break-even is low — 1.3× needs 14 % of the dot8 record, 2× needs 22 % — the risks are operand supply (our `i4→i8 unpack + 2×sdot4 = 0.24×` row) and int4-activation accuracy on an already-int4 model (AWQ `qzeros` → epilogue corrections). Cheaper first step: ISA-audit the W4A16 prefill inner loop for packed-MAC usage (free, no accuracy risk) | INT8 §3.10, DEVLOG-int8-transfer.md, dequant-instructions.md |
| W1 GDN mixed-batch chunk reclass (~20 ms/step per no-draft seq) | 2-req mixed probe + identity + serving A/B | **SHIPPED** 2026-08-26: kernel spy 2016 wasted chunk calls → 0 (9B); spec side token-identical; 27B mixed 2-req ngram serving 59.35 vs 55.60 t/s = **+6.7 %** (4 samples/arm, ±0.3 %) | W1 |

## The recurring lessons (why the negative evidence is the point)

1. **Standalone harness wins do NOT transfer to serving wall-clock.** The
   profiler/launch-regime is pipeline-state dependent even in eager, not
   only in graph contexts. `G1 §4` measured the graph-per-kernel census to
   be impossible here (~7% visibility).
2. **Serving `_bench_gfx906.py` graph A/B is THE gate** for µs-scale
   verdicts; eager A/B can tie even when kernels differ (launch-bound).
3. PPL/prompt_logprobs is an unreliable gate on Gemma-4 (hybrid attention);
   gate on coherent text + logprob A/B (`GA`).

## Branch gfx906/fa-decode-fp16 (2026-09-10 → 2026-09-12) — rows added at branch closure

| Hyp | Gate | Verdict | Commit/revert | Comment | Refs |
|-----|------|---------|---------------|---------|------|
| T-1 int8-mass: int8 W8A16 the unquantized drafter fp16 mass | serving k=4 A/B | **NOT PASS** (final k=4 re-test parity −1%; earlier +15-18% was a cold-start misread) | code stays env-gated `T1_INT8_MASS=1` default OFF; archive candidate | quality gate was clean (0/120 argmax flips) — speed didn't transfer | DEVLOG-t1-int8-fp16-mass.md |
| SYV-12 context-lookup verify (MTP k=2 + fill) | production A/B @120k/64k | **DEAD-END** (v1 net loss; v2 payload-conditional; 2026-09-12 corpus agent: not workable in the current shape, corpus-independent) | deep-scrubbed; code grouped on `archive/syv12` | revival only if the fill/verify SHAPE changes | DEVLOG-syv12.md |
| FA ncols1=5/6 native tiles for Sq=5 verify | standalone VGPR/spill + wall @120k | **NO-WIN** (VGPR=128, spill 171/167; wall flat) | experiment code reverted | occupancy needs a build; config levers exhausted for Sq=5 | DEVLOG-fa-verify-sq8.md |
| KVSPLIT=32 uniform (all Sq) | same-boot A/B | **REJECTED** (bit-exact −10%) | shape-aware default shipped instead (32 for Sq≥4, 16 for Sq=2) | — | DEVLOG-fa-verify-sq8.md |
| MTP k=4 on real (agent+chat) payload | boot-Y mtp4ag 9/9 | **LOSS** (synthetic-payload win did not transfer) | depth set to k=3 | — | DEVLOG-mtp-depth-matrix.md |
| A3 fused multi-step draft metadata | serving A/B @k=4 | **NEUTRAL** | fork opt-in **stripped** 2026-09-13 (upstream default `False`); archived to `archive/a3-fused-draft` (@ `3fbb4e3f81`, code + tests + verdict + `A3-REVIVAL.md`); revival = V2 path + re-audit (k=7 never measured) | — | a3-strip-decision.md |
| FD-1 fused-draft decode-metadata path @B=4/120k | offline 4×122880 wall | **NEUTRAL** (stack-confounded: offline arm vs serving control) | **same code as A3** — A3's original opt-in was **stripped** 2026-09-13 (`f8a9400789`) and then **revived 2026-09-14** on the V2 bring-up branch, where the flag is again the opt-in for the fused multi-step draft-metadata protocol (`_generate_fused_drafts` is in-tree) with three tests incl. a reuse/corruption guard — so `VLLM_GFX906_FUSED_DRAFT` **does have a reader** (corrected 2026-09-17; the earlier "no reader" note was stale). FD-1's *neutral* verdict stands for the B=4/120k offline arm only; flip the default only after a same-stack gate | — | DEVLOG-spec-decode.md, fd1-keep-strip-decision.md |
| P1 (stall theory: prefix-cache/padding artifact) | D1a cached-KV probe | **DEAD** (5.327 vs 5.324 s control) | — | — | DEVLOG-ttft-prefill-stall.md |
